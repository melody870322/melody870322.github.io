---
import BaseLayout from '../../../../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="環境安裝與執行測試 — PyBullet"
  description="PyBullet 安裝流程教學，包含 VS Code、Anaconda、pip 與 PyBullet 的安裝步驟，以及常見問題排解。"
  lang="zh"
>
  <section class="section">
    <div class="container pb-setup">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">先把環境弄好：PyBullet 安裝與測試</h1>
        <p class="detail-subtitle">
          在開始使用 PyBullet 之前，最重要的一件事就是先把環境安裝好，而且要能成功跑起來。很多人卡關，其實不是因為不會寫程式，而是環境還沒設定好。
        </p>
        <p class="detail-subtitle" style="margin-top: var(--space-3);">
          這一頁的目標很單純：讓你的電腦可以正常執行 PyBullet，並且確認你真的能跑出第一個模擬畫面。
        </p>
      </header>

      <!-- Section: Tools -->
      <h2 class="detail-heading">你需要準備哪些工具？</h2>
      <p class="detail-paragraph">
        這一區會用到幾個常見工具，照順序裝就好：
      </p>

      <ol class="tool-list">
        <li class="tool-item">
          <span class="tool-num">1</span>
          <div class="tool-body">
            <h3 class="tool-name">Visual Studio Code</h3>
            <p class="tool-desc">寫程式用的編輯器</p>
          </div>
        </li>
        <li class="tool-item">
          <span class="tool-num">2</span>
          <div class="tool-body">
            <h3 class="tool-name">Anaconda</h3>
            <p class="tool-desc">用來管理 Python 環境（推薦）</p>
          </div>
        </li>
        <li class="tool-item">
          <span class="tool-num">3</span>
          <div class="tool-body">
            <h3 class="tool-name">pip</h3>
            <p class="tool-desc">安裝 Python 套件</p>
          </div>
        </li>
        <li class="tool-item">
          <span class="tool-num">4</span>
          <div class="tool-body">
            <h3 class="tool-name">PyBullet</h3>
            <p class="tool-desc">主角，物理模擬工具</p>
          </div>
        </li>
        <li class="tool-item">
          <span class="tool-num">5</span>
          <div class="tool-body">
            <h3 class="tool-name">NumPy / Pillow / Pygame</h3>
            <p class="tool-desc">後面做計算、影像、搖桿會用到</p>
          </div>
        </li>
      </ol>

      <!-- Section: Install Steps -->
      <h2 class="detail-heading">安裝流程</h2>
      <p class="detail-paragraph">
        依照下列順序，一步一步把環境建立起來。
      </p>

      <ol class="step-list">
        <li class="step-item">
          <span class="step-num">1</span>
          <div class="step-body">
            <h3 class="step-title">安裝 VS Code</h3>
            <p class="step-desc">
              VS Code 是用來寫和執行 Python 程式的工具。到官網下載安裝，依照你的作業系統選擇對應版本（Windows 或 Linux），安裝完成後打開即可使用。
            </p>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">2</span>
          <div class="step-body">
            <h3 class="step-title">安裝 Anaconda（建議）</h3>
            <p class="step-desc">
              Anaconda 可以幫你管理不同的 Python 環境，避免套件互相衝突。安裝完成後，建議建立一個獨立的環境：
            </p>
            <pre class="code-block"><code>conda create --name pybullet_env
conda activate pybullet_env</code></pre>
            <p class="step-note">
              不建議直接用 base 環境，之後比較容易出問題。
            </p>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">3</span>
          <div class="step-body">
            <h3 class="step-title">安裝 pip（如果需要）</h3>
            <p class="step-desc">
              有些環境可能已經有 pip，如果沒有可以用以下指令安裝：
            </p>
            <pre class="code-block"><code>conda install pip</code></pre>
            <p class="step-note">
              通常這個步驟僅 Windows 使用者需要操作。
            </p>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">4</span>
          <div class="step-body">
            <h3 class="step-title">安裝 PyBullet</h3>
            <p class="step-desc">
              這一步是重點。在終端機中輸入以下指令：
            </p>
            <pre class="code-block"><code>pip install pybullet</code></pre>
            <p class="step-desc" style="margin-top: var(--space-3);">
              安裝完成後，基本上就已經可以跑模擬了。
            </p>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">5</span>
          <div class="step-body">
            <h3 class="step-title">安裝其他套件（建議一起裝）</h3>
            <p class="step-desc">
              這些套件之後會用到，例如影像處理或搖桿控制：
            </p>
            <pre class="code-block"><code>pip install numpy pillow pygame</code></pre>
          </div>
        </li>
      </ol>

      <!-- Section: Troubleshooting -->
      <h2 class="detail-heading"><span class="warning-icon">&#9888;</span> 常見安裝問題</h2>
      <p class="detail-paragraph">
        安裝過程偶爾會遇到小狀況，這裡整理了最常見的問題與解法。
      </p>

      <div class="faq-card">
        <div class="faq-tag">Windows 常見問題</div>
        <h3 class="faq-question">
          安裝 PyBullet 時出現錯誤，提示缺少 C++ 編譯工具？
        </h3>
        <figure class="faq-figure">
          <img
            src="/learning/virtual-env/Windows使用者安裝PyBullet時可能遇到的Error .png"
            alt="Windows 使用者安裝 PyBullet 時可能遇到的錯誤畫面"
            loading="lazy"
          />
          <figcaption class="faq-caption">Windows 使用者安裝 PyBullet 時可能遇到的錯誤</figcaption>
        </figure>
        <p class="faq-desc">
          有可能是缺少 C++ 編譯工具，需要至 Microsoft 網頁去安裝 Microsoft C++ Build Tools。
        </p>
        <figure class="faq-figure">
          <img
            src="/learning/virtual-env/Microsoft C++ Build Tools 網頁.png"
            alt="Microsoft C++ Build Tools 網頁"
            loading="lazy"
          />
          <figcaption class="faq-caption">Microsoft C++ Build Tools 網頁</figcaption>
        </figure>
        <p class="faq-desc">
          安裝時記得勾選：
        </p>
        <ul class="faq-checklist">
          <li>C++ 桌面開發</li>
          <li>Windows SDK（請依照自身所使用版本擇一勾選即可）</li>
        </ul>
        <figure class="faq-figure">
          <img
            src="/learning/virtual-env/Microsoft C++ Build Tools 安裝選項.png"
            alt="Microsoft C++ Build Tools 安裝選項"
            loading="lazy"
          />
          <figcaption class="faq-caption">Microsoft C++ Build Tools 安裝選項</figcaption>
        </figure>
        <p class="faq-desc" style="margin-bottom: 0;">
          裝完後重新開機，再安裝一次 <code>pip install pybullet</code>，通常就可以成功。
        </p>
      </div>

      <div class="faq-card">
        <div class="faq-tag">套件問題</div>
        <h3 class="faq-question">
          出現 <code>numpy.core.multiarray failed to import</code> 錯誤？
        </h3>
        <p class="faq-desc">
          通常是版本問題，更新 NumPy 就可以解決：
        </p>
        <pre class="code-block" style="margin: 0;"><code>pip install --upgrade numpy</code></pre>
      </div>

      <div class="faq-card">
        <div class="faq-tag">環境問題</div>
        <h3 class="faq-question">
          VS Code 找不到環境？
        </h3>
        <p class="faq-desc">
          請在 VS Code 中進行以下操作：
        </p>
        <ol class="faq-steps">
          <li>
            按下 <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>P</kbd>
          </li>
          <li>
            選擇 <strong>Python: Select Interpreter</strong>
          </li>
          <li>
            選到你剛剛建立的 conda 環境（<code>pybullet_env</code>）
          </li>
        </ol>
      </div>

      <!-- Section: Verify -->
      <h2 class="detail-heading">最重要的一步：測試有沒有成功</h2>
      <p class="detail-paragraph">
        安裝完成後，不要急著寫新程式，先做一件事：跑一個範例，確認環境是正常的。
      </p>

      <div class="verify-card">
        <p class="verify-label">在終端機輸入：</p>
        <pre class="code-block" style="margin: 0 0 var(--space-4);"><code>python -m pybullet_robots.panda.loadpanda</code></pre>
        <p class="verify-desc">
          如果成功，你會看到一個機械手臂的模擬畫面。這代表：
        </p>
        <ul class="verify-checklist">
          <li>PyBullet 已經安裝成功</li>
          <li>環境設定正確</li>
        </ul>
        <p class="verify-desc">
          這一步在學習過程中非常關鍵，因為它能幫你確認「問題是不是出在環境」。
        </p>
      </div>

      <!-- Section: Reminders -->
      <h2 class="detail-heading"><span class="warning-icon">&#9888;</span> 小提醒（真的很重要）</h2>
      <p class="detail-paragraph">
        剛開始學的時候，最常見的狀況是：
      </p>
      <ul class="reminder-list">
        <li>環境沒切對</li>
        <li>套件裝在錯的地方</li>
        <li>VS Code 沒選到正確 interpreter</li>
      </ul>
      <p class="detail-paragraph">
        這些問題看起來很小，但會讓程式完全跑不起來。建議一定要先確認「範例可以跑」，再往下學。
      </p>

    </div>
  </section>
</BaseLayout>

<style>
  .pb-setup {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  /* Header */
  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  /* Section headings */
  .detail-heading {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-heading);
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-paragraph {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    margin-bottom: var(--space-4);
  }

  .detail-paragraph strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Warning icon */
  .warning-icon {
    color: var(--color-accent);
    margin-right: var(--space-2);
    font-size: 1.1em;
  }

  /* Tool list (numbered like steps) */
  .tool-list {
    list-style: none;
    padding: 0;
    margin: var(--space-6) 0 var(--space-8);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .tool-item {
    display: flex;
    align-items: center;
    gap: var(--space-6);
    padding: var(--space-8) var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: all var(--transition);
  }

  .tool-item:hover {
    border-color: var(--color-accent);
  }

  .tool-num {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-base);
    font-weight: 700;
  }

  .tool-body {
    flex: 1;
    min-width: 0;
  }

  .tool-name {
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-1);
  }

  .tool-desc {
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    line-height: 1.6;
  }

  /* FAQ figure (screenshot) */
  .faq-figure {
    margin: var(--space-4) 0;
    padding: var(--space-3);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg);
  }

  .faq-figure img {
    display: block;
    width: 100%;
    height: auto;
    border-radius: var(--border-radius);
  }

  .faq-caption {
    margin-top: var(--space-2);
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    text-align: center;
    line-height: 1.6;
  }

  /* Step list */
  .step-list {
    list-style: none;
    padding: 0;
    margin: var(--space-6) 0 var(--space-8);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .step-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-6);
    padding: var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: all var(--transition);
  }

  .step-item:hover {
    border-color: var(--color-accent);
  }

  .step-num {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-base);
    font-weight: 700;
  }

  .step-body {
    flex: 1;
    min-width: 0;
  }

  .step-title {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-heading);
    margin-bottom: var(--space-2);
  }

  .step-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    margin-bottom: var(--space-3);
  }

  .step-desc:last-child {
    margin-bottom: 0;
  }

  .step-desc strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .step-note {
    margin-top: var(--space-3);
    padding: var(--space-3) var(--space-4);
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    line-height: 1.8;
    background-color: var(--color-bg);
    border-left: 3px solid var(--color-accent);
    border-radius: var(--border-radius);
  }

  .step-note strong {
    color: var(--color-heading);
  }

  /* Code block */
  .code-block {
    display: block;
    margin: var(--space-3) 0 0;
    padding: var(--space-4);
    background-color: #0f172a;
    color: #e2e8f0;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: var(--text-xs);
    line-height: 1.7;
    border-radius: var(--border-radius);
    overflow-x: auto;
    white-space: pre;
  }

  .code-block code {
    font-family: inherit;
    color: inherit;
    background: none;
    padding: 0;
  }

  /* Keyboard key */
  kbd {
    display: inline-block;
    padding: 0 var(--space-2);
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: var(--text-xs);
    color: var(--color-text);
    background-color: var(--color-bg);
    border: 1px solid var(--color-border);
    border-bottom-width: 2px;
    border-radius: var(--border-radius);
    vertical-align: middle;
  }

  /* Inline code */
  .faq-card code,
  .step-desc code,
  .faq-question code {
    padding: 0.1em 0.4em;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 0.9em;
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
    border-radius: var(--border-radius);
  }

  /* FAQ card */
  .faq-card {
    margin: var(--space-6) 0 var(--space-4);
    padding: var(--space-8);
    border: 1px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-tag-bg);
  }

  .faq-tag {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    margin-bottom: var(--space-3);
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 0.05em;
    border-radius: var(--border-radius);
  }

  .faq-question {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.6;
  }

  .faq-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    margin-bottom: var(--space-4);
  }

  .faq-steps {
    padding-left: var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  .faq-steps strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .faq-checklist {
    list-style: disc;
    margin: var(--space-2) 0 0;
    padding-left: var(--space-6);
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  .faq-checklist li {
    margin-bottom: var(--space-1);
  }

  /* Verify card */
  .verify-card {
    margin: var(--space-6) 0 var(--space-8);
    padding: var(--space-8);
    border: 2px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .verify-label {
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
  }

  .verify-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  .verify-checklist {
    list-style: none;
    padding: 0;
    margin: var(--space-3) 0;
  }

  .verify-checklist li {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    padding-left: var(--space-6);
    position: relative;
  }

  .verify-checklist li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Reminder list */
  .reminder-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .reminder-list li {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .reminder-list li::before {
    content: '\2022';
    position: absolute;
    left: var(--space-2);
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Mobile */
  @media (max-width: 720px) {
    .tool-item {
      gap: var(--space-4);
      padding: var(--space-4) var(--space-6);
    }

    .tool-num {
      width: 2rem;
      height: 2rem;
      font-size: var(--text-sm);
    }

    .step-item {
      gap: var(--space-4);
      padding: var(--space-6);
    }

    .step-num {
      width: 2rem;
      height: 2rem;
      font-size: var(--text-sm);
    }

    .faq-card {
      padding: var(--space-6);
    }

    .verify-card {
      padding: var(--space-6);
    }
  }
</style>
