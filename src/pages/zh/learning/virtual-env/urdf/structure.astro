---
import BaseLayout from '../../../../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="模型結構與組成說明 — URDF 格式與模型描述"
  description="拆開來看 URDF 模型的核心元素：Link、Joint、Visual、Collision、Inertial，以及位置與方向，理解一台機器人在數位世界裡是怎麼一塊一塊組起來的。"
  lang="zh"
>
  <section class="section">
    <div class="container urdf-structure">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">拆開來看：URDF 模型是怎麼組成的？</h1>
        <p class="detail-subtitle">
          剛剛提到，URDF 就像機器人的數位藍圖。它會告訴電腦機器人有哪些零件、這些零件怎麼接在一起，以及模型該怎麼被讀取和使用。如果把整份 URDF 拆開來看，最重要的內容可以先分成兩部分。
        </p>
      </header>

      <!-- Section: Intro overview -->
      <h2 class="detail-heading">URDF 的兩個主要組成</h2>
      <div class="overview-grid">
        <div class="overview-card">
          <p class="overview-tag">第一部分</p>
          <h3 class="overview-title">Link 和 Joint</h3>
          <p class="overview-desc">
            負責把機器人的結構搭起來。Link 是零件，Joint 是把零件接起來的關節。
          </p>
        </div>
        <div class="overview-card">
          <p class="overview-tag">第二部分</p>
          <h3 class="overview-title">Visual、Collision 和 Inertial</h3>
          <p class="overview-desc">
            幫每個零件補上外觀、碰撞和物理資訊，讓模型不只看得見，也能在模擬器裡正常運作。
          </p>
        </div>
      </div>

      <div class="callout-card">
        <p class="callout-text">
          你也可以把它想成在拼一台機器人：先做出一個個零件，再把它們接起來，最後補上外觀和基本物理資料。這樣一來，模型不只看得到，也比較能在模擬器裡正常運作。
        </p>
      </div>

      <!-- Section: Link -->
      <h2 class="detail-heading">Link：機器人的零件</h2>
      <p class="detail-paragraph">
        在 URDF 裡，Link 可以先想成機器人的一個零件。像是底座、手臂、前臂、輪子、夾爪，這些都可以是一個 link。
      </p>
      <p class="detail-paragraph">
        你可以把 Link 想成機器人的<strong>「身體部位」</strong>或<strong>「骨架零件」</strong>。如果只有 Link，機器人就像一堆分開的零件，還沒有真正組成完整結構。
      </p>
      <p class="detail-paragraph">
        在 URDF 裡，Link 底下常常會看到幾個很重要的組成：
      </p>

      <div class="concept-grid concept-grid--three">
        <div class="concept-card">
          <h3 class="concept-label">Visual</h3>
          <p class="concept-desc">外觀看起來怎麼樣</p>
        </div>
        <div class="concept-card">
          <h3 class="concept-label">Collision</h3>
          <p class="concept-desc">碰撞範圍怎麼算</p>
        </div>
        <div class="concept-card">
          <h3 class="concept-label">Inertial</h3>
          <p class="concept-desc">重量和物理性質怎麼設定</p>
        </div>
      </div>

      <p class="detail-paragraph">
        不過也要先知道，這些內容不一定每個 link 都會全部寫滿。在簡單模型裡，有時候只會先寫外觀；在需要做模擬時，才會再把碰撞和物理資訊補完整。
      </p>

      <!-- Section: Joint -->
      <h2 class="detail-heading">Joint：把零件接起來的關節</h2>
      <p class="detail-paragraph">
        如果 Link 是零件，那 Joint 就是把零件接起來的地方。它的工作很重要，因為它會告訴電腦：
      </p>
      <ul class="check-list">
        <li>哪兩個 Link 是連在一起的</li>
        <li>它們之間可以怎麼動</li>
        <li>關節裝在什麼位置</li>
        <li>是沿哪個方向運動</li>
      </ul>
      <p class="detail-paragraph">
        也就是說，Joint 不只是「接起來」而已，它還決定了整台機器人的活動方式。
      </p>

      <p class="detail-paragraph">
        你也可以把整台機器人想成一棵樹：從底座開始，一個零件接一個零件往外長。這也是為什麼在 Joint 裡，常常會看到 <strong>Parent</strong> 和 <strong>Child</strong> 這種寫法。
      </p>

      <div class="relation-grid">
        <div class="relation-card relation-card--parent">
          <p class="relation-tag">Parent</p>
          <p class="relation-desc">比較像上層、主體的那個 link</p>
          <p class="relation-example">例如：肩膀、輪軸</p>
        </div>
        <div class="relation-arrow" aria-hidden="true">↓</div>
        <div class="relation-card relation-card--child">
          <p class="relation-tag">Child</p>
          <p class="relation-desc">接在下面、跟著連出去的那個 link</p>
          <p class="relation-example">例如：手臂、輪子</p>
        </div>
      </div>

      <!-- Section: Joint types -->
      <h2 class="detail-heading">常見的 Joint 類型</h2>
      <p class="detail-paragraph">
        Joint 不只一種。不同類型的關節，決定了零件之間能不能動、怎麼動。入門時，先把這四種熟悉起來就很好用了。
      </p>

      <ol class="stage-list">
        <li class="stage-item">
          <span class="stage-num">1</span>
          <div class="stage-body">
            <h3 class="stage-title">Fixed（固定）</h3>
            <p class="stage-desc">
              這種 Joint 不會動，像是直接把兩個零件鎖在一起。如果兩個部分本來就應該固定不動，通常就會用 Fixed。
            </p>
          </div>
        </li>
        <li class="stage-item">
          <span class="stage-num">2</span>
          <div class="stage-body">
            <h3 class="stage-title">Revolute（旋轉）</h3>
            <p class="stage-desc">
              這種 Joint 可以轉動，但有角度限制。它很像手肘或膝蓋，只能在一定範圍內轉。
            </p>
          </div>
        </li>
        <li class="stage-item">
          <span class="stage-num">3</span>
          <div class="stage-body">
            <h3 class="stage-title">Continuous（連續旋轉）</h3>
            <p class="stage-desc">
              這種 Joint 也能轉動，但沒有角度限制。最常見的例子就是輪子，因為輪子通常可以一直轉下去。
            </p>
          </div>
        </li>
        <li class="stage-item">
          <span class="stage-num">4</span>
          <div class="stage-body">
            <h3 class="stage-title">Prismatic（滑動）</h3>
            <p class="stage-desc">
              這種 Joint 不是轉，而是直線滑動。你可以把它想成抽屜拉出來、推回去的那種感覺。
            </p>
          </div>
        </li>
      </ol>

      <p class="detail-paragraph">
        除了這幾種之外，還有其他類型，不過入門時先把這幾個熟悉起來就很好用了。
      </p>

      <!-- Section: Visual -->
      <h2 class="detail-heading">Visual：看起來長什麼樣子</h2>
      <p class="detail-paragraph">
        Visual 負責的是模型的外觀。簡單來說，就是這個零件在畫面上看起來長什麼樣。你可以在這裡設定：
      </p>
      <ul class="check-list">
        <li>幾何形狀，例如方塊、圓柱、球</li>
        <li>顏色</li>
        <li>材質</li>
        <li>匯入 3D 模型檔</li>
      </ul>
      <p class="detail-paragraph">
        所以 Visual 主要是給人看、給畫面顯示用的。當我們在模擬器裡看到一台機器人的外型，大多就是 Visual 在發揮作用。
      </p>

      <!-- Section: Collision -->
      <h2 class="detail-heading">Collision：碰撞要怎麼算</h2>
      <p class="detail-paragraph">
        Collision 是用來告訴電腦：這個零件的「實體範圍」大概在哪裡。
      </p>
      <p class="detail-paragraph">
        有了 Collision，模擬器才能知道模型有沒有撞到地板、牆壁，或是其他物件。如果沒有設定好碰撞範圍，模型就可能會像幽靈一樣穿過東西。
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          <strong>Collision 不一定要跟 Visual 長得一模一樣。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        很多時候，Visual 會做得比較精緻，讓畫面好看；但 Collision 會故意做得比較簡單，像是用方塊、圓柱或比較簡單的外殼去包住零件。
      </p>
      <p class="detail-paragraph">
        這不是偷懶，反而是很常見的做法。因為碰撞模型越簡單，模擬通常越穩、運算也比較快。
      </p>

      <!-- Section: Inertial -->
      <h2 class="detail-heading">Inertial：模型的物理資訊</h2>
      <p class="detail-paragraph">
        Inertial 負責的是這個零件的基本物理資訊。如果只是單純把模型畫出來，沒有 inertial 還不一定馬上看出問題；但只要進到物理模擬，這一塊就很重要。
      </p>
      <p class="detail-paragraph">
        它通常會影響像這些事情：
      </p>
      <ul class="check-list">
        <li>零件有多重</li>
        <li>重心大概在哪裡</li>
        <li>轉動起來的慣性有多大</li>
      </ul>
      <p class="detail-paragraph">
        這些設定會影響模型在模擬中的平衡、受力和運動表現。如果這部分設得很奇怪，機器人可能會看起來不穩、容易倒，甚至動起來很不自然。
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          所以可以把 inertial 想成：讓模型不只<strong>「看起來像真的」</strong>，而是<strong>「動起來也更像真的」</strong>的重要資料。
        </p>
      </div>

      <!-- Section: Position and direction -->
      <h2 class="detail-heading">只知道 Link 和 Joint 還不夠，還要注意位置和方向</h2>
      <p class="detail-paragraph">
        很多初學者一開始學 URDF 時，會先知道：
      </p>
      <ul class="check-list">
        <li>有 link</li>
        <li>有 joint</li>
        <li>joint 有不同種類</li>
      </ul>
      <p class="detail-paragraph">
        這些都對，但還有一個很常被忽略的重點：
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          <strong>關節裝在哪裡，以及它沿哪個方向動。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        如果這些設定沒放對，常見的情況會像是：
      </p>
      <ul class="cross-list">
        <li>手臂轉動方向怪怪的</li>
        <li>輪子歪著轉</li>
        <li>零件接起來的位置不對</li>
        <li>整台模型看起來歪掉</li>
      </ul>
      <p class="detail-paragraph">
        所以在實際寫 URDF 時，除了零件和關節本身，<strong>「位置」和「方向」</strong>也是非常重要的一部分。
      </p>

      <!-- Section: Summary -->
      <h2 class="detail-heading">總結</h2>
      <p class="detail-paragraph">
        如果用最簡單的方式來看，一份 URDF 的核心工作，其實就是反覆做兩件事：
      </p>

      <div class="summary-grid">
        <div class="summary-card">
          <p class="summary-step">第一步</p>
          <h3 class="summary-title">先做出零件</h3>
          <p class="summary-desc">
            用 <strong>Link</strong> 建立機器人的各個部分，再幫這些零件補上：
          </p>
          <ul class="summary-list">
            <li><strong>Visual</strong>：外觀看起來怎樣</li>
            <li><strong>Collision</strong>：碰撞範圍怎麼算</li>
            <li><strong>Inertial</strong>：物理資訊怎麼設定</li>
          </ul>
        </div>
        <div class="summary-card">
          <p class="summary-step">第二步</p>
          <h3 class="summary-title">把零件接起來</h3>
          <p class="summary-desc">
            用 <strong>Joint</strong> 把這些零件串起來，並且告訴電腦：
          </p>
          <ul class="summary-list">
            <li>誰接誰</li>
            <li>這是什麼類型的關節</li>
            <li>關節裝在哪裡</li>
            <li>會沿哪個方向運動</li>
          </ul>
        </div>
      </div>

      <p class="detail-paragraph">
        當這兩件事慢慢完成後，一台機器人的模型就會越來越完整。
      </p>

    </div>
  </section>
</BaseLayout>

<style>
  .urdf-structure {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  /* Header */
  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  /* Section headings */
  .detail-heading {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-heading);
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-paragraph {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    margin-bottom: var(--space-4);
  }

  .detail-paragraph strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Check list */
  .check-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .check-list li {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .check-list li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Cross list (✗ items) */
  .cross-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .cross-list li {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .cross-list li::before {
    content: '\2717';
    position: absolute;
    left: 0;
    color: var(--color-text-secondary);
    font-weight: 700;
  }

  /* Callout card */
  .callout-card {
    margin: var(--space-6) 0 var(--space-8);
    padding: var(--space-6) var(--space-8);
    border-left: 4px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .callout-text {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.9;
    margin: 0;
  }

  .callout-text strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Highlight card (for important takeaways — replaces red text) */
  .highlight-card {
    margin: var(--space-4) 0 var(--space-6);
    padding: var(--space-5) var(--space-8);
    border: 1px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-tag-bg);
  }

  .highlight-text {
    font-size: var(--text-base);
    color: var(--color-heading);
    line-height: 1.8;
    margin: 0;
    text-align: center;
  }

  .highlight-text strong {
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Overview grid (Link & Joint / Visual Collision Inertial intro) */
  .overview-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
    margin: var(--space-6) 0 var(--space-6);
  }

  .overview-card {
    padding: var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: all var(--transition);
  }

  .overview-card:hover {
    border-color: var(--color-accent);
  }

  .overview-tag {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    margin-bottom: var(--space-3);
    font-size: var(--text-xs);
    font-weight: 700;
    color: var(--color-accent);
    background-color: var(--color-tag-bg);
    border-radius: var(--border-radius);
    letter-spacing: 0.05em;
  }

  .overview-title {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
  }

  .overview-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  /* Concept grid (Visual / Collision / Inertial) */
  .concept-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
    margin: var(--space-6) 0 var(--space-8);
  }

  .concept-grid--three {
    grid-template-columns: repeat(3, 1fr);
  }

  .concept-card {
    padding: var(--space-6);
    border: 2px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: all var(--transition);
  }

  .concept-card:hover {
    transform: translateY(-2px);
  }

  .concept-label {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-accent);
    margin-bottom: var(--space-2);
    letter-spacing: 0.02em;
  }

  .concept-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  /* Relation (Parent / Child) */
  .relation-grid {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-3);
    margin: var(--space-6) 0 var(--space-8);
  }

  .relation-card {
    width: 100%;
    max-width: 28rem;
    padding: var(--space-6) var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    text-align: center;
  }

  .relation-card--parent {
    border-color: var(--color-accent);
  }

  .relation-card--child {
    border-color: var(--color-border);
    background-color: var(--color-bg);
  }

  .relation-tag {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-accent);
    margin-bottom: var(--space-2);
    letter-spacing: 0.04em;
  }

  .relation-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.7;
    margin-bottom: var(--space-2);
  }

  .relation-example {
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  .relation-arrow {
    font-size: var(--text-xl);
    color: var(--color-accent);
    line-height: 1;
  }

  /* Stage list (numbered Joint types) */
  .stage-list {
    list-style: none;
    padding: 0;
    margin: var(--space-6) 0 var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .stage-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-6);
    padding: var(--space-6) var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: all var(--transition);
  }

  .stage-item:hover {
    border-color: var(--color-accent);
  }

  .stage-num {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-base);
    font-weight: 700;
  }

  .stage-body {
    flex: 1;
    min-width: 0;
  }

  .stage-title {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-heading);
    margin-bottom: var(--space-2);
  }

  .stage-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  /* Summary grid (final two-step recap) */
  .summary-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
    margin: var(--space-6) 0 var(--space-6);
  }

  .summary-card {
    padding: var(--space-8);
    border: 1px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .summary-step {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    margin-bottom: var(--space-3);
    font-size: var(--text-xs);
    font-weight: 700;
    color: #fff;
    background-color: var(--color-accent);
    border-radius: var(--border-radius);
    letter-spacing: 0.05em;
  }

  .summary-title {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
  }

  .summary-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    margin-bottom: var(--space-3);
  }

  .summary-desc strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .summary-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .summary-list li {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .summary-list li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  .summary-list strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Mobile */
  @media (max-width: 720px) {
    .overview-grid,
    .concept-grid,
    .concept-grid--three,
    .summary-grid {
      grid-template-columns: 1fr;
    }

    .stage-item {
      gap: var(--space-4);
      padding: var(--space-6);
    }

    .stage-num {
      width: 2rem;
      height: 2rem;
      font-size: var(--text-sm);
    }

    .overview-card,
    .summary-card {
      padding: var(--space-6);
    }

    .relation-card {
      padding: var(--space-5) var(--space-6);
    }

    .highlight-card {
      padding: var(--space-4) var(--space-6);
    }

    .highlight-text {
      font-size: var(--text-sm);
    }
  }
</style>
