---
import BaseLayout from '../../../../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="模型常見問題與排查方式 — URDF 格式與模型描述"
  description="做完基本預覽後，如果模型還是哪裡怪怪的，就從這四種情況開始查：結構沒真的接成完整模型、關節動得不合理、預覽正常卻進不了模擬器、檔案越寫越亂到難以維護。"
  lang="zh"
>
  <section class="section">
    <div class="container urdf-troubleshoot">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">模型還是不對？這幾種問題最值得先查</h1>
        <p class="detail-subtitle">
          剛剛比較像是在做第一層檢查：模型有沒有出來、零件有沒有接錯、位置和方向大致對不對。但實際上，URDF 真正麻煩的地方，常常不是這些第一眼就看得出來的錯，而是<strong>模型看起來好像差不多，實際上卻還有更深一層的問題</strong>。
        </p>
      </header>

      <!-- Section: 4 cases overview -->
      <h2 class="detail-heading">這裡要整理的四種情況</h2>
      <p class="detail-paragraph">
        這裡不再重複前面那些基本檢查，而是整理四種更值得單獨拿出來排查的狀況：
      </p>

      <ol class="cases-list">
        <li class="cases-item">
          <span class="cases-num">1</span>
          <span class="cases-text">結構其實沒有真的接成一台完整模型</span>
        </li>
        <li class="cases-item">
          <span class="cases-num">2</span>
          <span class="cases-text">關節會動，但限制不合理</span>
        </li>
        <li class="cases-item">
          <span class="cases-num">3</span>
          <span class="cases-text">預覽看起來正常，進模擬器卻問題很多</span>
        </li>
        <li class="cases-item">
          <span class="cases-num">4</span>
          <span class="cases-text">檔案越寫越亂，開始難以維護</span>
        </li>
      </ol>

      <div class="callout-card">
        <p class="callout-text">
          如果你已經做過基本預覽，模型還是怪怪的，就<strong>很適合從這四種情況開始查</strong>。
        </p>
      </div>

      <!-- Case 1 -->
      <div class="case-header">
        <p class="case-label">狀況一</p>
        <h2 class="case-title">模型看起來有很多零件，但其實沒有接成完整結構</h2>
      </div>
      <p class="detail-paragraph">
        這種情況不一定一眼就看得出來。你可能會覺得：
      </p>
      <ul class="check-list">
        <li><code>link</code> 都有寫</li>
        <li><code>joint</code> 也有寫</li>
        <li>好像每個零件都在</li>
      </ul>
      <p class="detail-paragraph">
        但整體看起來就是不像一台完整的機器人。有時候會出現像這樣的狀況：
      </p>
      <ul class="check-list">
        <li>模型分成兩塊，彼此沒接起來</li>
        <li>有些零件像是漂在旁邊，自成一組</li>
        <li>明明每個部分都存在，但整體沒有一個清楚的主體</li>
      </ul>

      <div class="highlight-card">
        <p class="highlight-text">
          這時候要回頭想一件事：<strong>URDF 的結構很像一棵樹。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        也就是說，整台機器人不是把零件隨便堆在一起，而是要有清楚的連接關係，一個接一個串起來。
      </p>

      <h3 class="sub-heading">先查這幾個地方</h3>
      <ol class="diagnosis-list">
        <li class="diagnosis-item">
          <span class="diagnosis-num">1</span>
          <div class="diagnosis-body">
            <h4 class="diagnosis-title">每個 link 是不是都有真正接到整體結構上</h4>
            <p class="diagnosis-desc">
              有些 link 雖然寫了，但沒有透過 <code>joint</code> 接到整體模型裡，結果就會像一塊獨立零件。
            </p>
          </div>
        </li>
        <li class="diagnosis-item">
          <span class="diagnosis-num">2</span>
          <div class="diagnosis-body">
            <h4 class="diagnosis-title">是不是不小心做出兩個分開的部分</h4>
            <p class="diagnosis-desc">
              有時候你以為自己在做一台機器人，實際上卻做成兩棵分開的小樹，彼此沒有連起來。
            </p>
          </div>
        </li>
        <li class="diagnosis-item">
          <span class="diagnosis-num">3</span>
          <div class="diagnosis-body">
            <h4 class="diagnosis-title">整個模型有沒有一個清楚的起點</h4>
            <p class="diagnosis-desc">
              通常會有一個比較像本體或底座的部分，整體是從那裡一路接出去的。如果連這個起點都不清楚，整個結構就很容易亂掉。
            </p>
          </div>
        </li>
      </ol>

      <h3 class="sub-heading">可以怎麼理解？</h3>
      <p class="detail-paragraph">
        如果前面那種「位置錯」比較像是零件放歪了，這一種更像是：
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          <strong>骨架本身還沒真正搭起來。</strong>
        </p>
      </div>

      <!-- Case 2 -->
      <div class="case-header">
        <p class="case-label">狀況二</p>
        <h2 class="case-title">關節會動，但動得不合理</h2>
      </div>
      <p class="detail-paragraph">
        這種問題比「完全不會動」更麻煩。因為模型表面上看起來好像沒壞，關節也真的能動，<strong>但一動起來就讓人覺得很不對勁</strong>。常見的感覺像是：
      </p>
      <ul class="check-list">
        <li>關節一下就卡住</li>
        <li>本來只該動一點點，結果動過頭</li>
        <li>滑動關節衝得太遠</li>
        <li>旋轉關節轉到不合理的角度</li>
      </ul>

      <div class="highlight-card">
        <p class="highlight-text">
          這時候，問題通常不是「有沒有 joint」，而是：<strong>joint 的限制設定合不合理。</strong>
        </p>
      </div>

      <h3 class="sub-heading">先查這幾個地方</h3>
      <ol class="diagnosis-list">
        <li class="diagnosis-item">
          <span class="diagnosis-num">1</span>
          <div class="diagnosis-body">
            <h4 class="diagnosis-title">limit 有沒有漏寫</h4>
            <p class="diagnosis-desc">
              有些關節不是不能動，而是因為限制沒設好，結果表現得很奇怪。
            </p>
          </div>
        </li>
        <li class="diagnosis-item">
          <span class="diagnosis-num">2</span>
          <div class="diagnosis-body">
            <h4 class="diagnosis-title">限制範圍是不是太小或太大</h4>
            <p class="diagnosis-desc">
              如果範圍太小，關節看起來像一動就卡住；如果範圍太大，模型又可能動得太誇張。
            </p>
          </div>
        </li>
        <li class="diagnosis-item">
          <span class="diagnosis-num">3</span>
          <div class="diagnosis-body">
            <h4 class="diagnosis-title">有沒有把不同類型的關節概念混在一起</h4>
            <p class="diagnosis-desc">
              <strong>旋轉關節</strong>想的是「能轉多少」，<strong>滑動關節</strong>想的是「能移多遠」。如果這兩種概念沒有分清楚，模型就很容易雖然能動，卻動得不合理。
            </p>
          </div>
        </li>
      </ol>

      <h3 class="sub-heading">這種問題的特色是什麼？</h3>
      <p class="detail-paragraph">
        它不像位置錯那樣一眼就看出來，而是<strong>要在模型真的開始動時，問題才會慢慢浮現</strong>。所以如果你覺得：
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          <strong>模型不是不會動，而是動得不像你想像中的那台機器人。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        那就很值得回頭查 <code>joint</code> 的限制。
      </p>

      <!-- Case 3 -->
      <div class="case-header">
        <p class="case-label">狀況三</p>
        <h2 class="case-title">預覽看起來正常，但一進模擬器就問題很多</h2>
      </div>
      <p class="detail-paragraph">
        這種情況非常值得<strong>提前知道</strong>。很多模型在預覽工具裡看起來都沒什麼問題，但一放進真正有物理計算的環境後，問題就開始出現了。例如：
      </p>
      <ul class="check-list">
        <li>穿過地板</li>
        <li>和其他物體重疊</li>
        <li>碰撞反應很奇怪</li>
        <li>模型晃得很不自然</li>
        <li>一碰就倒，或像沒有重量一樣</li>
      </ul>

      <div class="highlight-card">
        <p class="highlight-text">
          這代表你要看的已經不只是外觀，而是：<strong>碰撞和物理資訊有沒有補完整。</strong>
        </p>
      </div>

      <h3 class="sub-heading">先查這幾個地方</h3>
      <ol class="diagnosis-list">
        <li class="diagnosis-item">
          <span class="diagnosis-num">1</span>
          <div class="diagnosis-body">
            <h4 class="diagnosis-title">有沒有設定 collision</h4>
            <p class="diagnosis-desc">
              如果只有 <code>visual</code>，模型看得到，但不代表碰撞一定算得對。需要碰撞檢查的零件，通常都要有合理的 <code>collision</code>。
            </p>
          </div>
        </li>
        <li class="diagnosis-item">
          <span class="diagnosis-num">2</span>
          <div class="diagnosis-body">
            <h4 class="diagnosis-title">collision 是不是設得太離譜</h4>
            <p class="diagnosis-desc">
              有時候不是沒寫，而是碰撞外殼太大或太小。這樣一來，模擬器裡的碰撞結果就會和你看到的外觀差很多。
            </p>
          </div>
        </li>
        <li class="diagnosis-item">
          <span class="diagnosis-num">3</span>
          <div class="diagnosis-body">
            <h4 class="diagnosis-title">有沒有補上 inertial</h4>
            <p class="diagnosis-desc">
              如果要做物理模擬，<code>inertial</code> 很重要。它會影響像是<strong>重量、重心和平衡</strong>。如果這一塊沒設好，模型動起來就很容易不自然。
            </p>
          </div>
        </li>
      </ol>

      <h3 class="sub-heading">這裡要先記住一件事</h3>
      <div class="callout-card">
        <p class="callout-text">
          預覽工具主要是幫你看<strong>模型外觀和基本結構</strong>，但真正和「物理」有關的問題，常常要等模型進到模擬器裡才會暴露出來。所以如果你的目標是後面要放進 <strong>PyBullet</strong> 或 <strong>Gazebo</strong>，那 <code>collision</code> 和 <code>inertial</code> 這兩塊就不能只是先放著不管。
        </p>
      </div>

      <!-- Case 4 -->
      <div class="case-header">
        <p class="case-label">狀況四</p>
        <h2 class="case-title">模型本身不一定錯，但檔案已經亂到很難改</h2>
      </div>
      <p class="detail-paragraph">
        這個問題和前面幾種不太一樣。它<strong>不一定會讓模型立刻歪掉，也不一定會讓畫面直接壞掉</strong>，但它會讓你越改越痛苦。常見的感覺像是：
      </p>
      <ul class="check-list">
        <li>同樣的內容一直重複貼</li>
        <li>改了一個地方，忘了別的地方也要一起改</li>
        <li>檔案一長，就開始找不到問題在哪</li>
        <li>明明模型本身不是大壞，但每次修改都像在拆炸彈</li>
      </ul>
      <p class="detail-paragraph">
        這時候，不一定是模型結構錯了，而是<strong>檔案已經變得很難維護</strong>。
      </p>

      <h3 class="sub-heading">這時可以做什麼？</h3>
      <p class="detail-paragraph">
        這時候就很適合開始接觸 <strong>Xacro</strong>。你可以先把 Xacro 想成：
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          <strong>幫你整理 URDF 的工具。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        它不是拿來按一下就幫你把錯修好，也不是專門用來抓 bug 的工具。它比較像是在幫你做這些事：
      </p>
      <ul class="check-list">
        <li>把重複的片段整理起來</li>
        <li>用變數統一管理尺寸</li>
        <li>減少一直複製貼上的機會</li>
        <li>讓整份檔案比較短、比較好讀</li>
      </ul>

      <h3 class="sub-heading">什麼時候該考慮用 Xacro？</h3>
      <p class="detail-paragraph">
        如果你開始覺得：
      </p>
      <ul class="check-list">
        <li>模型越來越大</li>
        <li>關節越來越多</li>
        <li>每改一次就怕漏改</li>
        <li>自己都快看不懂自己的 URDF</li>
      </ul>
      <p class="detail-paragraph">
        那通常就代表，差不多可以開始學 Xacro 了。
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          它的重點不是直接除錯，而是<strong>讓你之後比較不容易把檔案改亂。</strong>
        </p>
      </div>

    </div>
  </section>
</BaseLayout>

<style>
  .urdf-troubleshoot {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  /* Header */
  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  .detail-subtitle strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Section headings */
  .detail-heading {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-heading);
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-paragraph {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    margin-bottom: var(--space-4);
  }

  .detail-paragraph strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Sub-heading inside a section */
  .sub-heading {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-heading);
    margin-top: var(--space-8);
    margin-bottom: var(--space-3);
    padding-left: var(--space-3);
    border-left: 3px solid var(--color-accent);
  }

  /* Case header (狀況一/二/三/四) */

  .case-label {
    display: inline-block;
    margin-bottom: var(--space-0);
    padding: var(--space-1) var(--space-4);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.08em;
    color: #fff;
    background-color: var(--color-accent);
    border-radius: var(--border-radius);
  }

  .case-title {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-heading);
    margin: 0;
    line-height: 2.4;
  }

  /* Inline code */
  :global(.detail-paragraph) code,
  :global(.detail-subtitle) code,
  :global(.check-list) code,
  :global(.callout-text) code,
  :global(.highlight-text) code,
  :global(.diagnosis-desc) code,
  :global(.diagnosis-title) code {
    padding: 0.1em 0.4em;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 0.9em;
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
    border-radius: var(--border-radius);
  }

  /* Check list */
  .check-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .check-list li {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .check-list li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Callout card */
  .callout-card {
    margin: var(--space-6) 0 var(--space-8);
    padding: var(--space-4) var(--space-8);
    border-left: 4px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .callout-text {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.9;
    margin: 0;
  }

  .callout-text strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Highlight card (key takeaway — replaces red text) */
  .highlight-card {
    margin: var(--space-4) 0 var(--space-6);
    padding: var(--space-3) var(--space-8);
    border: 1px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-tag-bg);
  }

  .highlight-text {
    font-size: var(--text-base);
    color: var(--color-heading);
    line-height: 1.8;
    margin: 0;
    text-align: center;
  }

  .highlight-text strong {
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Cases list (4 cases overview) */
  .cases-list {
    list-style: none;
    padding: 0;
    margin: var(--space-6) 0 var(--space-4);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3);
  }

  .cases-item {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    padding: var(--space-3) ;
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: border-color var(--transition);
  }

  .cases-item:hover {
    border-color: var(--color-accent);
  }

  .cases-num {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 50%;
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-xs);
    font-weight: 700;
  }

  .cases-text {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.6;
  }

  /* Diagnosis list (先查這幾個地方) */
  .diagnosis-list {
    list-style: none;
    padding: 0;
    margin: var(--space-4) 0 var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .diagnosis-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    padding: var(--space-4) var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg);
    transition: border-color var(--transition);
  }

  .diagnosis-item:hover {
    border-color: var(--color-accent);
  }

  .diagnosis-num {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 50%;
    background-color: var(--color-tag-bg);
    color: var(--color-accent);
    font-size: var(--text-xs);
    font-weight: 700;
  }

  .diagnosis-body {
    flex: 1;
    min-width: 0;
  }

  .diagnosis-title {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-2);
    line-height: 1.5;
  }

  .diagnosis-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    margin: 0;
  }

  .diagnosis-desc strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Mobile */
  @media (max-width: 720px) {
    .cases-list {
      grid-template-columns: 1fr;
    }

    .case-header {
      padding: var(--space-5) var(--space-6);
    }

    .case-title {
      font-size: var(--text-lg);
    }

    .callout-card,
    .highlight-card {
      padding: var(--space-5) var(--space-6);
    }

    .diagnosis-item {
      gap: var(--space-4);
      padding: var(--space-4) var(--space-5);
    }
  }
</style>
