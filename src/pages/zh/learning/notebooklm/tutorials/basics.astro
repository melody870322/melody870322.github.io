---
import BaseLayout from '../../../../../layouts/BaseLayout.astro';

const figBase = '/learning/notebooklm';
---

<BaseLayout
  title="NotebookLM 基礎入門"
  description="從建立筆記本、整理來源、對話提問，到儲存記事 — 一份適合第一次使用 NotebookLM 的入門指引。"
  lang="zh"
>
  <section class="section">
    <div class="container nb-basics">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">基礎入門：從建立筆記本到整理重點</h1>
        <p class="detail-subtitle">
          NotebookLM 可以把你手上的資料整理成一個可對話的知識空間。把 PDF、網頁、YouTube 逐字稿、
          Google 文件、音訊檔等資料放進去，再用聊天方式問它重點、比較差異、整理摘要，
          回答裡還會附上內嵌引用，方便回頭檢查來源。
        </p>
        <p class="platform-note">
          <span class="platform-note__label">環境說明</span>
          以下內容以<strong>電腦版介面</strong>為主，因為目前手機 app 在部分來源匯入與記事功能上仍有限制。
        </p>
      </header>

      <!-- Section 1: Pricing -->
      <h2 class="detail-heading">收費方案</h2>
      <p class="detail-paragraph">
        NotebookLM 分成 <strong>Standard、Plus、Pro、Ultra</strong> 幾個層級。Standard 是一般個人帳號
        可以直接使用的免費層級；如果你有 Google AI Plus、Google AI Pro、Google AI Ultra，
        或是使用符合條件的 Workspace / Workspace for Education 授權帳號，就可能拿到更高的使用上限。
      </p>
      <p class="detail-paragraph">
        剛開始接觸 NotebookLM，先用 Standard 其實就夠入門。
        等真的開始做專題、研究案或團隊協作時，再考慮升級會比較實在。
      </p>

      <div class="plan-table-wrapper">
        <table class="plan-table">
          <thead>
            <tr>
              <th scope="col">項目</th>
              <th scope="col">Standard<span class="plan-table__sub">免費</span></th>
              <th scope="col">Plus</th>
              <th scope="col">Pro</th>
              <th scope="col">Ultra</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <th scope="row">筆記本數量</th>
              <td>100 本</td>
              <td>200 本</td>
              <td>500 本</td>
              <td>500 本</td>
            </tr>
            <tr>
              <th scope="row">每本最多來源</th>
              <td>50</td>
              <td>100</td>
              <td>300</td>
              <td>600</td>
            </tr>
            <tr>
              <th scope="row">每日聊天提問</th>
              <td>50 次</td>
              <td>200 次</td>
              <td>500 次</td>
              <td>5,000 次</td>
            </tr>
            <tr>
              <th scope="row">Audio Overviews</th>
              <td>3 次</td>
              <td>6 次</td>
              <td>20 次</td>
              <td>200 次</td>
            </tr>
          </tbody>
        </table>
      </div>

      <p class="detail-paragraph plan-note">
        另外，Standard 還包含每天 3 次 Video Overviews，以及 Reports、Flashcards、Quizzes 各每天 10 次的額度。
        每日額度是 24 小時重置、月額度是 30 天重置。<strong>Custom chat 和 analytics</strong> 所有人都可用，
        <strong>advanced sharing</strong> 是付費用戶才有。
      </p>

      <section class="concept-card">
        <p class="concept-card__label">該看哪一個方案？</p>
        <p class="concept-card__body">
          個人用戶看 <strong>Google AI 方案</strong>；組織帳號則看學校或公司是否<strong>已有授權</strong>。
          若是組織管理的帳號，管理員也必須先開啟權限，才能使用 NotebookLM。
        </p>
      </section>

      <!-- Section 2: Create a notebook -->
      <h2 class="detail-heading">建立筆記本</h2>
      <p class="detail-paragraph">
        NotebookLM 的使用方式，很像先替每個主題開一個獨立資料夾。你先打開 NotebookLM，
        按下<strong>「建立新的筆記本」</strong>（圖一），接著就可以在跳出的視窗裡直接新增來源（圖二中間）。
        這一步很重要，因為 NotebookLM 不是要你把所有資料都丟進同一個大池子，
        而是希望你<strong>把不同主題拆成不同筆記本</strong>，之後提問和整理才不會互相干擾。
      </p>

      <figure class="figure">
        <img src={`${figBase}/建立新的筆記本.png`} alt="NotebookLM 首頁，顯示「建立新的筆記本」按鈕" loading="lazy" />
        <figcaption>圖一、建立新的筆記本</figcaption>
      </figure>

      <figure class="figure">
        <img src={`${figBase}/新增來源.png`} alt="新增來源的對話視窗，以及左上角的筆記本命名欄位" loading="lazy" />
        <figcaption>圖二、新增來源（左上角為筆記本命名位置）</figcaption>
      </figure>

      <p class="detail-paragraph">
        實際操作時，我會建議一開始就用<strong>「主題」</strong>來命名，而不是用很籠統的名稱。
        像是不要只叫「期中考」，而可以改成「行銷管理期中考重點」、「專題研究訪談資料」、「職涯講座整理」
        這種一看就知道用途的名字。這樣等你的筆記本變多時，就比較不會找不到，
        也比較不會把不相關的資料混在一起。取名位置在圖二的左上角。
      </p>

      <!-- Section 3: Add and organize sources -->
      <h2 class="detail-heading">加入與整理來源</h2>
      <p class="detail-paragraph">
        NotebookLM 的核心概念很簡單：<strong>先有來源，才有好回答</strong>。
        官方把「來源」定義成一份被匯入到 app 裡的靜態副本，NotebookLM 會根據這些來源回答問題或完成整理工作。
        也就是說，你放進去的資料品質越好，它給你的整理結果通常也越穩。
      </p>

      <p class="detail-paragraph">目前它支援的來源種類其實很廣：</p>
      <div class="source-grid">
        <span class="source-chip">PDF</span>
        <span class="source-chip">Google Docs</span>
        <span class="source-chip">Google Slides</span>
        <span class="source-chip">Google Sheets</span>
        <span class="source-chip">Web URL</span>
        <span class="source-chip">YouTube 公開影片</span>
        <span class="source-chip">音訊檔</span>
        <span class="source-chip">貼上文字</span>
        <span class="source-chip">圖片</span>
        <span class="source-chip">docx / txt / md / csv / pptx / ePub</span>
        <span class="source-chip">部分 Gemini Chats</span>
      </div>

      <p class="detail-paragraph">
        每個來源最多可到 <strong>500,000 字</strong>；若是本機上傳檔案，大小上限是 <strong>200MB</strong>。
        Standard 層級每本筆記本最多 50 個來源，升級後還能更多。
      </p>

      <section class="caution-card">
        <p class="caution-card__label">匯入來源常見的三個誤解</p>
        <ul class="caution-card__numbered">
          <li>
            <strong>網頁匯入只抓文字</strong>——不會把圖片、嵌入影片或子頁面一併抓進來，付費牆內容也不支援。
          </li>
          <li>
            <strong>YouTube 匯入的是字幕逐字稿，不是影片本身</strong>——所以影片必須是公開的，而且要有字幕；
            沒有語音內容的影片也不行。
          </li>
          <li>
            <strong>多數來源是靜態副本</strong>——Google 文件類型在原檔更新後，可以手動重新同步；
            但其他來源改了原檔不會自動跟著變。
          </li>
        </ul>
      </section>

      <p class="detail-paragraph">
        整理來源時，建議<strong>不要一股腦全丟進去</strong>。比較好的做法是，
        先留下和主題真正相關、可信度也比較高的資料，再把來源名稱改得清楚一點，
        例如「課堂講義第 3 章」、「訪談逐字稿 A」、「官網產品說明」。
        這樣之後提問時，才能更快鎖定它到底是根據哪一份資料回答。
      </p>

      <p class="detail-paragraph">
        還有一個很好用的小技巧：當你在同一個筆記本裡放了很多資料後，
        可以先在左側來源區<strong>只勾選某幾份來源</strong>（圖三箭頭處），再開始提問。
        你可以個別選取來源來限定對話範圍，這對整理不同版本文件、比對兩份資料差異會特別好用。
      </p>

      <figure class="figure">
        <img src={`${figBase}/選擇來源.png`} alt="左側來源區，可勾選特定來源來限定對話範圍" loading="lazy" />
        <figcaption>圖三、選擇來源（勾選後對話只會根據這幾份回答）</figcaption>
      </figure>

      <!-- Section 4: Chat and citations -->
      <h2 class="detail-heading">對話提問與查看引用</h2>
      <p class="detail-paragraph">
        來源放好之後，真正好用的地方才開始。NotebookLM 可以直接讓你用聊天方式提問，
        而且回答會以你筆記本裡的資料為基礎，<strong>不是那種憑空講一大段、卻不知道出處在哪裡的回答</strong>。
        它的回答會帶有清楚的內嵌引用，幫你核對內容是否可靠。
      </p>

      <p class="detail-paragraph">
        查看引用的方式也很直覺。當回答裡出現引用標記時，把滑鼠移上去，就能先看到完整引用文字；
        如果直接點下去，NotebookLM 會自動跳到原始來源的對應位置，讓你在上下文裡檢查它是不是有斷章取義。
        這也是 NotebookLM 很適合做重點整理、資料比對和教學內容彙整的原因——
        你不只是得到答案，還能順手查證。
      </p>

      <section class="concept-card">
        <p class="concept-card__label">提問越具體，回答越準</p>
        <p class="concept-card__body">
          當筆記本裡有很多來源時，問得越清楚，NotebookLM 越容易抓到真正相關的內容。
        </p>
        <ul class="concept-card__list">
          <li>
            <span class="prompt-bad">「幫我摘要」</span>
            → 改成
            <span class="prompt-good">「請整理第 2 章中，和使用者行為有關的三個重點」</span>
          </li>
          <li>
            <span class="prompt-bad">「有什麼差別？」</span>
            → 改成
            <span class="prompt-good">「比較訪談 A 與訪談 B 對同一議題的差異」</span>
          </li>
        </ul>
      </section>

      <p class="detail-paragraph">
        NotebookLM 很適合拿來做「根據資料整理」這件事，但前提仍然是你給它的來源要夠清楚、夠乾淨。
        它不是魔法工具——<strong>來源混亂，回答通常也會跟著混亂</strong>。
      </p>

      <!-- Section 5: Notes -->
      <h2 class="detail-heading">儲存記事</h2>
      <p class="detail-paragraph">
        很多人第一次用 NotebookLM，會把聊天視窗當成一次性問答區，
        但其實<strong>「記事」功能非常值得使用</strong>。目前有兩種基本做法：
      </p>

      <ol class="start-steps">
        <li class="start-step">
          <span class="start-step__num">A</span>
          <div class="start-step__body">
            <h4 class="start-step__title">自己新增一則 note</h4>
            <p class="start-step__desc">
              把想法或整理內容直接打進去，適合放自己的歸納、待辦、觀察或筆記。
            </p>
          </div>
        </li>
        <li class="start-step">
          <span class="start-step__num">B</span>
          <div class="start-step__body">
            <h4 class="start-step__title">儲存聊天回答</h4>
            <p class="start-step__desc">
              當看到一段很好的聊天回答時，按下左下角的<strong>「儲存至記事」</strong>，
              那段內容就會直接存到右邊的記事區。
            </p>
          </div>
        </li>
      </ol>

      <p class="detail-paragraph">
        這個功能的價值在於，你不需要每次都重新翻聊天紀錄。
        你可以把重要段落、整理好的架構、已經確認可用的說法先存成記事，之後再慢慢加工。
        每本筆記本最多可建立 <strong>1,000 則 notes</strong>，拿來累積長期整理內容是夠用的。
      </p>

      <section class="caution-card">
        <p class="caution-card__label">⚠️ 小提醒</p>
        <p class="caution-card__body">
          從聊天回應<strong>存下來的記事，建立後不能再編輯</strong>。
          所以如果想保留可修改的版本，建議用「自己新增 note」的方式另外記下來。
        </p>
      </section>

      <p class="detail-paragraph">
        更進一步來看，NotebookLM 的記事不只是備忘而已。
        你可以把<strong>單一記事轉成來源</strong>，也可以<strong>把所有記事一次轉成來源</strong>。
        這代表你可以先透過聊天整理出一版重點，再把這些重點回灌成新的來源，
        讓後續的提問建立在更濃縮、也更聚焦的知識基礎上。
        這對做教學大綱、專案整理、讀書筆記或知識庫都很實用。
      </p>

      <blockquote class="pullquote pullquote--primary">
        看到好內容，不要只看完就算了——記得存成記事。
      </blockquote>

      <p class="detail-paragraph">
        因為聊天內容會一直往下跑，但存成記事後，你就能把真正重要的內容留在筆記本裡，
        之後還能再加工、再整理、再轉成新的來源。這樣 NotebookLM 才會從「問答工具」
        變成<strong>真正能累積內容的工作台</strong>。
      </p>
    </div>
  </section>
</BaseLayout>

<style>
  .nb-basics {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  /* Header */
  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.8;
    margin-bottom: var(--space-4);
  }

  .platform-note {
    margin-top: var(--space-4);
    padding: var(--space-3) var(--space-4);
    background-color: var(--color-bg-secondary);
    border-left: 3px solid var(--color-accent);
    border-radius: 0 var(--border-radius) var(--border-radius) 0;
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  .platform-note__label {
    display: inline-block;
    margin-right: var(--space-2);
    padding: 0 var(--space-2);
    background-color: var(--color-tag-bg);
    color: var(--color-accent);
    border-radius: var(--border-radius);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.05em;
  }

  .platform-note strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Section headings */
  .detail-heading {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-heading);
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-paragraph {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    margin-bottom: var(--space-4);
  }

  .detail-paragraph strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .plan-note {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    margin-top: var(--space-4);
  }

  /* Plan comparison table */
  .plan-table-wrapper {
    margin: var(--space-6) 0 var(--space-4);
    overflow-x: auto;
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
  }

  .plan-table {
    width: 100%;
    border-collapse: collapse;
    font-size: var(--text-sm);
    min-width: 36rem;
  }

  .plan-table thead th {
    padding: var(--space-4) var(--space-4);
    background-color: var(--color-bg-secondary);
    color: var(--color-heading);
    font-weight: 700;
    text-align: left;
    border-bottom: 1px solid var(--color-border);
  }

  .plan-table thead th:first-child {
    color: var(--color-text-secondary);
    font-weight: 600;
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }

  .plan-table__sub {
    display: block;
    font-size: var(--text-xs);
    font-weight: 500;
    color: var(--color-accent);
    margin-top: 2px;
  }

  .plan-table tbody tr:not(:last-child) {
    border-bottom: 1px solid var(--color-border);
  }

  .plan-table tbody th {
    padding: var(--space-3) var(--space-4);
    text-align: left;
    font-weight: 600;
    color: var(--color-text);
    background-color: var(--color-bg-secondary);
    font-size: var(--text-sm);
    white-space: nowrap;
  }

  .plan-table tbody td {
    padding: var(--space-3) var(--space-4);
    color: var(--color-text);
    font-weight: 500;
  }

  /* Concept card */
  .concept-card {
    margin: var(--space-6) 0 var(--space-4);
    padding: var(--space-6) var(--space-8);
    border: 1px solid var(--color-accent);
    background-color: var(--color-tag-bg);
    border-radius: var(--border-radius);
  }

  .concept-card__label {
    font-size: var(--text-xs);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-accent);
    margin-bottom: var(--space-2);
  }

  .concept-card__body {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
  }

  .concept-card__body strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .concept-card__list {
    margin: var(--space-3) 0 0;
    padding-left: var(--space-5);
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.9;
  }

  .concept-card__list li {
    margin-bottom: var(--space-2);
  }

  .prompt-bad,
  .prompt-good {
    display: inline-block;
    padding: 2px var(--space-2);
    border-radius: var(--border-radius);
    font-size: var(--text-sm);
  }

  .prompt-bad {
    background-color: var(--color-bg-secondary);
    color: var(--color-text-secondary);
    text-decoration: line-through;
  }

  .prompt-good {
    background-color: var(--color-bg);
    color: var(--color-heading);
    border: 1px solid var(--color-accent);
    font-weight: 500;
  }

  /* Caution card */
  .caution-card {
    margin: var(--space-6) 0 var(--space-4);
    padding: var(--space-6) var(--space-8);
    border: 1px solid var(--color-border);
    border-left: 4px solid var(--color-text-secondary);
    background-color: var(--color-bg-secondary);
    border-radius: 0 var(--border-radius) var(--border-radius) 0;
  }

  .caution-card__label {
    font-size: var(--text-xs);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-text-secondary);
    margin-bottom: var(--space-3);
  }

  .caution-card__body {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
  }

  .caution-card__body strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .caution-card__numbered {
    margin: 0;
    padding-left: var(--space-5);
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.9;
    counter-reset: caution;
    list-style: none;
  }

  .caution-card__numbered li {
    position: relative;
    margin-bottom: var(--space-3);
    padding-left: var(--space-3);
    counter-increment: caution;
  }

  .caution-card__numbered li::before {
    content: counter(caution);
    position: absolute;
    left: -1.25rem;
    top: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.25rem;
    height: 1.25rem;
    border-radius: 50%;
    background-color: var(--color-text-secondary);
    color: var(--color-bg);
    font-size: var(--text-xs);
    font-weight: 700;
  }

  .caution-card__numbered strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Source chips */
  .source-grid {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    margin: var(--space-4) 0 var(--space-6);
  }

  .source-chip {
    display: inline-block;
    padding: var(--space-2) var(--space-4);
    background-color: var(--color-bg-secondary);
    color: var(--color-text);
    border: 1px solid var(--color-border);
    border-radius: 9999px;
    font-size: var(--text-sm);
    font-weight: 500;
  }

  /* Figure with screenshot */
  .figure {
    margin: var(--space-6) 0;
    padding: var(--space-3);
    background-color: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
  }

  .figure img {
    display: block;
    width: 100%;
    height: auto;
    border-radius: calc(var(--border-radius) - 2px);
  }

  .figure figcaption {
    margin-top: var(--space-3);
    padding-bottom: var(--space-1);
    text-align: center;
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: 1.6;
  }

  /* Pullquote */
  .pullquote {
    margin: var(--space-8) 0;
    padding: var(--space-6) var(--space-8);
    border-left: 3px solid var(--color-accent);
    background-color: var(--color-tag-bg);
    border-radius: 0 var(--border-radius) var(--border-radius) 0;
    font-size: var(--text-lg);
    font-weight: 500;
    color: var(--color-heading);
    line-height: 1.7;
  }

  .pullquote--primary {
    font-size: var(--text-xl);
    font-weight: 700;
    border-left-width: 4px;
  }

  /* Start steps */
  .start-steps {
    list-style: none;
    padding: 0;
    margin: var(--space-4) 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .start-step {
    display: flex;
    align-items: flex-start;
    gap: var(--space-8);
    padding: var(--space-6) var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .start-step__num {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: 50%;
    border: 1.5px solid var(--color-accent);
    color: var(--color-accent);
    background-color: var(--color-bg);
    font-size: var(--text-sm);
    font-weight: 700;
  }

  .start-step__body {
    flex: 1;
    padding-top: var(--space-1);
  }

  .start-step__title {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-heading);
    margin-bottom: var(--space-2);
  }

  .start-step__desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  /* Mobile */
  @media (max-width: 720px) {
    .plan-table thead th,
    .plan-table tbody th,
    .plan-table tbody td {
      padding: var(--space-2) var(--space-3);
    }

    .concept-card,
    .caution-card {
      padding: var(--space-5) var(--space-6);
    }

    .pullquote {
      padding: var(--space-5) var(--space-6);
      font-size: var(--text-base);
    }

    .pullquote--primary {
      font-size: var(--text-lg);
    }

    .start-step {
      gap: var(--space-5);
      padding: var(--space-5) var(--space-6);
    }
  }
</style>
