---
import BaseLayout from '../../../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Claude Code 基礎操作與流程"
  description="從基礎對話、交付內容、權限與計畫模式，到倒帶救援與處理錯誤——帶你熟悉在 VS Code 中操作 Claude Code 的日常流程。"
  lang="zh"
>
  <section class="section">
    <div class="container cc-basics">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">Claude Code 基礎操作與流程</h1>
        <p class="detail-subtitle">
          在 VS Code 中操作 Claude Code，比起純文字的終端機，多了許多直覺的圖形按鈕與滑鼠操作，
          很適合初學者上手。把它當作一位坐在你旁邊、看得懂你螢幕的「貼身 AI 助教」就好。
        </p>
      </header>

      <!-- Section 1: 基礎對話與模型切換 -->
      <h2 class="detail-heading">基礎對話與模型切換</h2>
      <p class="detail-paragraph">
        Claude Code 在 VS Code 中擁有一個專屬的側邊欄面板，不需要記任何複雜指令，
        像在用 ChatGPT 或 Gemini 一樣，用白話文輸入需求即可。
      </p>

      <ol class="step-list">
        <li class="step-item">
          <span class="step-num">1</span>
          <div class="step-body">
            <h3 class="step-title">開始聊天</h3>
            <p class="step-desc">
              點開 VS Code 側邊欄的 Claude 圖示後，你會看到底部的輸入框，這就像用 ChatGPT 一樣，
              直接用白話文輸入需求即可。需要換行時按
              <kbd>Shift</kbd>+<kbd>Enter</kbd>，寫完後按 <kbd>Enter</kbd> 送出指令。
            </p>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">2</span>
          <div class="step-body">
            <h3 class="step-title">管理對話</h3>
            <p class="step-desc">
              點擊上方的 <strong>+ New session</strong> 可以隨時開啟全新對話，下方則能看到以前聊過的紀錄。
              你也可以幫歷史紀錄「重新命名」，方便日後查找。
            </p>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">3</span>
          <div class="step-body">
            <h3 class="step-title">切換聰明程度</h3>
            <p class="step-desc">
              在輸入框輸入 <code>/switch model</code>（其實打 <code>/</code> 就會跳出選單），
              可以切換不同的 AI 大腦（模型）。例如遇到超複雜的邏輯推理時，
              可以切換成最聰明的 <strong>Opus</strong> 模型。
            </p>
          </div>
        </li>
      </ol>

      <!-- Section 2: 把正確內容交給 Claude Code -->
      <h2 class="detail-heading">怎麼把正確內容交給 Claude Code</h2>
      <p class="detail-paragraph">
        要讓 AI 幫到忙，最重要的是讓它「看到對的東西」。在 VS Code 裡，你有三種主要方式可以把內容交給它：
      </p>

      <div class="method-grid">
        <article class="method-card">
          <p class="method-symbol">@</p>
          <h3 class="method-title">用 @ 指定檔案或資料夾</h3>
          <p class="method-desc">
            在對話框輸入 <code>@</code>，再加上檔案名稱，Claude Code 就會把那個檔案納入這次對話的參考。
            適合你已經知道要指定哪個檔案、哪個資料夾的時候使用。
          </p>
          <ul class="method-examples">
            <li>請幫我檢查 <code>@index.html</code> 裡的表單結構有沒有問題</li>
            <li>閱讀 <code>@src/app.js</code>，告訴我這支程式主要在做什麼</li>
            <li>根據 <code>@components</code> 這個資料夾，幫我整理目前有哪些元件</li>
          </ul>
        </article>

        <article class="method-card">
          <p class="method-symbol">+</p>
          <h3 class="method-title">用左下角的 + 加入檔案</h3>
          <p class="method-desc">
            如果你比較習慣用滑鼠操作，可以按對話框左下角的 <strong>+</strong>，把檔案加入這次對話。
            可以把它理解成 <code>@</code> 是直接在問題裡點名，<code>+</code> 則是把檔案附加給 Claude Code 參考。
          </p>
          <ul class="method-examples">
            <li>按左下角 <strong>+</strong> → 選擇 <code>styles.css</code></li>
            <li>輸入：請幫我整理這份 CSS，讓按鈕、標題的樣式更一致</li>
            <li>適合不想記檔名格式的新手，直接點選即可</li>
          </ul>
        </article>

        <article class="method-card">
          <p class="method-symbol">▥</p>
          <h3 class="method-title">直接反白要處理的程式碼</h3>
          <p class="method-desc">
            如果你只想處理某一小段程式碼，不一定要整個檔案都交給 Claude Code，
            直接在編輯器裡反白那一段，然後直接發問即可。
          </p>
          <ul class="method-examples">
            <li>幫我解釋這段程式碼在做什麼</li>
            <li>幫我把這段改寫得更容易讀</li>
            <li>請保留原本功能，幫我重構這段</li>
          </ul>
        </article>
      </div>

      <div class="info-card">
        <p class="info-title">給新手的簡單理解</p>
        <ul class="info-list">
          <li><strong>@</strong>：我直接點名要你看哪個檔案</li>
          <li><strong>+</strong>：我把檔案附加給你看</li>
          <li><strong>反白</strong>：我只要你看我現在選到的這一段</li>
        </ul>
      </div>

      <p class="detail-paragraph">
        想更精準時，也可以指定到行號。例如 <code>@app.js#10-25</code> 會告訴 Claude Code「就看這個檔案的第 10 到 25 行」，
        特別適合精準討論某幾行、避免它看太多不相關內容、或做教學示範時明確指出範圍。
      </p>

      <!-- Section 3: 權限模式 -->
      <h2 class="detail-heading">先看懂權限模式，不要一開始就全開自動</h2>
      <p class="detail-paragraph">
        很多人第一次用 Claude Code，最不習慣的地方就是：它怎麼一直跳出來問「要不要允許」？
      </p>
      <p class="detail-paragraph">
        其實這不是在找麻煩，而是在保護你的專案。Claude Code 的權限設計本來就偏保守：預設情況下，它不會一上來就什麼都自己做。像讀檔、搜尋這類偏唯讀的動作通常比較順；但一旦要改檔案、執行指令，或碰到比較敏感的操作，就會看你目前的權限模式和規則，決定是不是先問你。你也可以用 <code>/permissions</code> 去查看和管理目前的 allow、ask、deny 規則。
      </p>
      <p class="detail-paragraph">
        你可以把這件事想成：Claude Code 不是沒有能力直接動手，而是它先把方向盤交給你。你覺得安全，它才往前；你覺得這一步要小心，它就先停下來等你確認。
      </p>

      <h3 class="detail-subheading">給大多數人的起手式：先認識這三種就夠了</h3>
      <p class="detail-paragraph">
        Claude Code 官方支援的權限模式不只一種，包含 default、acceptEdits、plan、auto、dontAsk、bypassPermissions。但如果你是剛開始使用，其實先看懂最常碰到的三種就夠了：<strong>詢問權限</strong>、<strong>自動接受編輯</strong>、<strong>Plan Mode</strong>。在 VS Code 裡，你通常會看到它們以 Ask before edits、Edit automatically、Plan mode 這些名字出現。
      </p>

      <details class="collapsible">
        <summary class="collapsible-title">1. 詢問權限：最穩，也最適合剛開始</summary>
        <div class="collapsible-body">
          <p>這就是最保守、也最安心的用法。</p>
          <p>Claude Code 在真的要改檔或執行某些動作前，會先把差異或操作拿給你看，例如，改檔前先看「原始內容 vs 建議修改」，你可以接受、拒絕，或補一句話叫它換個方式改。</p>
          <p>這種模式的好處，不是比較慢，而是你比較容易建立手感。你會知道 Claude Code 平常怎麼思考、怎麼改、哪些地方容易改得太多。</p>
        </div>
      </details>

      <details class="collapsible">
        <summary class="collapsible-title">2. 自動接受編輯：省掉一堆小確認，但不是全自動放飛</summary>
        <div class="collapsible-body">
          <p>當你開始覺得每一刀都手動按允許有點打斷節奏，就可以考慮開自動接受編輯。</p>
          <p>這個模式的意思不是「它什麼都能做」，而是：工作資料夾裡的檔案建立、修改，以及一些常見的檔案系統指令，可以不用每次都跳提示。官方目前列出的自動核准範圍包含像 <code>mkdir</code>、<code>touch</code>、<code>rm</code>、<code>rmdir</code>、<code>mv</code>、<code>cp</code>、<code>sed</code> 這類常見檔案操作，但這個放行只限於你的工作資料夾或 additionalDirectories 之內；超出範圍、碰到受保護路徑，或是其他 Bash 指令，還是會再問你。</p>
          <p>它比較像是：把很常見、低摩擦的小動作先放快一點，但真正有副作用的事，還是留給你把關。如果你只是要讓 Claude Code 幫你補幾個檔案、改一些內容、整理目錄，這個模式會順很多；但如果你現在做的是重要專案、要大搬家、要大量刪除檔案，那還是先保守一點比較穩。</p>
        </div>
      </details>

      <details class="collapsible">
        <summary class="collapsible-title">3. Plan Mode：先研究、先列計畫，暫時不要動手</summary>
        <div class="collapsible-body">
          <p>如果你現在要做的是多檔案修改、流程重構、搬資料流，或你自己也還在想方向，那通常不要一開始就叫 Claude Code 直接改。這時候最好用的就是 Plan Mode。</p>
          <p>Plan Mode 的重點很簡單：先看、先想、先整理步驟，但先不要改。</p>
        </div>
      </details>

      <details class="collapsible">
        <summary class="collapsible-title">4. 另外幾種模式，先知道名字就好</summary>
        <div class="collapsible-body">
          <p>官方另外還有 auto、dontAsk、bypassPermissions 這幾種更進階的模式。</p>
          <p>其中 Auto mode 會用額外的安全檢查去幫忙判斷哪些動作能直接放行，但官方把它標成研究預覽功能，而且它不是所有方案、所有模型、所有環境都能用。</p>
          <p>Bypass permissions 則幾乎是直接略過提示，官方在 VS Code 文件裡明確提醒：這個模式只適合用在沒有網路的安全沙盒裡，不適合拿來當一般日常預設。至於 dontAsk，則比較像「沒先白名單就一律不做」的鎖很緊模式。</p>
          <p>所以對大多數人來說，真的不用急著把所有模式一次看完。先把<strong>詢問權限 → 自動接受編輯 → Plan Mode</strong> 這三個用順，你的體感就會差很多。</p>
        </div>
      </details>

      <!-- Section 3b: 防誤刪 -->
      <h2 class="detail-heading">防誤刪：先把邊界畫清楚，再讓 Claude Code 動手</h2>
      <p class="detail-paragraph">
        真正容易出事的，通常不是 Claude Code 完全亂改，而是「你給它的範圍太大、你自己的指令太模糊、或者你太早把自動化開得太大」。
      </p>
      <p class="detail-paragraph">
        所以防誤刪最重要的第一步，不是去背一堆設定，而是先把工作邊界畫清楚。Claude Code 預設只能寫入你啟動它的資料夾和其子資料夾；這等於是說，你從哪個資料夾開始工作，本身就在決定它能改到哪裡。它雖然能讀到工作目錄外的一些內容，但寫入邊界是被限制住的。
      </p>
      <p class="detail-paragraph">
        這件事帶來一個很實用的做法：第一次練手，不要一開始就把它開在又大又重要的資料夾裡。先用一個小專案、測試資料夾、或你今天真的只想改的那個子資料夾開始，會安全很多。因為你不是只在選資料夾而已，你其實是在替 Claude Code 畫出一條它能動到哪裡的邊界。
      </p>

      <details class="collapsible">
        <summary class="collapsible-title">不想讓它碰的東西，先明講比事後補救好</summary>
        <div class="collapsible-body">
          <p>有些檔案就算放在專案裡，也不代表你想讓 Claude Code 讀或改。像 <code>.env</code>、憑證、金鑰、某些 secrets 資料夾，最好一開始就先排除。官方設定文件明確建議，敏感檔案可以用 <code>permissions.deny</code> 先擋掉；而且規則判斷順序是 deny → ask → allow，也就是說，只要有 deny 規則先命中，它就會先被擋下來。</p>
          <p>你可以像這樣先放一個最基本的保護：</p>
          <pre class="code-block" style="padding-left: 2rem;"><code set:html={`{\n  "permissions": {\n    "deny": [\n      "Read(./.env)",\n      "Read(./.env.*)",\n      "Read(./secrets/**)",\n      "Read(./config/credentials.json)"\n    ]\n  }\n}`} /></pre>
          <p>這段的意思很簡單：不是等 Claude Code 已經讀到了，你才說「這個不要看」；而是先把不該碰的門鎖起來。</p>
        </div>
      </details>

      <details class="collapsible">
        <summary class="collapsible-title">要它刪、搬、改名時，範圍一定要講清楚</summary>
        <div class="collapsible-body">
          <p>很多「誤刪」不是因為模型突然失控，而是因為指令本身太籠統。</p>
          <p>像「幫我整理一下這個專案」這種話，對 Claude Code 來說就很大；如果你又剛好開著自動接受編輯，那它可能真的會開始搬檔案、刪掉它認為多餘的東西。比較穩的講法，會像這樣：</p>
          <div class="info-card" style="margin: var(--space-4) 0;">
            <p style="margin: 0; font-size: var(--text-sm); line-height: 1.8;">只整理 <code>src/components</code> 這個資料夾，不要碰 <code>.env</code>、部署設定和 <code>package-lock.json</code>。先列出你打算刪除、移動、重新命名的檔案，再等我確認後才動手。</p>
          </div>
          <p>這種說法的好處不是比較囉唆，而是把「可動範圍」和「不能碰的東西」都先講出來，Claude Code 比較不容易往外擴。</p>
        </div>
      </details>

      <details class="collapsible">
        <summary class="collapsible-title">大改動前，先用 Plan Mode 看一眼</summary>
        <div class="collapsible-body">
          <p>如果你準備做的是大規模整理，例如整批重命名、重構資料夾、搬移元件、刪除舊檔案，建議先切到 Plan Mode。因為 Plan Mode 本來就是設計來「先分析、先提方案、先讓你確認」，不是直接進入編修。你可以先看它列出的刪除清單、搬移步驟、相依影響，再決定要不要讓它繼續。</p>
        </div>
      </details>

      <details class="collapsible">
        <summary class="collapsible-title">改完後，立刻看差異，不要等到最後才一起看</summary>
        <div class="collapsible-body">
          <p>自動接受編輯的精神，不是叫你完全不看，而是把「每一步都要按允許」改成「做完一輪再一起看差異」。官方對 acceptEdits 的說明也很直接：這個模式適合在編輯器或 git diff 裡面事後檢查，而不是每一刀都在中途確認。</p>
        </div>
      </details>

      <!-- Section 4: 大功能先計畫 -->
      <h2 class="detail-heading">大功能先計畫，再動手</h2>
      <p class="detail-paragraph">
        當你要做的是新功能、多檔案修改、流程重構，或你自己也還不確定方向，不要一開始就叫 Claude Code 直接寫。
        在對話框右下角，把模式切到 <strong>Plan Mode</strong> 後，
        Claude Code 會先用唯讀方式研究專案、整理步驟，等你同意後才開始改。
      </p>
      <p class="detail-paragraph">
        VS Code 還會把這份計畫打開成一份 markdown 文件，你可以直接在上面留言、補條件、修方向，再讓它往下做。
      </p>

      <div class="scenario-grid">
        <div class="scenario-card scenario-card--good">
          <p class="scenario-tag scenario-tag--good">✓ 適合先計畫</p>
          <ul class="scenario-list">
            <li>改登入流程</li>
            <li>搬資料流</li>
            <li>重構舊模組</li>
            <li>牽一髮動全身的任務</li>
          </ul>
        </div>
        <div class="scenario-card scenario-card--skip">
          <p class="scenario-tag scenario-tag--skip">— 通常不用先計畫</p>
          <ul class="scenario-list">
            <li>改字、改文案</li>
            <li>補 log</li>
            <li>改一兩行小 bug</li>
            <li>單純的小修小補</li>
          </ul>
        </div>
      </div>

      <!-- Section 5: 倒帶 -->
      <h2 class="detail-heading">學會用「倒帶」救回來，不用怕 Claude 改壞</h2>
      <p class="detail-paragraph">
        跟 AI 一起寫程式時，最重要的不是一次就全對，而是出錯時你要知道怎麼安全退回去。
        在 VS Code 裡，Claude Code 會自動幫你記錄每一步編輯，所以當它改太多、方向跑掉，
        或你突然想換一種做法時，不需要慌張，直接用倒帶功能就好。
      </p>
      <p class="detail-paragraph">
        當你把滑鼠移到某一則對話訊息上，看到倒帶按鈕後，點下去會有三個選項：
      </p>

      <div class="rewind-list">
        <article class="rewind-card">
          <div class="rewind-head">
            <span class="rewind-num">1</span>
            <div>
              <h3 class="rewind-title">Fork conversation from here</h3>
              <p class="rewind-alias">保留現在成果，從這裡開平行宇宙繼續聊</p>
            </div>
          </div>
          <p class="rewind-desc">
            從這一則訊息開始，開出一條新的對話分支，但目前的程式碼修改全部保留。
            Claude Code 不會把檔案改回去，只是幫你從這個時間點另開一條新路線。
          </p>
          <p class="rewind-example">
            <strong>例子：</strong>Claude Code 已經幫你把登入頁改好了，但你突然想試試另一種表單驗證方式。
            這時可以選 Fork conversation from here，保留現在已改好的程式碼，再從這個點開始走另一條對話。
          </p>
        </article>

        <article class="rewind-card">
          <div class="rewind-head">
            <span class="rewind-num">2</span>
            <div>
              <h3 class="rewind-title">Rewind code to here</h3>
              <p class="rewind-alias">聊天不刪，程式碼退回去</p>
            </div>
          </div>
          <p class="rewind-desc">
            把檔案修改倒帶回這個時間點，但完整對話紀錄保留。聊天內容不會消失，
            你仍然看得到後面聊過什麼，只是程式碼會退回到你選的那一刻。
          </p>
          <p class="rewind-example">
            <strong>例子：</strong>你和 Claude Code 討論了很多排錯思路，後面它也改了好幾個檔案，
            但你測試後發現改壞了。這時可以選 Rewind code to here，把程式碼退回到比較穩定的版本，
            同時保留後面的討論紀錄方便繼續參考。
          </p>
        </article>

        <article class="rewind-card">
          <div class="rewind-head">
            <span class="rewind-num">3</span>
            <div>
              <h3 class="rewind-title">Fork conversation and rewind code</h3>
              <p class="rewind-alias">整段從這裡重開一條平行宇宙，程式碼也一起回到當時</p>
            </div>
          </div>
          <p class="rewind-desc">
            開一條新的對話分支，並且把程式碼也一起退回到這個時間點。
            它同時做了兩件事：不污染現在的對話主線，也把程式碼還原到你指定的狀態。
          </p>
          <p class="rewind-example">
            <strong>例子：</strong>Claude Code 連續做了好幾輪重構，但你覺得整個方向不對，
            不只程式碼想退回去，連後面的對話脈絡也不想沿用。這時就選 Fork conversation and rewind code，
            從那個較早的節點重新開始。
          </p>
        </article>
      </div>

      <!-- Section 6: 瘦身 -->
      <h2 class="detail-heading">對話太長時，記得幫 Claude Code「瘦身」</h2>
      <p class="detail-paragraph">
        Claude Code 不是無限記憶，它的 <em>context window</em> 裡裝的是對話歷史、讀過的檔案、
        命令輸出、CLAUDE.md、skills 等內容。聊久了、看過的檔案多了，整段對話就會變重。
      </p>
      <p class="detail-paragraph">
        VS Code 的對話框底部會顯示目前用了多少上下文，Claude Code 也會在接近上限時自動壓縮內容。
        你也可以手動輸入 <code>/compact</code>，順便告訴它這次壓縮要保留什麼。
        要是準備換到完全不同的新任務，最乾淨的做法通常不是硬接著聊，而是直接開一個新對話，
        或用 <code>/clear</code> 把這一串清掉。那些「每次都該遵守」的規則，
        不要反覆寫在聊天裡，放進 <strong>CLAUDE.md</strong> 會更穩。
      </p>

      <div class="example-card">
        <p class="example-title">範例</p>
        <ul class="example-list">
          <li><code>/compact 只保留這次 API 修改、測試指令和目前待修的 bug</code></li>
          <li><code>/clear</code></li>
          <li>請把「完成後先跑 typecheck」寫成我們專案的 CLAUDE.md 規則</li>
        </ul>
      </div>

      <!-- Section 7: 錯誤處理 -->
      <h2 class="detail-heading">遇到錯誤時，不要只說「幫我修」</h2>
      <p class="detail-paragraph">
        新手最常卡住的地方，不是 bug 太難，而是給 Claude Code 的資訊太少。
        比起只丟一句「這裡壞了」，更有效的做法是一起給它：
        <strong>錯誤訊息、怎麼重現、哪個檔案可能有關、修好後怎麼驗證</strong>。
      </p>
      <p class="detail-paragraph">
        官方也特別強調，Claude Code 在能夠自己驗證結果時表現會更好，
        所以測試、錯誤輸出、預期畫面、螢幕截圖，這些都很有幫助。
        在 VS Code 裡，你還可以用 <code>@terminal:name</code> 直接把終端機輸出交給 Claude Code 看，
        不用每次都自己複製貼上；UI 問題也很適合直接貼截圖或拖圖片進去。
      </p>

      <div class="example-card">
        <p class="example-title">提問範例</p>
        <ul class="example-list">
          <li>我執行 <code>npm test</code> 會報這個錯誤，請先找出根因，再修復，最後重新跑測試確認通過。</li>
          <li>請讀 <code>@terminal:npm test</code> 的輸出，告訴我第一個真正的錯誤是什麼。</li>
          <li>這是錯誤截圖，請判斷是哪個區塊壞掉，並告訴我你要改哪些檔案。</li>
          <li>users report that login fails after session timeout。請先寫一個會 fail 的 test 重現，再修正。</li>
        </ul>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .cc-basics {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  /* Header */
  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  /* Section headings */
  .detail-heading {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-heading);
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-subheading {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-heading);
    margin-top: var(--space-8);
    margin-bottom: var(--space-4);
  }

  /* Collapsible sections */
  .collapsible {
    margin-bottom: var(--space-3);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: border-color var(--transition);
  }

  .collapsible:hover {
    border-color: var(--color-accent);
  }

  .collapsible[open] {
    border-color: var(--color-accent);
  }

  .collapsible-title {
    padding: var(--space-4) var(--space-6);
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-heading);
    cursor: pointer;
    list-style: none;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-3);
    user-select: none;
  }

  .collapsible-title::-webkit-details-marker {
    display: none;
  }

  .collapsible-title::after {
    content: '';
    flex-shrink: 0;
    width: 0.5rem;
    height: 0.5rem;
    border-right: 2px solid var(--color-text-secondary);
    border-bottom: 2px solid var(--color-text-secondary);
    transform: rotate(-45deg);
    transition: transform 0.2s ease;
  }

  .collapsible[open] > .collapsible-title::after {
    transform: rotate(45deg);
  }

  .collapsible-body {
    padding: 0 var(--space-6) var(--space-6);
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.9;
    border-top: 1px dashed var(--color-border);
    padding-top: var(--space-4);
  }

  .collapsible-body p {
    margin-bottom: var(--space-3);
  }

  .collapsible-body p:last-child {
    margin-bottom: 0;
  }

  .collapsible-body strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .collapsible-body code {
    padding: 0.1em 0.4em;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 0.9em;
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
    border-radius: var(--border-radius);
  }

  /* Code block */
  .code-block {
    margin: var(--space-4) 0;
    padding: var(--space-5) var(--space-6) var(--space-5) var(--space-12);
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: var(--text-xs);
    line-height: 1.7;
    background-color: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    overflow-x: auto;
    white-space: pre;
  }

  .code-block code {
    padding: 0;
    background: none;
    color: var(--color-text);
    font-size: inherit;
  }

  .detail-paragraph {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    margin-bottom: var(--space-4);
  }

  .detail-paragraph strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Step list */
  .step-list {
    list-style: none;
    padding: 0;
    margin: var(--space-6) 0 var(--space-8);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .step-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-6);
    padding: var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: all var(--transition);
  }

  .step-item:hover {
    border-color: var(--color-accent);
  }

  .step-num {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-base);
    font-weight: 700;
  }

  .step-body {
    flex: 1;
    min-width: 0;
  }

  .step-title {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-heading);
    margin-bottom: var(--space-2);
  }

  .step-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  .step-desc strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Method cards (@ / + / 反白) */
  .method-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-4);
    margin: var(--space-6) 0 var(--space-6);
  }

  .method-card {
    padding: var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: all var(--transition);
    display: flex;
    flex-direction: column;
  }

  .method-card:hover {
    border-color: var(--color-accent);
    transform: translateY(-2px);
  }

  .method-symbol {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: var(--border-radius);
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-lg);
    font-weight: 700;
    margin-bottom: var(--space-3);
  }

  .method-title {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.4;
  }

  .method-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    margin-bottom: var(--space-3);
  }

  .method-examples {
    list-style: none;
    padding: var(--space-3) 0 0;
    margin: 0;
    border-top: 1px dashed var(--color-border);
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    line-height: 1.7;
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .method-examples li {
    padding-left: var(--space-3);
    position: relative;
  }

  .method-examples li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Info card (給新手的簡單理解) */
  .info-card {
    margin: var(--space-4) 0 var(--space-6);
    padding: var(--space-6);
    border: 1px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-tag-bg);
  }

  .info-title {
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-accent);
    letter-spacing: 0.05em;
    margin-bottom: var(--space-3);
    text-transform: uppercase;
  }

  .info-list {
    list-style: none;
    padding: 0;
    margin: 0;
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.9;
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .info-list strong {
    display: inline-block;
    min-width: 3rem;
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Scenario cards (適合 / 不適合) */
  .scenario-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
    margin: var(--space-6) 0 var(--space-8);
  }

  .scenario-card {
    padding: var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .scenario-tag {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    margin-bottom: var(--space-3);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.05em;
    border-radius: var(--border-radius);
  }

  .scenario-tag--good {
    background-color: var(--color-accent);
    color: #fff;
  }

  .scenario-tag--skip {
    background-color: var(--color-bg);
    color: var(--color-text-secondary);
    border: 1px solid var(--color-border);
  }

  .scenario-list {
    list-style: none;
    padding: 0;
    margin: 0;
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .scenario-list li {
    padding-left: var(--space-4);
    position: relative;
  }

  .scenario-list li::before {
    content: '·';
    position: absolute;
    left: var(--space-2);
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Rewind cards */
  .rewind-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    margin: var(--space-6) 0 var(--space-8);
  }

  .rewind-card {
    padding: var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: all var(--transition);
  }

  .rewind-card:hover {
    border-color: var(--color-accent);
  }

  .rewind-head {
    display: flex;
    align-items: flex-start;
    gap: var(--space-4);
    margin-bottom: var(--space-3);
    padding-bottom: var(--space-3);
    border-bottom: 1px dashed var(--color-border);
  }

  .rewind-num {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: 50%;
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-sm);
    font-weight: 700;
  }

  .rewind-title {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-1);
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  }

  .rewind-alias {
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    line-height: 1.6;
  }

  .rewind-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    margin-bottom: var(--space-3);
  }

  .rewind-example {
    margin: 0;
    padding: var(--space-3) var(--space-4);
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    line-height: 1.8;
    background-color: var(--color-bg);
    border-left: 3px solid var(--color-accent);
    border-radius: var(--border-radius);
  }

  .rewind-example strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Example card (compact / errors) */
  .example-card {
    margin: var(--space-4) 0 var(--space-8);
    padding: var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .example-title {
    font-size: var(--text-xs);
    font-weight: 700;
    color: var(--color-accent);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    margin-bottom: var(--space-3);
  }

  .example-list {
    list-style: none;
    padding: 0;
    margin: 0;
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .example-list li {
    padding-left: var(--space-4);
    position: relative;
  }

  .example-list li::before {
    content: '›';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Inline code */
  :global(.detail-paragraph) code,
  :global(.step-desc) code,
  :global(.method-desc) code,
  :global(.method-examples) code,
  :global(.example-list) code,
  :global(.rewind-desc) code {
    padding: 0.1em 0.4em;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 0.9em;
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
    border-radius: var(--border-radius);
  }

  /* Keyboard key */
  kbd {
    display: inline-block;
    padding: 0 var(--space-2);
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: var(--text-xs);
    color: var(--color-text);
    background-color: var(--color-bg);
    border: 1px solid var(--color-border);
    border-bottom-width: 2px;
    border-radius: var(--border-radius);
    vertical-align: middle;
  }

  /* Mobile */
  @media (max-width: 720px) {
    .method-grid {
      grid-template-columns: 1fr;
    }

    .scenario-grid {
      grid-template-columns: 1fr;
    }

    .step-item {
      gap: var(--space-4);
      padding: var(--space-6);
    }

    .step-num {
      width: 2rem;
      height: 2rem;
      font-size: var(--text-sm);
    }

    .rewind-card {
      padding: var(--space-5);
    }
  }
</style>
