---
import BaseLayout from '../../../../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="先把 Claude Code 裝起來，再接上 VS Code 開始用"
  description="Claude Code 在 Mac 與 Windows 上的安裝、啟動與登入流程，以及新手常見的除錯解法。"
  lang="zh"
>
  <section class="section">
    <div class="container cc-install">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">先把 Claude Code 裝起來，再接上 VS Code 開始用</h1>
        <p class="detail-subtitle">
          從課前準備、Mac／Windows 安裝、帳號登入到常見問題排解，
          帶你一步一步把 Claude Code 跑起來。
        </p>
      </header>

      <!-- Overview Diagram -->
      <h2 class="detail-heading">快速流程總覽</h2>
      <p class="detail-paragraph">
        下圖整理了整個安裝過程的五個主要階段，從環境準備到專案啟動，讓你先對流程有個整體的印象。
      </p>
      <figure class="overview-figure">
        <img
          src="/learning/claude-code/install-zh.png"
          alt="Claude Code 快速安裝與配置流程示意圖"
          loading="lazy"
        />
      </figure>

      <!-- Section: Prerequisites -->
      <h2 class="detail-heading">課前準備：開始安裝前的兩件事</h2>
      <p class="detail-paragraph">
        在動手安裝 Claude Code 之前，請先確認以下兩件事情都準備好了：
      </p>

      <div class="prep-grid">
        <div class="prep-card">
          <p class="prep-index">01</p>
          <h3 class="prep-title">一個專案資料夾</h3>
          <p class="prep-desc">
            在電腦裡新增一個空的資料夾，這會是你未來讓 AI 幫你寫程式、管理檔案的工作區。
            建議放在容易找到的位置，例如桌面或文件夾。
          </p>
        </div>
        <div class="prep-card">
          <p class="prep-index">02</p>
          <h3 class="prep-title">一個付費帳號</h3>
          <p class="prep-desc">
            目前 Claude Code 需要搭配
            <strong>Claude Pro</strong>（每月 20 美金）或更進階的
            <strong>Max</strong> 方案帳號才能使用。請先到 Claude 官網申請並完成付款。
          </p>
        </div>
      </div>

      <!-- Section: Mac Install -->
      <h2 class="detail-heading">Mac 電腦安裝教學</h2>
      <p class="detail-paragraph">
        <span class="platform-badge platform-badge--mac">macOS</span>
        Mac 系統的安裝非常簡單，只需要叫出電腦內建的終端機即可完成。
      </p>

      <ol class="step-list">
        <li class="step-item">
          <span class="step-num">1</span>
          <div class="step-body">
            <h3 class="step-title">打開「終端機」（Terminal）</h3>
            <p class="step-desc">
              按下鍵盤上的 <kbd>⌘</kbd> + <kbd>空白鍵</kbd> 叫出 Spotlight 搜尋框，
              輸入「Terminal」或「終端機」後按下 Enter。
              你會看到一個黑底白字（或白底黑字）的視窗。
            </p>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">2</span>
          <div class="step-body">
            <h3 class="step-title">貼上安裝指令</h3>
            <p class="step-desc">
              複製下方的指令，貼到剛剛打開的終端機視窗中：
            </p>
            <pre class="code-block"><code>curl -fsSL https://claude.ai/install.sh | bash</code></pre>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">3</span>
          <div class="step-body">
            <h3 class="step-title">按下 Enter 鍵</h3>
            <p class="step-desc">
              電腦會自動幫你把 Claude Code 安裝好，請稍微等待它跑完。
              過程中如果出現任何提示，依照螢幕指示操作即可。
            </p>
          </div>
        </li>
      </ol>

      <!-- Section: Windows Install -->
      <h2 class="detail-heading">Windows 電腦安裝教學</h2>
      <p class="detail-paragraph">
        <span class="platform-badge platform-badge--win">Windows</span>
        Windows 的安裝會多一個小步驟，請務必依照順序來做。
      </p>

      <ol class="step-list">
        <li class="step-item">
          <span class="step-num">1</span>
          <div class="step-body">
            <h3 class="step-title">先安裝「Git」軟體（非常重要！）</h3>
            <p class="step-desc">
              Claude Code 在 Windows 上規定必須要有 Git 這個工具才能運作。
              請到 Google 搜尋「<strong>Git for Windows</strong>」下載安裝檔，
              安裝過程一直按「下一步（Next）」到底即可。
            </p>
            <p class="step-note">
              小提醒：安裝過程中如果看到「<strong>Add to PATH</strong>」的選項，請記得勾選。
              安裝完畢後，請先關閉所有視窗。
            </p>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">2</span>
          <div class="step-body">
            <h3 class="step-title">打開「PowerShell」</h3>
            <p class="step-desc">
              點擊螢幕左下角的 Windows 開始按鈕，輸入「PowerShell」來搜尋並打開它。
              這是一個藍底白字或黑底白字的指令視窗。
            </p>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">3</span>
          <div class="step-body">
            <h3 class="step-title">貼上安裝指令</h3>
            <p class="step-desc">
              複製下方指令，貼到 PowerShell 視窗中，並按下 Enter 鍵：
            </p>
            <pre class="code-block"><code>curl -fsSL https://claude.ai/install.sh | bash</code></pre>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">4</span>
          <div class="step-body">
            <h3 class="step-title">關閉並「重新打開」PowerShell</h3>
            <p class="step-desc">
              安裝跑完後，請一定要先關掉 PowerShell 視窗，再重新打開一個新的 PowerShell 視窗。
              這個動作是為了讓電腦記住剛剛安裝的新工具。
            </p>
          </div>
        </li>
      </ol>

      <!-- Section: Launch & Login -->
      <h2 class="detail-heading">啟動與登入</h2>
      <p class="detail-paragraph">
        完成上面的安裝步驟後，就可以正式啟動 Claude Code 了。
      </p>

      <ol class="step-list">
        <li class="step-item">
          <span class="step-num">1</span>
          <div class="step-body">
            <h3 class="step-title">呼叫 AI 助理</h3>
            <p class="step-desc">
              在終端機（或 PowerShell）中輸入以下指令並按下 Enter：
            </p>
            <pre class="code-block"><code>claude</code></pre>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">2</span>
          <div class="step-body">
            <h3 class="step-title">網頁授權登入</h3>
            <p class="step-desc">
              第一次啟動時，電腦會自動彈出一個瀏覽器網頁。
              請確認上面顯示的是你的 Claude 帳號，然後點擊「<strong>Approve（授權允許）</strong>」按鈕。
            </p>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">3</span>
          <div class="step-body">
            <h3 class="step-title">完成！</h3>
            <p class="step-desc">
              關閉網頁回到指令視窗，你會看到 Claude Code 已經成功啟動並向你打招呼。
              系統可能會詢問你要不要信任這個資料夾，選擇同意即可開始對話。
            </p>
          </div>
        </li>
      </ol>

      <!-- Section: Troubleshooting -->
      <h2 class="detail-heading">新手除錯急救站</h2>
      <p class="detail-paragraph">
        安裝過程偶爾會遇到小狀況，特別是 Windows 使用者。這裡整理了最常見的問題與解法：
      </p>

      <div class="faq-card">
        <div class="faq-tag">Windows 常見陷阱</div>
        <h3 class="faq-question">
          輸入 <code>claude</code> 後，出現紅字說「無法辨識 'claude' 詞彙是否為 Cmdlet…」
        </h3>
        <p class="faq-desc">
          這代表你的電腦「找不到」剛剛安裝的檔案。請依照以下步驟自救：
        </p>
        <ol class="faq-steps">
          <li>
            <strong>先關掉 PowerShell 重開一次。</strong>
            最常見的原因是剛安裝完忘記重開 PowerShell。先試試這一步。
          </li>
          <li>
            <strong>如果重開還是不行，請手動加入環境變數。</strong>
            在 PowerShell 中輸入下列指令並按下 Enter：
            <pre class="code-block code-block--small"><code>$env:PATH += ";$env:USERPROFILE\.local\bin"</code></pre>
            接著「<strong>再一次</strong>」關掉 PowerShell 並重新打開，
            輸入 <code>claude</code>，通常這樣就能完美解決了！
          </li>
        </ol>
      </div>

      <!-- Section: Optional VS Code GUI -->
      <h2 class="detail-heading">
        <span class="optional-badge">可選</span>
        為 Claude Code 裝上好用的圖形介面
      </h2>
      <p class="detail-paragraph">
        完成前面的終端機安裝步驟後，你的電腦裡其實已經有了 Claude Code 這個「聰明的大腦」。
        接下來的步驟會教你安裝一款名為 <strong>Visual Studio Code</strong>（簡稱 VS Code）的免費軟體，
        幫這個大腦裝上一個漂亮好操作的「控制面板」。
        有了它，你就不需要一直看著黑底白字的終端機畫面，
        而是可以用滑鼠點擊按鈕來同意任務，並且清楚對比 AI 幫你改了哪些檔案。
      </p>

      <ol class="step-list">
        <li class="step-item">
          <span class="step-num">1</span>
          <div class="step-body">
            <h3 class="step-title">下載並安裝 VS Code</h3>
            <p class="step-desc">打造你的專屬數位工作桌。</p>
            <ol class="substep-list">
              <li>
                請打開瀏覽器，搜尋「<strong>VS Code</strong>」（或 Visual Studio Code）。
              </li>
              <li>
                進入官方網站後，網站會自動偵測你是 Mac 還是 Windows 電腦，
                請點擊「<strong>Download</strong>（下載）」按鈕。
              </li>
              <li>
                下載完成後，點擊安裝檔。安裝過程非常簡單，就像安裝一般軟體一樣，
                一路點擊「下一步（Next）」，直到安裝完成並打開軟體。
              </li>
            </ol>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">2</span>
          <div class="step-body">
            <h3 class="step-title">準備一個專屬的「工作房間」（資料夾）</h3>
            <p class="step-desc">AI 助理工作時需要一個明確的範圍，也就是資料夾。</p>
            <ol class="substep-list">
              <li>
                請先在你的電腦桌面，點擊滑鼠右鍵「<strong>建立一個新的空資料夾</strong>」，
                這會是你未來的專案資料夾。
              </li>
              <li>打開剛安裝好的 VS Code 軟體。</li>
              <li>
                把剛剛建立的空資料夾，直接用滑鼠拖曳進 VS Code 的畫面中，
                或者從左上角選單選擇「<strong>打開資料夾（Open Folder）</strong>」將它開啟。
              </li>
            </ol>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">3</span>
          <div class="step-body">
            <h3 class="step-title">在 VS Code 裡安裝 Claude Code 擴充套件</h3>
            <p class="step-desc">現在我們要幫 VS Code 裝上 Claude 的專屬面板。</p>
            <ol class="substep-list">
              <li>
                在 VS Code 畫面的最左側，有一排直立的圖示，請找到並點擊一個長得像
                「<strong>四個小方塊</strong>」的圖示（這叫做延伸模組頁面）。
              </li>
              <li>
                點開後，上方會有一個搜尋框。請在搜尋框中輸入關鍵字
                「<strong>Claude Code</strong>」。
              </li>
              <li>
                找到 <strong>Anthropic</strong> 官方推出的擴充功能後，
                點擊旁邊的「<strong>Install</strong>（安裝）」按鈕。
              </li>
            </ol>
          </div>
        </li>
        <li class="step-item">
          <span class="step-num">4</span>
          <div class="step-body">
            <h3 class="step-title">啟動助理並開始使用</h3>
            <p class="step-desc">
              安裝完成後，你的 VS Code 左側邊欄會多出一個 Claude 的專屬圖示。
            </p>
            <ol class="substep-list">
              <li>
                點擊那個 Claude 圖示，就能打開專屬的圖形化對話面板。
                你也可以按下快捷鍵 <kbd>Cmd</kbd>+<kbd>Shift</kbd>+<kbd>P</kbd>（Mac）
                或 <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>P</kbd>（Windows），
                輸入「Claude Code」來打開它。
              </li>
              <li>
                因為你是第一次在這個介面使用，系統會引導你進行網頁授權。
                只要跟著畫面指示，在彈出的瀏覽器網頁中點擊
                「<strong>Approve</strong>（授權允許）」，就能完成綁定。
              </li>
              <li>
                授權成功後回到 VS Code，你就會看到熟悉的對話框。
                這時候，你就可以開始用白話文跟 AI 許願，請它幫你寫程式囉！
              </li>
            </ol>
          </div>
        </li>
      </ol>
    </div>
  </section>
</BaseLayout>

<style>
  .cc-install {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  /* Header */
  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  /* Section headings */
  .detail-heading {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-heading);
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-paragraph {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    margin-bottom: var(--space-4);
  }

  .detail-paragraph strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Overview figure */
  .overview-figure {
    margin: var(--space-6) 0 var(--space-8);
    padding: var(--space-4);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .overview-figure img {
    display: block;
    width: 100%;
    height: auto;
    border-radius: var(--border-radius);
  }

  /* Prep cards */
  .prep-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
    margin: var(--space-6) 0 var(--space-8);
  }

  .prep-card {
    padding: var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: all var(--transition);
  }

  .prep-card:hover {
    border-color: var(--color-accent);
    transform: translateY(-2px);
  }

  .prep-index {
    font-size: var(--text-xs);
    font-weight: 700;
    color: var(--color-accent);
    letter-spacing: 0.08em;
    margin-bottom: var(--space-2);
  }

  .prep-title {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
  }

  .prep-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  .prep-desc strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Platform badge */
  .platform-badge {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    margin-right: var(--space-2);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.05em;
    border-radius: var(--border-radius);
  }

  .platform-badge--mac {
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
  }

  .platform-badge--win {
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
  }

  /* Step list (numbered) */
  .step-list {
    list-style: none;
    padding: 0;
    margin: var(--space-6) 0 var(--space-8);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .step-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-6);
    padding: var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: all var(--transition);
  }

  .step-item:hover {
    border-color: var(--color-accent);
  }

  .step-num {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-base);
    font-weight: 700;
  }

  .step-body {
    flex: 1;
    min-width: 0;
  }

  .step-title {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-heading);
    margin-bottom: var(--space-2);
  }

  .step-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    margin-bottom: var(--space-3);
  }

  .step-desc:last-child {
    margin-bottom: 0;
  }

  .step-desc strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .step-note {
    margin-top: var(--space-3);
    padding: var(--space-3) var(--space-4);
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    line-height: 1.8;
    background-color: var(--color-bg);
    border-left: 3px solid var(--color-accent);
    border-radius: var(--border-radius);
  }

  .step-note strong {
    color: var(--color-heading);
  }

  /* Code block */
  .code-block {
    display: block;
    margin: var(--space-3) 0 0;
    padding: var(--space-4);
    background-color: #0f172a;
    color: #e2e8f0;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: var(--text-xs);
    line-height: 1.7;
    border-radius: var(--border-radius);
    overflow-x: auto;
    white-space: pre;
  }

  .code-block code {
    font-family: inherit;
    color: inherit;
    background: none;
    padding: 0;
  }

  .code-block--small {
    font-size: var(--text-xs);
  }

  /* Keyboard key */
  kbd {
    display: inline-block;
    padding: 0 var(--space-2);
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: var(--text-xs);
    color: var(--color-text);
    background-color: var(--color-bg);
    border: 1px solid var(--color-border);
    border-bottom-width: 2px;
    border-radius: var(--border-radius);
    vertical-align: middle;
  }

  /* Inline code */
  :global(.faq-card) code,
  :global(.step-desc) code,
  :global(.faq-question) code {
    padding: 0.1em 0.4em;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 0.9em;
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
    border-radius: var(--border-radius);
  }

  /* FAQ / troubleshooting card */
  .faq-card {
    margin: var(--space-6) 0 var(--space-4);
    padding: var(--space-8);
    border: 1px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-tag-bg);
  }

  .faq-tag {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    margin-bottom: var(--space-3);
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 0.05em;
    border-radius: var(--border-radius);
  }

  .faq-question {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.6;
  }

  .faq-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    margin-bottom: var(--space-4);
  }

  .faq-steps {
    padding-left: var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  .faq-steps strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Optional badge in heading */
  .optional-badge {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    margin-right: var(--space-2);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.05em;
    background-color: var(--color-accent);
    color: #fff;
    border-radius: var(--border-radius);
    vertical-align: middle;
  }

  /* Sub-steps (a, b, c) inside a step */
  .substep-list {
    list-style: lower-alpha;
    margin: var(--space-3) 0 0;
    padding-left: var(--space-6);
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  .substep-list li {
    margin-bottom: var(--space-2);
    padding-left: var(--space-1);
  }

  .substep-list li:last-child {
    margin-bottom: 0;
  }

  .substep-list strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Mobile */
  @media (max-width: 720px) {
    .prep-grid {
      grid-template-columns: 1fr;
    }

    .step-item {
      gap: var(--space-4);
      padding: var(--space-6);
    }

    .step-num {
      width: 2rem;
      height: 2rem;
      font-size: var(--text-sm);
    }

    .faq-card {
      padding: var(--space-6);
    }
  }
</style>
