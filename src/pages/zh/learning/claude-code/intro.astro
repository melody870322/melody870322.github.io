---
import BaseLayout from '../../../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Claude Code 介紹"
  description="認識 Claude Code —— 一位有手有腳的貼身 AI 助理，以及它背後的運作原理與進階功能。"
  lang="zh"
>
  <section class="section">
    <div class="container cc-intro">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">Claude Code 介紹</h1>
        <p class="detail-subtitle">
          一位有手有腳的貼身 AI 助理，讓人即使不懂程式，也能用自然語言打造出想要的工具。
        </p>
      </header>

      <!-- Section 1: What is Claude Code -->
      <h2 class="detail-heading">什麼是 Claude Code？</h2>
      <p class="detail-paragraph">
        我們熟悉的 AI（例如早期的 ChatGPT 或 Gemini）就像是一座「沒有手腳的圖書館」：
        你問它問題，它給你答案，但你必須自己把答案複製貼上、自己去執行。
        而 Claude Code 則像是一位「有手有腳的貼身 AI 助理」，具備了
        <strong>代理能力（Agentic）</strong>，這意味著它不只能跟你聊天，還能自主採取行動。
        它可以直接讀取你電腦裡的資料夾、修改檔案、執行指令，甚至是打開瀏覽器幫你測試畫面。
      </p>

      <div class="compare-grid">
        <div class="compare-card compare-card--muted">
          <p class="compare-label">傳統 AI 聊天工具</p>
          <p class="compare-title">沒有手腳的圖書館</p>
          <p class="compare-desc">
            只能回答問題，你得自己把答案複製貼上、自己動手執行。
          </p>
        </div>
        <div class="compare-card compare-card--accent">
          <p class="compare-label">Claude Code</p>
          <p class="compare-title">有手有腳的貼身助理</p>
          <p class="compare-desc">
            能讀檔、改檔、執行指令、測試結果，從頭到尾替你完成一件事。
          </p>
        </div>
      </div>

      <p class="detail-paragraph">
        最棒的是，即使你完全不懂程式碼也沒關係。透過 Claude Code，一般人只要用平常講話的方式
        （自然語言）下達指令（Prompt），AI 就會自己規劃並做出你想要的網頁或小工具，
        這種全新的開發方式被稱為
        <span class="tech-tag inline-tag">Vibe Coding</span>。
      </p>

      <!-- Section 2: Agentic Loop -->
      <h2 class="detail-heading">Claude Code 的核心運作原理</h2>
      <p class="detail-paragraph">
        Claude Code 之所以這麼聰明，是因為它有兩個核心元素：<strong>大腦（AI 模型）</strong>和
        <strong>手腳（工具）</strong>。當你交給它一個任務時，它會不斷重複一個被稱為
        「代理循環（Agentic Loop）」的三步驟過程：
      </p>

      <ol class="loop-list">
        <li class="loop-step">
          <span class="loop-num">1</span>
          <div class="loop-body">
            <h3 class="loop-title">收集資訊 <span class="loop-en">Gather Context</span></h3>
            <p class="loop-desc">
              當你給出任務（例如：「幫我修好這個錯誤」），Claude Code 會自動去找相關的檔案來讀，
              或是上網搜尋資料，先搞懂目前的狀況。
            </p>
          </div>
        </li>
        <li class="loop-step">
          <span class="loop-num">2</span>
          <div class="loop-body">
            <h3 class="loop-title">採取行動 <span class="loop-en">Take Action</span></h3>
            <p class="loop-desc">
              弄懂狀況後，它會自動擬定計畫，接著動手修改檔案內容、建立新文件，或是執行系統指令。
            </p>
          </div>
        </li>
        <li class="loop-step">
          <span class="loop-num">3</span>
          <div class="loop-body">
            <h3 class="loop-title">驗證結果 <span class="loop-en">Verify Results</span></h3>
            <p class="loop-desc">
              這是它最厲害的地方。修改完後，它會自己跑測試，確認這個方法到底有沒有用。
              如果發現還是會報錯，它會讀取錯誤訊息，重新回到第一步自我修正，直到把問題解決為止。
            </p>
          </div>
        </li>
      </ol>

      <!-- Section 3: Four Power Features -->
      <h2 class="detail-heading">讓 Claude Code 變得更強大的 4 個秘密武器</h2>
      <p class="detail-paragraph">
        為了讓這位助理更懂你、工作更有效率，Claude Code 還有幾個非常實用的進階功能，
        可以把它們想像成職場上的各種工具：
      </p>

      <div class="feature-grid">
        <article class="feature-card">
          <p class="feature-index">01</p>
          <h3 class="feature-title">CLAUDE.md</h3>
          <p class="feature-alias">專屬員工手冊</p>
          <p class="feature-desc">
            這是一個放在專案裡的備忘錄檔案。你可以把公司的設計規範、個人偏好、
            或是工作流程寫在裡面。Claude Code 每次上工前都會先讀過這份手冊，
            這樣它就不會忘記你的規定，也不用你每次都重新交代。
          </p>
        </article>

        <article class="feature-card">
          <p class="feature-index">02</p>
          <h3 class="feature-title">MCP</h3>
          <p class="feature-alias">外掛連接線</p>
          <p class="feature-desc">
            全名是「模型上下文協定（Model Context Protocol）」，
            可以把它想像成一條 USB 傳輸線。只要裝上對應的 MCP，
            Claude Code 就能連上外部服務，例如讀取 Gmail 信件、查看 Google 日曆行程，
            或是把資料寫進 Notion 筆記裡，幫你自動整理每日早報。
          </p>
        </article>

        <article class="feature-card">
          <p class="feature-index">03</p>
          <h3 class="feature-title">Skills</h3>
          <p class="feature-alias">專業技能包</p>
          <p class="feature-desc">
            當「員工手冊」寫了太多東西時，AI 會因為資訊量太大而分心。
            我們可以把一些「偶爾才會用到」的標準作業流程（例如：如何製作 PPT 簡報、如何畫圖）
            打包成「技能（Skill）」。AI 只有在碰到相關任務時才會把這個技能包打開來用，
            這樣能讓大腦保持清醒、提升效率。
          </p>
        </article>

        <article class="feature-card">
          <p class="feature-index">04</p>
          <h3 class="feature-title">Subagents</h3>
          <p class="feature-alias">AI 小幫手</p>
          <p class="feature-desc">
            如果你交代了一個很龐大、需要找很多資料的任務，Claude Code 可以直接「分身」出幾個
            專屬小幫手。小幫手們會各自去查資料、做苦工，最後再把重點回報給 Claude 主大腦，
            幫助你省下大量的等待時間。
          </p>
        </article>
      </div>

      <!-- Section 4: Which entry point? -->
      <h2 class="detail-heading">我該選 Desktop、VS Code 還是 CLI？</h2>
      <p class="detail-paragraph">
        很多人第一次接觸 Claude Code，最容易卡住的不是「會不會用」，而是「我到底該從哪裡開始」。
        其實這三種入口沒有誰比較高級，比較像是三種不同的工作方式：
        Desktop 比較像直接打開就能用的桌面工具、VS Code 比較像把 Claude 帶進你原本的編輯器裡、
        CLI 則比較像直接在終端機裡跟它一起工作。
      </p>

      <div class="entry-grid">
        <!-- Desktop -->
        <article class="entry-card">
          <div class="entry-header">
            <span class="entry-index">1</span>
            <h3 class="entry-title">Desktop</h3>
            <p class="entry-tagline">最像「直接打開就能開始」</p>
          </div>
          <p class="entry-desc">
            你可以把 Desktop 想成最不需要暖身的入口。下載、安裝、登入之後，就能直接在桌面版開始使用 Claude Code。
            對第一次接觸的人來說，這條路最大的好處不是功能比較多，而是<strong>心理壓力比較小</strong>：
            你不用先理解終端機，也不用先把工作方式搬進 IDE，而是先把 Claude 打開、先讓它幫你做一點事，就已經很夠了。
          </p>
          <p class="entry-fit-label">適合情況</p>
          <ul class="entry-fit-list">
            <li>你剛開始接觸 Claude Code，想先用最直覺的方式上手。</li>
            <li>你不想一開始就碰終端機。</li>
            <li>你想先用小任務試手感，例如讀資料夾、解釋檔案、整理內容。</li>
            <li>你需要的是「先開始用」，不是「先理解整個開發環境」。</li>
          </ul>
          <p class="entry-desc">
            Desktop 的感覺比較像你先把一位 AI 助理請進來，讓它坐到你旁邊，先陪你看、陪你做，再慢慢熟悉工作方式。
            它不是最偏工程感的入口，但通常是最容易跨出去的第一步。
          </p>
          <div class="entry-transition">
            <p class="entry-transition__label">什麼時候你可能會想從 Desktop 走到下一步？</p>
            <p class="entry-transition__body">
              當你開始需要一邊看檔案樹、一邊改程式、一邊檢查差異，或你本來就長時間待在 VS Code 裡，
              那時候改用 VS Code 整合介面，通常會更順。這不是因為 Desktop 不夠用，
              而是你的工作方式開始變得更像「在專案裡持續合作」。
            </p>
          </div>
        </article>

        <!-- VS Code -->
        <article class="entry-card">
          <div class="entry-header">
            <span class="entry-index">2</span>
            <h3 class="entry-title">VS Code</h3>
            <p class="entry-tagline">最像「把 Claude 直接接進你的工作桌」</p>
          </div>
          <p class="entry-desc">
            如果 Desktop 比較像把 Claude 請進來，那 VS Code 比較像是把 Claude 直接裝進你平常工作的地方。
            你本來就已經在 VS Code 裡看檔案、編輯內容、切換資料夾，
            那把 Claude Code 放進同一個畫面，整個流程通常會更自然。
          </p>
          <p class="entry-fit-label">適合情況</p>
          <ul class="entry-fit-list">
            <li>你平常就會用 VS Code。</li>
            <li>你想一邊看原始檔，一邊和 Claude 對話。</li>
            <li>你很在意修改前後的差異能不能清楚看到。</li>
            <li>你會經常指定檔案、資料夾、某段程式碼讓 Claude 處理。</li>
          </ul>
          <p class="entry-desc">
            VS Code 的好處不是「看起來比較專業」，而是<strong>切換成本很低</strong>。
            因為你的檔案、編輯器、對話面板都在同一個地方，Claude 幫你改東西時，
            你比較容易理解它改了哪裡，也比較容易即時補一句：「這段不要這樣改，換另一種寫法。」
          </p>
          <div class="entry-transition">
            <p class="entry-transition__label">哪種人會很喜歡 VS Code？</p>
            <p class="entry-transition__body">
              如果你不是完全排斥技術工具，但也不想一開始就用純終端機，那 VS Code 常常是很剛好的中間點。
              它保留了圖形介面的直覺，又讓你直接站在專案裡工作。
              對很多要長期維護網站、程式碼或檔案的人來說，這會是最穩定的主力入口。
            </p>
          </div>
        </article>

        <!-- CLI -->
        <article class="entry-card">
          <div class="entry-header">
            <span class="entry-index">3</span>
            <h3 class="entry-title">CLI</h3>
            <p class="entry-tagline">最像「直接進到 Claude Code 的原生工作區」</p>
          </div>
          <p class="entry-desc">
            CLI 就是終端機版本。對有些人來說，看到終端機會緊張；
            但對另外一些人來說，這反而是最直接、最乾淨、最不繞路的方式。
          </p>
          <p class="entry-fit-label">適合情況</p>
          <ul class="entry-fit-list">
            <li>你本來就不排斥終端機。</li>
            <li>你喜歡直接下指令、直接執行、直接看輸出。</li>
            <li>你會需要比較偏 shell 的工作流。</li>
            <li>你之後想做更進階、更工程向的使用方式。</li>
          </ul>
          <p class="entry-desc">
            CLI 的強項通常不是「比較炫」，而是<strong>直接</strong>。
            你不用先打開另一個大介面，也不用先切到編輯器的某個側欄，
            就能立刻進入 Claude Code 的互動流程。對已經熟悉 terminal 的人來說，
            這種直接感很舒服。Claude Code 能讀取程式碼庫、編輯檔案、執行命令、
            與開發工具整合的 agentic coding tool，而終端機正是這種工作方式很自然的載體。
          </p>
          <div class="entry-transition">
            <p class="entry-transition__label">什麼時候不要一開始就選 CLI？</p>
            <p class="entry-transition__body">
              如果你現在連終端機是什麼都還很陌生，或你真正想做的是先理解 Claude Code 怎麼跟你合作，
              那通常沒有必要硬從 CLI 開始。不是因為 CLI 不好，而是因為它會讓你同時面對兩件新事：
              一個是 Claude Code 本身；另一個是終端機工作方式。對很多人來說，這會讓起步變得比較重。
            </p>
          </div>
        </article>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .cc-intro {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  /* Header */
  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-eyebrow {
    font-size: var(--text-xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-accent);
    margin-bottom: var(--space-2);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  /* Section headings */
  .detail-heading {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-heading);
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-paragraph {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    margin-bottom: var(--space-4);
  }

  .detail-paragraph strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Compare cards (library vs assistant) */
  .compare-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
    margin: var(--space-6) 0 var(--space-8);
  }

  .compare-card {
    padding: var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .compare-card--accent {
    border-color: var(--color-accent);
    background-color: var(--color-tag-bg);
  }

  .compare-label {
    font-size: var(--text-xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--color-text-secondary);
    margin-bottom: var(--space-2);
  }

  .compare-card--accent .compare-label {
    color: var(--color-accent);
  }

  .compare-title {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-2);
  }

  .compare-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  /* Inline tag inside paragraph */
  .tech-tag {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
    font-size: var(--text-xs);
    font-weight: 500;
    border-radius: var(--border-radius);
  }

  .inline-tag {
    vertical-align: baseline;
  }

  /* Agentic loop steps */
  .loop-list {
    list-style: none;
    padding: 0;
    margin: var(--space-6) 0 var(--space-4);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .loop-step {
    display: flex;
    align-items: flex-start;
    gap: var(--space-6);
    padding: var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: all var(--transition);
  }

  .loop-step:hover {
    border-color: var(--color-accent);
    transform: translateX(4px);
  }

  .loop-num {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-base);
    font-weight: 700;
  }

  .loop-body {
    flex: 1;
  }

  .loop-title {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-heading);
    margin-bottom: var(--space-2);
  }

  .loop-en {
    font-size: var(--text-xs);
    font-weight: 500;
    color: var(--color-text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-left: var(--space-2);
  }

  .loop-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  /* Feature cards grid */
  .feature-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
    margin-top: var(--space-6);
  }

  .feature-card {
    position: relative;
    padding: var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg);
    transition: all var(--transition);
  }

  .feature-card:hover {
    border-color: var(--color-accent);
    box-shadow: 0 4px 12px var(--color-card-shadow);
    transform: translateY(-2px);
  }

  .feature-index {
    font-size: var(--text-xs);
    font-weight: 700;
    color: var(--color-accent);
    letter-spacing: 0.08em;
    margin-bottom: var(--space-2);
  }

  .feature-title {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-1);
  }

  .feature-alias {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-3);
    padding-bottom: var(--space-3);
    border-bottom: 1px dashed var(--color-border);
  }

  .feature-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  /* Entry-point cards (Desktop / VS Code / CLI) */
  .entry-grid {
    display: flex;
    flex-direction: column;
    gap: var(--space-8);
    margin-top: var(--space-6);
  }

  .entry-card {
    padding: var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg);
    transition: border-color var(--transition);
  }

  .entry-card:hover {
    border-color: var(--color-accent);
  }

  .entry-header {
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-4);
    border-bottom: 1px dashed var(--color-border);
  }

  .entry-index {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 50%;
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-xs);
    font-weight: 700;
    margin-bottom: var(--space-2);
  }

  .entry-title {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-1);
  }

  .entry-tagline {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
  }

  .entry-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.9;
    margin-bottom: var(--space-4);
  }

  .entry-desc strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .entry-fit-label {
    font-size: var(--text-xs);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-accent);
    margin-bottom: var(--space-2);
  }

  .entry-fit-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-4);
  }

  .entry-fit-list li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-2);
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  .entry-fit-list li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  .entry-transition {
    margin-top: var(--space-4);
    padding: var(--space-4) var(--space-8);
    background-color: var(--color-bg-secondary);
    border-left: 3px solid var(--color-accent);
    border-radius: 0 var(--border-radius) var(--border-radius) 0;
  }

  .entry-transition__label {
    font-size: var(--text-xs);
    font-weight: 700;
    color: var(--color-accent);
    letter-spacing: 0.04em;
    margin-bottom: var(--space-2);
  }

  .entry-transition__body {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  /* Mobile */
  @media (max-width: 720px) {
    .compare-grid,
    .feature-grid {
      grid-template-columns: 1fr;
    }

    .loop-step {
      gap: var(--space-4);
      padding: var(--space-6);
    }

    .loop-num {
      width: 2rem;
      height: 2rem;
      font-size: var(--text-sm);
    }

    .entry-card {
      padding: var(--space-6);
    }

    .entry-transition {
      padding: var(--space-3) var(--space-4);
    }
  }
</style>
