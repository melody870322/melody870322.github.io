---
import BaseLayout from '../../../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="現成 MCP 的課堂版工具面設計 — AI Agent 拼圖"
  description="不要直接把整個 MCP 系統交給 AI，而是先把任務限縮、再定義工具面。透過 5 步設計模板（誰在用 → 要做什麼 → 不能做什麼 → 最小工具集 → 出錯時怎麼辦），把現成 MCP 改造成適合你場景的小版本。"
  lang="zh"
>
  <section class="section">
    <div class="container ai-agent-classroom-tools">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">不要把整個系統都丟給 AI</h1>
        <p class="detail-subtitle">
          前面幾頁，我們已經慢慢把路走出來了：先裝好 Gemini CLI、接上第一個現成 MCP；接著看到 Notion 可以真的跑一輪工作流；再來學到工具不是越多越好，也不是所有東西都該做成 tool。這一頁要處理的是<strong>下一個很重要的問題</strong>。
        </p>
      </header>

      <div class="highlight-card highlight-card--strong">
        <p class="highlight-text">
          就算我已經接上現成 MCP，<br/>
          <strong>也不代表我應該把整個系統原封不動交給 AI。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        很多時候，真正好用、也比較安全的做法，<strong>不是直接拿現成工具來用，而是先把它改造成一個課堂版工具面</strong>。也就是說，不是「這個 MCP 有什麼我就全給」，而是<strong>「我現在這個情境，到底需要它做哪些事」</strong>。
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          真正的安全，不只是改設定，而是<strong>先重寫任務，再決定工具面</strong>。
        </p>
      </div>

      <!-- Section: What is classroom-version tool surface -->
      <h2 class="detail-heading">什麼叫「課堂版工具面」</h2>
      <p class="detail-paragraph">
        你可以先把「課堂版工具面」想成一個很簡單的概念：
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          <strong>把原本很大、很完整、很通用的工具能力，縮成一個符合情境的小版本。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        這個小版本不一定比較厲害，甚至常常比較簡單。但它有幾個很大的好處：
      </p>
      <ul class="check-list">
        <li>比較容易懂</li>
        <li>比較容易觀察</li>
        <li>比較不容易出事</li>
        <li>比較接近真實任務</li>
      </ul>

      <div class="callout-card">
        <p class="callout-text">
          所以這裡說的「課堂版」，它比較像是：<strong>我先幫這個工具畫出邊界，讓它只做這個任務、這群使用者真正需要的事。</strong>
        </p>
      </div>

      <!-- Section: Why "manage entire Notion" doesn't work -->
      <h2 class="detail-heading">為什麼不能直接說「讓 AI 幫我管理整個 Notion」</h2>
      <p class="detail-paragraph">
        這種說法聽起來很自然，但其實太大了。最大的問題，不是它做不到，而是：
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          <strong>邊界太模糊。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        你一旦沒有先把任務寫小，後面就會跟著出現很多問題：
      </p>
      <ul class="cross-list">
        <li>到底哪些頁面可以碰？</li>
        <li>哪些頁面不能碰？</li>
        <li>它可以建立新內容嗎？</li>
        <li>它可以修改舊內容嗎？</li>
        <li>它只是在整理，還是在幫你做決策？</li>
        <li>出錯時要停在哪裡？</li>
      </ul>
      <p class="detail-paragraph">
        所以真正成熟的做法不是先問「這個 MCP 能做什麼」，而是先問：<strong>我現在到底要它做哪一小段工作？</strong>
      </p>

      <!-- Section: Key sentence -->
      <h2 class="detail-heading">這一頁最重要的一句話：先限縮任務，再定義工具面</h2>
      <p class="detail-paragraph">
        很多人會以為安全設定的重點，是去改 <code>includeTools</code> 或 <code>excludeTools</code>。那些當然重要，但<strong>它們通常不是第一步</strong>。
      </p>

      <div class="highlight-card highlight-card--strong">
        <p class="highlight-text">
          第一步應該是：<br/>
          <strong>先把任務本身寫小。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        因為如果任務本身就很大、很模糊，那你後面怎麼限制工具，都還是會有很多灰色地帶。你可以把這件事想成：
      </p>

      <div class="analogy-grid">
        <article class="analogy-card analogy-card--bad">
          <p class="analogy-card__label">×</p>
          <p class="analogy-card__text">不是先問「<strong>我有哪幾把刀</strong>」</p>
        </article>
        <article class="analogy-card analogy-card--good">
          <p class="analogy-card__label">✓</p>
          <p class="analogy-card__text">而是先問「<strong>我現在是要切水果，還是要雕刻，還是只是要削皮</strong>」</p>
        </article>
      </div>

      <div class="callout-card">
        <p class="callout-text">
          <strong>任務不清楚，工具再精緻也會亂。任務先寫清楚，工具面才有可能設得好。</strong>
        </p>
      </div>

      <!-- Section: Practical rewrite -->
      <h2 class="detail-heading">一個很實用的改寫方式：把大任務改成小任務</h2>

      <div class="rewrite-grid">
        <article class="rewrite-card rewrite-card--bad">
          <p class="rewrite-card__label">太大的寫法</p>
          <p class="rewrite-card__text">讓 AI 幫我管理整個 Notion。</p>
        </article>
        <div class="rewrite-arrow" aria-hidden="true">↓</div>
        <article class="rewrite-card rewrite-card--good">
          <p class="rewrite-card__label">比較好的寫法</p>
          <p class="rewrite-card__text">
            只允許 AI 在指定的課堂資料庫裡建立「課堂反思頁面」，而且內容必須依照固定格式產生，<strong>不要修改既有頁面</strong>。
          </p>
        </article>
      </div>

      <p class="detail-paragraph">
        你會發現，改完之後整件事突然清楚很多：
      </p>

      <ul class="dimension-list">
        <li class="dimension-item">
          <span class="dimension-key">使用範圍</span>
          <span class="dimension-value">指定資料庫</span>
        </li>
        <li class="dimension-item">
          <span class="dimension-key">可以做的事</span>
          <span class="dimension-value">建立頁面</span>
        </li>
        <li class="dimension-item">
          <span class="dimension-key">不能做的事</span>
          <span class="dimension-value">不改既有頁面</span>
        </li>
        <li class="dimension-item">
          <span class="dimension-key">內容格式</span>
          <span class="dimension-value">固定格式</span>
        </li>
        <li class="dimension-item">
          <span class="dimension-key">任務目的</span>
          <span class="dimension-value">課堂反思</span>
        </li>
      </ul>

      <div class="callout-card">
        <p class="callout-text">
          這就比較像一個<strong>可教、可管、可驗證</strong>的 agent。
        </p>
      </div>

      <!-- Section: Design template -->
      <h2 class="detail-heading">大家都適合使用的設計步驟模板</h2>
      <p class="detail-paragraph">
        要把現成 MCP 改成課堂版工具面，可以照下面這 5 步走：
      </p>

      <ol class="template-list">
        <li class="template-item">
          <span class="template-num">1</span>
          <div class="template-body">
            <h3 class="template-title">使用者是誰</h3>
            <p class="template-desc">
              第一個要想的不是工具，而是：<strong>這個工具面是給誰用的？</strong>因為不同使用者，適合的能力差很多。
            </p>
            <ul class="check-list check-list--inset">
              <li>如果是<strong>學生</strong>，可能需要的是搜尋、讀取、建立草稿</li>
              <li>如果是<strong>助教</strong>，可能還需要整理、分類、檢查格式</li>
              <li>如果是<strong>老師</strong>，可能還需要總覽、彙整、審核</li>
            </ul>
            <p class="template-desc">
              只要這一步沒寫，後面很容易越做越大。
            </p>
          </div>
        </li>

        <li class="template-item">
          <span class="template-num">2</span>
          <div class="template-body">
            <h3 class="template-title">它真正要完成的任務是什麼</h3>
            <p class="template-desc">
              注意，這裡講的是「<strong>哪一種任務</strong>」，不是「所有可能的事」。例如比較好的任務寫法可能是：
            </p>
            <ul class="check-list check-list--inset">
              <li>幫學生根據既有資料整理一頁<strong>課堂反思草稿</strong></li>
              <li>幫專題小組把議題整理成<strong>每週進度摘要</strong></li>
              <li>幫使用者把指定資料夾中的文件<strong>分類成三類</strong></li>
              <li>幫讀者把一頁內容改寫成<strong>簡報式重點</strong></li>
            </ul>
            <p class="template-desc">
              這些任務都有一個共通點：<strong>範圍清楚，而且有明確的輸入和輸出</strong>。這樣設計出來的工具面，才比較穩。
            </p>
          </div>
        </li>

        <li class="template-item">
          <span class="template-num">3</span>
          <div class="template-body">
            <h3 class="template-title">它不能做什麼</h3>
            <p class="template-desc">
              這一點<strong>非常重要，而且常常被忽略</strong>。很多人只寫：它能做什麼、它會做什麼、它適合做什麼。但如果你真的要把它設計成課堂版工具面，最好還要寫：<strong>它不能做什麼</strong>。例如：
            </p>
            <ul class="cross-list cross-list--inset">
              <li>不修改既有課堂公告</li>
              <li>不刪除任何頁面</li>
              <li>不處理課程以外的資料庫</li>
              <li>不代替學生做最後定稿</li>
              <li>不自行發布內容</li>
            </ul>
            <p class="template-desc">
              只要這些不能做的事先寫出來，<strong>你整個工具面的邊界就會清楚很多</strong>。
            </p>
          </div>
        </li>

        <li class="template-item">
          <span class="template-num">4</span>
          <div class="template-body">
            <h3 class="template-title">最小工具集合是什麼</h3>
            <p class="template-desc">
              等你前面三件事都想清楚之後，第四步才是回來問：<strong>為了完成這個任務，最少需要哪幾個工具？</strong>
            </p>
            <p class="template-desc">
              例如你要設計一個「Notion 課堂反思助理」，如果它的任務只是搜尋主題頁面、讀取內容、依固定格式建立反思頁面，那最小工具集合就可能是：
            </p>
            <ul class="check-list check-list--inset">
              <li>搜尋頁面</li>
              <li>讀頁面</li>
              <li>建頁面</li>
            </ul>
            <p class="template-desc">
              這時「更新頁面」其實就可以先不要開。<strong>不是因為它永遠不需要，而是因為這個版本的任務還沒需要它。</strong>
            </p>
            <div class="callout-card callout-card--inset">
              <p class="callout-text">
                這就是前一頁和這一頁真正接起來的地方。前一頁是講「<strong>工具不要全開</strong>」，這一頁則是講「<strong>為什麼這裡只需要這幾個工具</strong>」。
              </p>
            </div>
          </div>
        </li>

        <li class="template-item">
          <span class="template-num">5</span>
          <div class="template-body">
            <h3 class="template-title">如果出錯，它該怎麼回應</h3>
            <p class="template-desc">
              一般人最常忽略這件事。一個好的課堂版工具面，不只要知道正常時怎麼工作，也要知道：<strong>如果資料找不到、權限不夠、格式不對，它要怎麼回應</strong>。
            </p>

            <div class="response-grid">
              <article class="response-card response-card--bad">
                <p class="response-card__label">不該做的</p>
                <ul class="response-list response-list--bad">
                  <li>硬做下去</li>
                  <li>猜一個答案</li>
                  <li>自己補空白</li>
                </ul>
              </article>
              <article class="response-card response-card--good">
                <p class="response-card__label">應該做的</p>
                <ul class="response-list response-list--good">
                  <li>說明缺了什麼</li>
                  <li>停在目前這一步</li>
                  <li>請使用者確認下一步</li>
                  <li>保留人工決定的空間</li>
                </ul>
              </article>
            </div>

            <p class="template-desc">
              這樣比較像專業工作流。因為你不是在做一個會亂衝的機器，而是在做一個<strong>知道什麼時候該停下來的 agent</strong>。
            </p>
          </div>
        </li>
      </ol>

      <!-- Example 1: Notion -->
      <div class="case-header">
        <p class="case-label">範例一</p>
        <h2 class="case-title">把 Notion 改造成「課堂反思助理」</h2>
      </div>

      <dl class="case-spec">
        <div class="case-spec__row">
          <dt class="case-spec__key">使用者是誰</dt>
          <dd class="case-spec__value">修這門課的學生</dd>
        </div>
        <div class="case-spec__row">
          <dt class="case-spec__key">任務是什麼</dt>
          <dd class="case-spec__value">根據指定主題與既有課堂資料，建立一頁新的課堂反思草稿</dd>
        </div>
        <div class="case-spec__row">
          <dt class="case-spec__key">什麼不能做</dt>
          <dd class="case-spec__value">
            <ul class="cross-list cross-list--inset">
              <li>不修改既有頁面</li>
              <li>不刪除頁面</li>
              <li>不碰課程以外的資料庫</li>
              <li>不代替學生做最後定稿</li>
            </ul>
          </dd>
        </div>
        <div class="case-spec__row">
          <dt class="case-spec__key">最小工具集合</dt>
          <dd class="case-spec__value">
            <ul class="check-list check-list--inset">
              <li>搜尋頁面</li>
              <li>讀頁面</li>
              <li>建頁面</li>
            </ul>
          </dd>
        </div>
        <div class="case-spec__row">
          <dt class="case-spec__key">錯誤時怎麼回覆</dt>
          <dd class="case-spec__value">
            <ul class="check-list check-list--inset">
              <li>如果<strong>找不到相關資料</strong>，就告知找不到，並請使用者重新指定主題</li>
              <li>如果<strong>權限不足</strong>，就停下來，不自行改用別的頁面</li>
            </ul>
          </dd>
        </div>
      </dl>

      <div class="callout-card">
        <p class="callout-text">
          這樣一寫完，它就<strong>不再是「讓 AI 管整個 Notion」，而是一個很清楚的課堂版 agent</strong>。
        </p>
      </div>

      <!-- Example 2: Google Drive -->
      <div class="case-header">
        <p class="case-label">範例二</p>
        <h2 class="case-title">把 Google Drive 改造成「文件整理員」</h2>
      </div>

      <dl class="case-spec">
        <div class="case-spec__row">
          <dt class="case-spec__key">使用者是誰</dt>
          <dd class="case-spec__value">需要整理課程資料或專案資料的人</dd>
        </div>
        <div class="case-spec__row">
          <dt class="case-spec__key">任務是什麼</dt>
          <dd class="case-spec__value">讀取指定資料夾中的文件標題與內容摘要，分類成幾個主題群組</dd>
        </div>
        <div class="case-spec__row">
          <dt class="case-spec__key">什麼不能做</dt>
          <dd class="case-spec__value">
            <ul class="cross-list cross-list--inset">
              <li>不刪除原始檔</li>
              <li>不移動原始檔</li>
              <li>不改動檔名</li>
              <li>不碰指定資料夾以外的內容</li>
            </ul>
          </dd>
        </div>
        <div class="case-spec__row">
          <dt class="case-spec__key">最小工具集合</dt>
          <dd class="case-spec__value">
            <ul class="check-list check-list--inset">
              <li>列出文件</li>
              <li>讀取文件內容</li>
              <li>建立整理結果頁面</li>
            </ul>
          </dd>
        </div>
        <div class="case-spec__row">
          <dt class="case-spec__key">錯誤時怎麼回覆</dt>
          <dd class="case-spec__value">
            <ul class="check-list check-list--inset">
              <li>如果<strong>資料夾讀不到</strong>，就停下來並說明</li>
              <li>如果<strong>某些檔案權限不足</strong>，就標示哪些檔案沒被納入</li>
            </ul>
          </dd>
        </div>
      </dl>

      <div class="highlight-card">
        <p class="highlight-text">
          課堂版工具面的重點從來都不是系統本身，而是<strong>任務被寫得夠不夠清楚</strong>。
        </p>
      </div>

      <!-- Section: Closing - design habit -->
      <h2 class="detail-heading">這一頁其實不是在教設定，而是在教設計</h2>
      <p class="detail-paragraph">
        表面上看起來，這一頁好像在講工具怎麼限縮、任務怎麼改寫、agent 怎麼設計。但更核心一點地說，這一頁其實是在教一種<strong>設計習慣</strong>：
      </p>

      <div class="highlight-card highlight-card--strong">
        <p class="highlight-text">
          <strong>不要直接拿現成系統就上，而是先把它改造成適合你場景的小版本。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        這個習慣非常重要。<strong>因為它不只適用在這門課，也適用在你之後碰到的任何 AI 工具。</strong>你只要先問這 5 個問題，通常就不會走太歪：
      </p>

      <ol class="closing-list">
        <li class="closing-item">
          <span class="closing-num">1</span>
          <span class="closing-text">誰在用？</span>
        </li>
        <li class="closing-item">
          <span class="closing-num">2</span>
          <span class="closing-text">要做什麼？</span>
        </li>
        <li class="closing-item">
          <span class="closing-num">3</span>
          <span class="closing-text">不能做什麼？</span>
        </li>
        <li class="closing-item">
          <span class="closing-num">4</span>
          <span class="closing-text">最少要哪些工具？</span>
        </li>
        <li class="closing-item">
          <span class="closing-num">5</span>
          <span class="closing-text">出錯時怎麼辦？</span>
        </li>
      </ol>

    </div>
  </section>
</BaseLayout>

<style>
  .ai-agent-classroom-tools {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  /* Header */
  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  .detail-subtitle strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Section headings */
  .detail-heading {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-heading);
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-paragraph {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    margin-bottom: var(--space-4);
  }

  .detail-paragraph strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Inline code */
  :global(.detail-paragraph) code,
  :global(.detail-subtitle) code,
  :global(.check-list) code,
  :global(.cross-list) code,
  :global(.callout-text) code,
  :global(.highlight-text) code,
  :global(.template-desc) code,
  :global(.case-spec__value) code {
    padding: 0.1em 0.4em;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 0.9em;
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
    border-radius: var(--border-radius);
  }

  /* Check list */
  .check-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .check-list li {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .check-list li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  .check-list li strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .check-list--inset {
    margin: var(--space-3) 0 var(--space-3);
  }

  .check-list--inset li {
    font-size: var(--text-sm);
    line-height: 1.8;
    margin-bottom: var(--space-1);
  }

  /* Cross list */
  .cross-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .cross-list li {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.9;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .cross-list li::before {
    content: '\2717';
    position: absolute;
    left: 0;
    color: var(--color-text-secondary);
    font-weight: 700;
  }

  .cross-list--inset {
    margin: var(--space-3) 0 var(--space-3);
  }

  .cross-list--inset li {
    font-size: var(--text-sm);
    line-height: 1.8;
    margin-bottom: var(--space-1);
  }

  /* Callout card */
  .callout-card {
    margin: var(--space-6) 0 var(--space-8);
    padding: var(--space-4) var(--space-8);
    border-left: 4px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .callout-card--inset {
    margin: var(--space-4) 0 var(--space-2);
    padding: var(--space-3) var(--space-6);
  }

  .callout-text {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.9;
    margin: 0;
  }

  .callout-text strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Highlight card */
  .highlight-card {
    margin: var(--space-4) 0 var(--space-6);
    padding: var(--space-3) var(--space-8);
    border: 1px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-tag-bg);
  }

  .highlight-card--strong {
    padding: var(--space-6) var(--space-8);
    border-width: 2px;
  }

  .highlight-text {
    font-size: var(--text-base);
    color: var(--color-heading);
    line-height: 1.8;
    margin: 0;
    text-align: center;
  }

  .highlight-text strong {
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Analogy grid (knife metaphor) */
  .analogy-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
    margin: var(--space-6) 0 var(--space-6);
  }

  .analogy-card {
    padding: var(--space-3) var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    display: flex;
    align-items: flex-start;
    gap: var(--space-4);
  }

  .analogy-card--bad {
    opacity: 0.85;
  }

  .analogy-card--good {
    border-color: var(--color-accent);
    border-width: 2px;
    background-color: var(--color-tag-bg);
  }

  .analogy-card__label {
    flex-shrink: 0;
    width: 1.75rem;
    height: 1.75rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    font-size: var(--text-base);
    font-weight: 700;
    margin: 0;
  }

  .analogy-card--bad .analogy-card__label {
    background-color: var(--color-text-muted);
    color: #fff;
  }

  .analogy-card--good .analogy-card__label {
    background-color: var(--color-accent);
    color: #fff;
  }

  .analogy-card__text {
    margin: 0;
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.7;
  }

  .analogy-card__text strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Rewrite grid (before / after task wording) */
  .rewrite-grid {
    display: flex;
    flex-direction: column;
    align-items: stretch;
    gap: var(--space-3);
    margin: var(--space-6) 0 var(--space-6);
  }

  .rewrite-card {
    padding: var(--space-3) var(--space-8);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
  }

  .rewrite-card--bad {
    background-color: var(--color-bg-secondary);
    border-left: 4px solid var(--color-text-muted);
    opacity: 0.85;
  }

  .rewrite-card--good {
    background-color: var(--color-tag-bg);
    border: 2px solid var(--color-accent);
  }

  .rewrite-card__label {
    margin: 0 0 var(--space-2);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.05em;
    color: var(--color-text-secondary);
    text-transform: uppercase;
  }

  .rewrite-card--good .rewrite-card__label {
    color: var(--color-accent);
  }

  .rewrite-card__text {
    margin: 0;
    font-size: var(--text-sm);
    color: var(--color-heading);
    line-height: 1.8;
  }

  .rewrite-card__text strong {
    color: var(--color-accent);
    font-weight: 700;
  }

  .rewrite-arrow {
    text-align: center;
    color: var(--color-accent);
    font-size: var(--text-xl);
    line-height: 1;
    font-weight: 700;
  }

  /* Dimension list (5 dimensions key:value) */
  .dimension-list {
    list-style: none;
    padding: var(--space-5) var(--space-6);
    margin: var(--space-4) 0 var(--space-6);
    border-radius: var(--border-radius);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .dimension-item {
    display: flex;
    align-items: baseline;
    gap: var(--space-4);
    margin: 0;
  }

  .dimension-key {
    flex-shrink: 0;
    min-width: 6rem;
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.05em;
    color: var(--color-accent);
    padding: var(--space-1) var(--space-3);
    background-color: var(--color-tag-bg);
    border-radius: var(--border-radius);
    text-align: center;
  }

  .dimension-value {
    flex: 1;
    font-size: var(--text-sm);
    color: var(--color-heading);
    font-weight: 500;
    line-height: 1.7;
  }

  /* Template list (5 numbered design steps) */
  .template-list {
    list-style: none;
    padding: 0;
    margin: var(--space-3) 0 var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .template-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    padding: var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    transition: border-color var(--transition);
  }

  .template-item:hover {
    border-color: var(--color-accent);
  }

  .template-num {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: 50%;
    background-color: var(--color-accent);
    color: #fff;
    font-size: var(--text-sm);
    font-weight: 700;
    margin-top: 0.1rem;
  }

  .template-body {
    flex: 1;
    min-width: 0;
  }

  .template-title {
    margin: 0 0 var(--space-3);
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-heading);
    line-height: 1.4;
  }

  .template-desc {
    margin: 0 0 var(--space-3);
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.9;
  }

  .template-desc:last-child {
    margin-bottom: 0;
  }

  .template-desc strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Response grid (Step 5: bad vs good error responses) */
  .response-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3);
    margin: var(--space-3) 0 var(--space-3);
  }

  .response-card {
    padding: var(--space-3);
    border-radius: var(--border-radius);
    border: 1px solid var(--color-border);
  }

  .response-card--bad {
    background-color: var(--color-bg);
    opacity: 0.85;
  }

  .response-card--good {
    background-color: var(--color-tag-bg);
    border-color: var(--color-accent);
  }

  .response-card__label {
    margin: 0 0 var(--space-2);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.05em;
  }

  .response-card--bad .response-card__label {
    color: var(--color-text-secondary);
  }

  .response-card--good .response-card__label {
    color: var(--color-accent);
  }

  .response-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .response-list li {
    font-size: var(--text-xs);
    line-height: 1.8;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-1);
  }

  .response-list--bad li {
    color: var(--color-text-secondary);
  }

  .response-list--bad li::before {
    content: '\2717';
    position: absolute;
    left: 0;
    color: var(--color-text-muted);
    font-weight: 700;
  }

  .response-list--good li {
    color: var(--color-text);
  }

  .response-list--good li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Case header (範例一 / 範例二) */
  .case-header {
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
  }

  .case-label {
    display: inline-block;
    margin: 0 0 var(--space-2);
    padding: var(--space-1) var(--space-4);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.08em;
    color: #fff;
    background-color: var(--color-accent);
    border-radius: var(--border-radius);
  }

  .case-title {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-heading);
    margin: 0;
    line-height: 1.4;
  }

  /* Case spec (definition list) */
  .case-spec {
    margin: var(--space-4) 0 var(--space-6);
    padding: 0;
    border: 2px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    overflow: hidden;
  }

  .case-spec__row {
    display: grid;
    grid-template-columns: 8.5rem 1fr;
    gap: var(--space-4);
    padding: var(--space-4) var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .case-spec__row:last-child {
    border-bottom: none;
  }

  .case-spec__key {
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-accent);
    line-height: 1.7;
    margin: 0;
  }

  .case-spec__value {
    margin: 0;
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  .case-spec__value strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .case-spec__value .check-list,
  .case-spec__value .cross-list {
    margin: 0;
  }

  /* Closing list (5 design questions) */
  .closing-list {
    list-style: none;
    padding: 0;
    margin: var(--space-6) 0 var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .closing-item {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-3) var(--space-6);
    border: 1px solid var(--color-border);
    border-left: 3px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg);
  }

  .closing-num {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 50%;
    background-color: var(--color-tag-bg);
    color: var(--color-accent);
    font-size: var(--text-xs);
    font-weight: 700;
  }

  .closing-text {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-heading);
    line-height: 1.6;
  }

  /* Mobile */
  @media (max-width: 720px) {
    .analogy-grid,
    .response-grid {
      grid-template-columns: 1fr;
    }

    .callout-card,
    .highlight-card {
      padding: var(--space-4) var(--space-6);
    }

    .highlight-card--strong {
      padding: var(--space-5) var(--space-6);
    }

    .template-item {
      gap: var(--space-4);
      padding: var(--space-5);
    }

    .template-num {
      width: 1.75rem;
      height: 1.75rem;
      font-size: var(--text-xs);
    }

    .case-spec__row {
      grid-template-columns: 1fr;
      gap: var(--space-2);
      padding: var(--space-4) var(--space-5);
    }

    .dimension-list {
      padding: var(--space-4) var(--space-5);
    }

    .dimension-item {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-2);
    }

    .dimension-key {
      min-width: auto;
    }

    .rewrite-card {
      padding: var(--space-4) var(--space-5);
    }

    .closing-item {
      padding: var(--space-4) var(--space-5);
      gap: var(--space-4);
    }
  }
</style>
