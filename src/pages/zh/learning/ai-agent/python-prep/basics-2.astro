---
import BaseLayout from '../../../../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="基礎二 — Python 基礎：為自製 MCP Server 做準備"
  description="接續基礎一，補上自製 MCP Server 真的會用到的進階觀念：import、Path、讀寫檔案、exists、try / except、type hints、docstring、decorator，以及 if __name__ == '__main__'。"
  lang="zh"
>
  <section class="section">
    <div class="container python-basics">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">做 MCP 前，你只需要這些 Python（續）</h1>
        <p class="detail-subtitle">
          基礎一已經把 <strong>變數、字串、if、list、for、函式、return</strong> 都先帶過了。這一頁要做的事情很單純：把<strong>第一個自製 MCP server</strong> 還會用到的那幾個觀念補齊。
        </p>
      </header>

      <div class="highlight-card">
        <p class="highlight-text">
          一樣的原則：<strong>不學一整套 Python，只學接下來真的會用到的那一小塊。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        看到 <code>def</code>、<code>return</code>、<code>Path("notes")</code>、<code>@mcp.tool()</code> 這些東西時，心裡很容易先卡一下。<strong>沒關係，我們就一個一個拆開看</strong>。
      </p>

      <!-- Concept: import -->
      <div class="concept-header">
        <p class="concept-label">概念 08</p>
        <h2 class="concept-title">import：借用別人已經寫好的工具</h2>
      </div>

      <p class="detail-paragraph">
        Python 不需要所有事情都自己從零開始寫。<strong>很多常用功能，已經有人寫好了</strong>。我們可以用 <code>import</code> 把它們拿進來。例如：
      </p>

      <pre class="code-block code-block--py"><code>from pathlib import Path</code></pre>

      <p class="detail-paragraph">
        這句可以先理解成：<strong>從 <code>pathlib</code> 這個工具箱裡，拿出 <code>Path</code> 這個工具</strong>。
      </p>
      <p class="detail-paragraph">
        之後做 MCP Server 時，你會看到：
      </p>

      <pre class="code-block code-block--py"><code>from pathlib import Path
from mcp.server.fastmcp import FastMCP</code></pre>

      <p class="detail-paragraph">它們的意思是：</p>
      <ul class="check-list">
        <li>我要借用 <code>Path</code> 來<strong>處理檔案路徑</strong></li>
        <li>我要借用 <code>FastMCP</code> 來<strong>建立 MCP Server</strong></li>
      </ul>

      <div class="highlight-card">
        <p class="highlight-text">
          所以 <code>import</code> 可以先理解成：<strong>把需要的工具拿進來。</strong>
        </p>
      </div>

      <!-- Concept: Path -->
      <div class="concept-header">
        <p class="concept-label">概念 09</p>
        <h2 class="concept-title">Path：用比較清楚的方式處理檔案路徑</h2>
      </div>

      <p class="detail-paragraph">
        做 MCP Server 時，我們常常需要處理資料夾和檔案。例如：
      </p>
      <ul class="check-list">
        <li>課堂筆記放在哪裡？</li>
        <li>草稿要存到哪裡？</li>
        <li>這個檔案存不存在？</li>
        <li>我要讀哪一份筆記？</li>
      </ul>
      <p class="detail-paragraph">
        Python 的 <code>Path</code> 可以讓這些事情比較好讀。例如：
      </p>

      <pre class="code-block code-block--py"><code>from pathlib import Path

BASE_DIR = Path(__file__).parent
NOTES_DIR = BASE_DIR / "class_notes"

print(NOTES_DIR)</code></pre>

      <p class="detail-paragraph">這段程式可以先這樣理解：</p>
      <ul class="check-list">
        <li><code>Path(__file__)</code> 是<strong>目前這個 Python 檔案的位置</strong></li>
        <li><code>.parent</code> 是<strong>它所在的資料夾</strong></li>
        <li><code>BASE_DIR / "class_notes"</code> 是<strong>在這個資料夾底下找到 <code>class_notes</code></strong></li>
      </ul>

      <div class="callout-card">
        <p class="callout-text">
          這裡不需要一開始就理解所有細節。你只要先記得：<strong><code>Path</code> 是在幫我們找資料夾和檔案。</strong>
        </p>
      </div>

      <!-- Concept: read file -->
      <div class="concept-header">
        <p class="concept-label">概念 10</p>
        <h2 class="concept-title">讀取檔案：讓 Python 看見筆記內容</h2>
      </div>

      <p class="detail-paragraph">
        接下來我們會讓 Python 讀取課堂筆記。先假設你有一個資料夾叫做 <code>class_notes</code>，裡面有一個檔案 <code>week1.md</code>。我們可以用 Python 讀取它：
      </p>

      <pre class="code-block code-block--py"><code>from pathlib import Path

path = Path("class_notes/week1.md")
content = path.read_text(encoding="utf-8")
print(content)</code></pre>

      <p class="detail-paragraph">這段程式的意思是：</p>
      <ul class="check-list">
        <li>先<strong>指定檔案位置</strong></li>
        <li>再用 <code>read_text</code> <strong>讀取文字內容</strong></li>
        <li>最後用 <code>print</code> <strong>顯示出來</strong></li>
      </ul>
      <p class="detail-paragraph">
        這裡的 <code>encoding="utf-8"</code> 是為了<strong>讓中文比較不容易亂碼</strong>。
      </p>

      <div class="callout-card">
        <p class="callout-text">
          如果你執行時出錯，先不要慌。第一次練習讀檔案時，<strong>最常見的錯誤通常不是你不會寫程式，而是檔案路徑打錯了</strong>。例如資料夾名稱不一樣、檔案名稱不一樣，或是終端機目前所在的位置不對。
        </p>
      </div>

      <!-- Concept: write file -->
      <div class="concept-header">
        <p class="concept-label">概念 11</p>
        <h2 class="concept-title">寫入檔案：讓 Python 建立草稿</h2>
      </div>

      <p class="detail-paragraph">
        除了讀取檔案，我們也可以讓 Python 建立新的文字檔。先建立一個資料夾 <code>drafts</code>，然後可以寫：
      </p>

      <pre class="code-block code-block--py"><code>from pathlib import Path

path = Path("drafts/my_reflection.md")
path.write_text("# 我的反思\n\n這是第一版草稿。", encoding="utf-8")</code></pre>

      <p class="detail-paragraph">
        這段程式會在 <code>drafts</code> 資料夾裡建立一個檔案 <code>my_reflection.md</code>，打開後裡面內容會是：
      </p>

      <pre class="code-block code-block--py"><code># 我的反思

這是第一版草稿。</code></pre>

      <p class="detail-paragraph">這裡要特別注意：</p>

      <div class="highlight-card">
        <p class="highlight-text">
          <code>write_text</code> 如果遇到已經存在的檔案，<strong>可能會覆蓋原本內容。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        所以之後我們做 MCP Server 時，<strong>不會讓 AI 隨便覆蓋舊檔案</strong>。我們會先檢查檔案是否存在。第一次做 AI 寫入功能時，建議記住這個原則：
      </p>

      <div class="highlight-card highlight-card--strong">
        <p class="highlight-text">
          <strong>先建立新檔，不要直接修改原始資料。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        這樣就算 AI 寫得不好，也只是多一份草稿，<strong>不會破壞原本的筆記</strong>。
      </p>

      <!-- Concept: exists -->
      <div class="concept-header">
        <p class="concept-label">概念 12</p>
        <h2 class="concept-title">exists：先檢查，再行動</h2>
      </div>

      <p class="detail-paragraph">
        在讀檔或寫檔之前，我們可以<strong>先檢查檔案是否存在</strong>。例如：
      </p>

      <pre class="code-block code-block--py"><code>from pathlib import Path

path = Path("class_notes/week1.md")

if path.exists():
    print("找到了")
else:
    print("找不到這個檔案")</code></pre>

      <p class="detail-paragraph">
        這裡的 <code>path.exists()</code> 意思是：<strong>這個檔案或資料夾存在嗎？</strong>如果存在，就顯示「找到了」；如果不存在，就顯示「找不到這個檔案」。
      </p>

      <div class="callout-card">
        <p class="callout-text">
          這個觀念在 MCP Server 裡很重要。如果 AI 要讀一份不存在的筆記，<strong>工具不應該亂猜</strong>。它應該清楚回覆：「找不到這份筆記，請先確認檔名。」
        </p>
      </div>

      <div class="highlight-card highlight-card--strong">
        <p class="highlight-text">
          好的工具不是什麼都硬做。<br/>
          <strong>好的工具知道什麼時候該停下來。</strong>
        </p>
      </div>

      <!-- Concept: string handling for filename -->
      <div class="concept-header">
        <p class="concept-label">概念 13</p>
        <h2 class="concept-title">字串處理：把標題變成比較安全的檔名</h2>
      </div>

      <p class="detail-paragraph">
        之後我們會讓 AI 根據標題建立草稿。例如標題可能是「我對 MCP 的理解」。<strong>但標題不一定適合直接拿來當檔名</strong>：
      </p>
      <ul class="cross-list">
        <li>有些標題可能有空白</li>
        <li>有些標題可能有特殊符號</li>
        <li>有些人甚至可能沒有輸入標題</li>
      </ul>
      <p class="detail-paragraph">
        所以我們會寫一個小函式，把標題整理成比較安全的檔名。概念大概是：
      </p>
      <ul class="check-list">
        <li>把空白換成底線</li>
        <li>移除不適合的符號</li>
        <li>如果標題是空的，就給它一個預設名稱</li>
      </ul>

      <p class="detail-paragraph">
        這一段你不用現在就完全背起來。<strong>重點是先記住一件事</strong>：
      </p>

      <div class="highlight-card highlight-card--strong">
        <p class="highlight-text">
          <strong>使用者輸入的東西，不要直接拿來操作檔案。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        因為使用者輸入的內容可能不穩定，而 AI 也是根據使用者輸入來工作。所以我們要<strong>先幫工具設計一些保護措施</strong>。
      </p>

      <!-- Concept: try / except -->
      <div class="concept-header">
        <p class="concept-label">概念 14</p>
        <h2 class="concept-title">try / except：出錯時不要直接崩潰</h2>
      </div>

      <p class="detail-paragraph">
        有些錯誤是我們<strong>預料得到的</strong>。例如：
      </p>
      <ul class="cross-list">
        <li>檔名不對</li>
        <li>路徑不安全</li>
        <li>檔案不存在</li>
        <li>輸入格式不符合要求</li>
      </ul>
      <p class="detail-paragraph">
        這時候可以用 <code>try / except</code>。你可以先把它想成：<strong>先試著做做看。如果出錯，不要整個程式停掉，而是回覆一個比較清楚的訊息。</strong>例如：
      </p>

      <pre class="code-block code-block--py"><code>try:
    number = int("abc")
except ValueError:
    print("這不是一個數字")</code></pre>

      <p class="detail-paragraph">
        這段程式裡，Python 會試著把 <code>"abc"</code> 變成數字。但 <code>"abc"</code> 不是數字，所以會出錯。<strong>如果沒有 <code>try / except</code>，程式可能就直接停掉。</strong>有了 <code>try / except</code>，我們就可以讓它顯示：「這不是一個數字」。
      </p>
      <p class="detail-paragraph">
        在 MCP Server 裡，這個觀念很重要。<strong>因為我們不希望學生看到一大堆看不懂的紅字</strong>。我們希望工具可以回覆人看得懂的訊息。例如：
      </p>

      <ul class="check-list">
        <li>請只輸入檔名，不要輸入資料夾路徑</li>
        <li>找不到這份筆記，請先用 <code>list_notes</code> 確認檔名</li>
      </ul>

      <!-- Concept: type hints -->
      <div class="concept-header">
        <p class="concept-label">概念 15</p>
        <h2 class="concept-title">type hints：先看懂 filename: str -&gt; str 就好</h2>
      </div>

      <p class="detail-paragraph">
        之後你會看到這種寫法：
      </p>

      <pre class="code-block code-block--py"><code>def read_note(filename: str) -&gt; str:</code></pre>

      <p class="detail-paragraph">
        這看起來可能有點複雜，但<strong>可以拆開看</strong>：
      </p>

      <div class="hint-grid">
        <article class="hint-card">
          <p class="hint-card__name"><code>filename: str</code></p>
          <p class="hint-card__desc">
            <code>filename</code> 這個參數<strong>預期是一段文字</strong>。
          </p>
        </article>
        <article class="hint-card">
          <p class="hint-card__name"><code>-&gt; str</code></p>
          <p class="hint-card__desc">
            這個函式最後會<strong>回傳一段文字</strong>。
          </p>
        </article>
      </div>

      <p class="detail-paragraph">所以這整行可以先讀成：</p>
      <ul class="check-list">
        <li>這是一個叫做 <code>read_note</code> 的函式</li>
        <li>它需要一個<strong>文字形式的檔名</strong></li>
        <li>它最後會<strong>回傳一段文字</strong></li>
      </ul>

      <div class="callout-card">
        <p class="callout-text">
          剛開始不用把 type hints 當成考試內容，<strong>先把它當成提示就好</strong>。它是在提醒你，也是在提醒之後使用這個工具的人：這個函式需要什麼，會回傳什麼。
        </p>
      </div>

      <!-- Concept: docstring -->
      <div class="concept-header">
        <p class="concept-label">概念 16</p>
        <h2 class="concept-title">docstring：寫給人看的工具說明</h2>
      </div>

      <p class="detail-paragraph">
        之後你會看到這種寫法：
      </p>

      <pre class="code-block code-block--py"><code>def list_notes() -&gt; str:
    """列出 class_notes 資料夾中的課堂筆記。"""</code></pre>

      <p class="detail-paragraph">
        三個引號包起來的文字，常常叫做 <strong>docstring</strong>。你可以把它想成：
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          <strong>這個函式的簡短說明書。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        它<strong>不是主要的執行指令</strong>，而是用來說明這個函式在做什麼。
      </p>

      <div class="callout-card">
        <p class="callout-text">
          在 MCP tool 裡，docstring 特別重要。因為它<strong>不只讓人看得懂，也可以幫助 AI 理解這個 tool 的用途</strong>。
        </p>
      </div>

      <!-- Concept: decorator -->
      <div class="concept-header">
        <p class="concept-label">概念 17</p>
        <h2 class="concept-title">decorator：先把 @mcp.tool() 看成貼標籤</h2>
      </div>

      <p class="detail-paragraph">
        接下來這個符號，很多人第一次看到會緊張：
      </p>

      <pre class="code-block code-block--py"><code>@mcp.tool()
def hello() -&gt; str:
    return "MCP Server 已經啟動。"</code></pre>

      <p class="detail-paragraph">
        寫在函式上面的 <code>@mcp.tool()</code> 叫做 <strong>decorator</strong>。名字聽起來有點難，但在這裡可以先把它想成：
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          <strong>貼標籤。</strong>
        </p>
      </div>

      <p class="detail-paragraph">它的意思大概是：</p>
      <ul class="check-list">
        <li>下面這個函式不只是一般 Python 函式</li>
        <li>它要被<strong>登記成 MCP tool</strong></li>
        <li>之後 Gemini CLI 可以呼叫它</li>
      </ul>

      <h3 class="sub-heading">有貼 vs 沒貼的差別</h3>
      <p class="detail-paragraph">
        如果只有：
      </p>

      <pre class="code-block code-block--py"><code>def hello() -&gt; str:
    return "MCP Server 已經啟動。"</code></pre>

      <p class="detail-paragraph">
        這只是一個<strong>普通的 Python 函式</strong>。但如果加上：
      </p>

      <pre class="code-block code-block--py"><code>@mcp.tool()
def hello() -&gt; str:
    return "MCP Server 已經啟動。"</code></pre>

      <p class="detail-paragraph">
        它就會變成 <strong>MCP Server 對外提供的工具</strong>。
      </p>

      <div class="callout-card">
        <p class="callout-text">
          這裡不用理解 decorator 的底層原理。<strong>先記住這句話就好：<code>@mcp.tool()</code> 是把下面的函式登記成 tool。</strong>
        </p>
      </div>

      <!-- Concept: __main__ -->
      <div class="concept-header">
        <p class="concept-label">概念 18</p>
        <h2 class="concept-title">if __name__ == "__main__"：直接執行這個檔案時才啟動</h2>
      </div>

      <p class="detail-paragraph">
        之後你會看到 MCP Server 的最後通常有這段：
      </p>

      <pre class="code-block code-block--py"><code>if __name__ == "__main__":
    mcp.run()</code></pre>

      <p class="detail-paragraph">
        這段對初學者來說有點抽象。<strong>你現在可以先不用完全理解它的所有細節</strong>。先把它看成：
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          <strong>當我們直接執行這個 <code>server.py</code> 時，就啟動 MCP Server。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        其中真正讓 MCP Server 跑起來的是：
      </p>

      <pre class="code-block code-block--py"><code>mcp.run()</code></pre>

      <p class="detail-paragraph">
        也就是說，這段程式的意思可以先簡化成：<strong>如果這個檔案是被直接執行的，就讓 MCP Server 開始運作</strong>。之後 Gemini CLI 會透過設定檔啟動這個 Python 檔案，所以這一段會出現在我們的 MCP Server 程式裡。
      </p>

    </div>
  </section>
</BaseLayout>

<style>
  .python-basics {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  /* Header */
  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  .detail-subtitle strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Section headings */
  .detail-heading {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-heading);
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-paragraph {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    margin-bottom: var(--space-4);
  }

  .detail-paragraph strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Sub-heading */
  .sub-heading {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-heading);
    margin-top: var(--space-8);
    margin-bottom: var(--space-3);
    padding-left: var(--space-3);
    border-left: 3px solid var(--color-accent);
  }

  /* Concept header */
  .concept-header {
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }

  .concept-label {
    display: inline-block;
    margin: 0 0 var(--space-2);
    padding: var(--space-1) var(--space-4);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.08em;
    color: #fff;
    background-color: var(--color-accent);
    border-radius: var(--border-radius);
  }

  .concept-title {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-heading);
    margin: 0;
    line-height: 1.4;
  }

  /* Inline code */
  :global(.detail-paragraph) code,
  :global(.detail-subtitle) code,
  :global(.check-list) code,
  :global(.cross-list) code,
  :global(.callout-text) code,
  :global(.highlight-text) code,
  :global(.hint-card__desc) code {
    padding: 0.1em 0.4em;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 0.9em;
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
    border-radius: var(--border-radius);
  }

  /* Code block (Python) */
  .code-block {
    margin: var(--space-3) 0;
    padding: var(--space-4) var(--space-8);
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: var(--text-sm);
    line-height: 1.7;
    background-color: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    overflow-x: auto;
    white-space: pre;
    color: var(--color-heading);
  }

  .code-block code {
    padding: 0;
    background: none;
    color: var(--color-heading);
    font-size: inherit;
  }

  /* Check list */
  .check-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .check-list li {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .check-list li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  .check-list li strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Cross list */
  .cross-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .cross-list li {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.9;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .cross-list li::before {
    content: '\2717';
    position: absolute;
    left: 0;
    color: var(--color-text-secondary);
    font-weight: 700;
  }

  /* Callout card */
  .callout-card {
    margin: var(--space-6) 0 var(--space-8);
    padding: var(--space-4) var(--space-8);
    border-left: 4px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .callout-text {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.9;
    margin: 0;
  }

  .callout-text strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Highlight card */
  .highlight-card {
    margin: var(--space-4) 0 var(--space-6);
    padding: var(--space-3) var(--space-8);
    border: 1px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-tag-bg);
  }

  .highlight-card--strong {
    padding: var(--space-4) var(--space-8);
    border-width: 2px;
  }

  .highlight-text {
    font-size: var(--text-base);
    color: var(--color-heading);
    line-height: 1.8;
    margin: 0;
    text-align: center;
  }

  .highlight-text strong {
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Hint grid (type hints) */
  .hint-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
    margin: var(--space-4) 0 var(--space-6);
  }

  .hint-card {
    padding: var(--space-4) var(--space-6);
    border: 1px solid var(--color-border);
    border-left: 3px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .hint-card__name {
    margin: 0 0 var(--space-2);
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-heading);
  }

  .hint-card__name code {
    padding: 0.1em 0.4em;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 1em;
    background-color: var(--color-bg);
    color: var(--color-accent);
    border-radius: var(--border-radius);
  }

  .hint-card__desc {
    margin: 0;
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  .hint-card__desc strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Mobile */
  @media (max-width: 720px) {
    .hint-grid {
      grid-template-columns: 1fr;
    }

    .callout-card,
    .highlight-card {
      padding: var(--space-4) var(--space-6);
    }

    .highlight-card--strong {
      padding: var(--space-5) var(--space-6);
    }

    .code-block {
      padding: var(--space-4) var(--space-5);
      font-size: var(--text-xs);
    }

    .hint-card {
      padding: var(--space-4) var(--space-5);
    }
  }
</style>
