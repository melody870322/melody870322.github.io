---
import BaseLayout from '../../../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Python 基礎：為自製 MCP Server 做準備 — AI Agent 拼圖"
  description="先補上自製 MCP Server 會用到的 Python 基礎，為下一步動手實作打底。"
  lang="zh"
>
  <section class="section">
    <div class="container ai-agent-python-prep">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">Python 基礎：為自製 MCP Server 做準備</h1>
        <p class="detail-subtitle">
          頁面建置中...
        </p>
      </header>

    </div>
  </section>
</BaseLayout>

<style>
  .ai-agent-python-prep {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }
</style>
