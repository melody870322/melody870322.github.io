---
import BaseLayout from '../../../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="自製 MCP Server — AI Agent 拼圖"
  description="從零開始打造一個屬於自己的 MCP Server，讓 AI Agent 接上你專屬的工具。"
  lang="zh"
>
  <section class="section">
    <div class="container ai-agent-custom-server">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">自製 MCP Server</h1>
        <p class="detail-subtitle">
          頁面建置中...
        </p>
      </header>

    </div>
  </section>
</BaseLayout>

<style>
  .ai-agent-custom-server {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }
</style>
