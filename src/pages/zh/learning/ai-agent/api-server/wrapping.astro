---
import BaseLayout from '../../../../../layouts/BaseLayout.astro';

const fullServerPy = String.raw`from typing import Any

import httpx
from mcp.server.fastmcp import FastMCP

mcp = FastMCP("Weather API Assistant")

OPEN_METEO_API_URL = "https://api.open-meteo.com/v1/forecast"

CITY_LOCATIONS = {
    "台北": {
        "latitude": 25.0330,
        "longitude": 121.5654,
    },
    "台中": {
        "latitude": 24.1477,
        "longitude": 120.6736,
    },
    "高雄": {
        "latitude": 22.6273,
        "longitude": 120.3014,
    },
    "淡水": {
        "latitude": 25.1676,
        "longitude": 121.4458,
    },
}


@mcp.tool()
def hello() -> str:
    """確認天氣 API MCP Server 已經啟動。"""
    return "天氣 API MCP Server 已經啟動。"


@mcp.tool()
def list_supported_cities() -> str:
    """列出目前可以查詢天氣的城市。"""
    cities = "、".join(CITY_LOCATIONS.keys())
    return f"目前可以查詢的城市有：{cities}"


@mcp.tool()
def get_current_weather(city: str) -> str:
    """查詢指定城市目前的天氣資料。"""
    city = city.strip()

    if city not in CITY_LOCATIONS:
        supported_cities = "、".join(CITY_LOCATIONS.keys())
        return (
            f"目前不支援「{city}」。"
            f"請改查以下城市：{supported_cities}"
        )

    location = CITY_LOCATIONS[city]

    params = {
        "latitude": location["latitude"],
        "longitude": location["longitude"],
        "current": "temperature_2m,wind_speed_10m",
        "timezone": "Asia/Taipei",
    }

    try:
        with httpx.Client(timeout=10.0) as client:
            response = client.get(OPEN_METEO_API_URL, params=params)
            response.raise_for_status()
            data: dict[str, Any] = response.json()
    except httpx.TimeoutException:
        return "查詢天氣時等太久沒有回應，請稍後再試。"
    except httpx.HTTPStatusError as error:
        return f"天氣 API 回傳錯誤，狀態碼：{error.response.status_code}"
    except httpx.RequestError:
        return "無法連線到天氣 API，請確認網路連線是否正常。"

    current = data.get("current", {})

    temperature = current.get("temperature_2m")
    wind_speed = current.get("wind_speed_10m")
    update_time = current.get("time")

    if temperature is None or wind_speed is None:
        return "天氣 API 有回傳資料，但缺少目前溫度或風速欄位。"

    return (
        f"{city}目前天氣資料：\n"
        f"- 更新時間：{update_time}\n"
        f"- 氣溫：約 {temperature} °C\n"
        f"- 風速：約 {wind_speed} km/h\n\n"
        f"提醒：這是由外部天氣 API 回傳的即時資料，"
        f"實際天氣仍可能因地點和時間有所差異。"
    )


if __name__ == "__main__":
    mcp.run()
`;
---

<BaseLayout
  title="工具封裝：將外部服務整合為 MCP Tool — AI Agent 拼圖"
  description="把外部 API 包成 AI 可以使用的 MCP Tool。AI 不需要看懂整個 API 文件，只要知道工具名稱與輸入；MCP Server 在背後處理城市檢查、緯經度轉換、呼叫 API、整理回傳資料。用 Open-Meteo 公開天氣 API 一步步示範。"
  lang="zh"
>
  <section class="section">
    <div class="container ai-agent-wrapping">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">讓 AI 不用看懂 API：我們替它做一個工具</h1>
        <p class="detail-subtitle">
          前面我們已經知道 MCP Server 可以當成 AI 和外部資料之間的橋。AI 不是直接衝進外部服務裡亂拿資料，而是透過我們寫好的 MCP Server，使用幾個被設計過的 tools 來完成任務。這一頁要做的事情就是 ——<strong>把一個外部 API，包成 AI 可以使用的 MCP Tool</strong>。
        </p>
      </header>

      <p class="detail-paragraph">
        原本程式可能要自己處理網址、參數、回傳資料、錯誤訊息。現在我們把這些細節整理好，讓 AI 只需要看到一個比較簡單的工具，例如：
      </p>

      <blockquote class="prompt-example">
        查詢指定城市目前的天氣。
      </blockquote>

      <p class="detail-paragraph">
        這樣 AI 不需要理解整個 API 文件，也不需要自己組網址。<strong>它只要知道：如果需要查天氣，就呼叫 <code>get_current_weather</code> 這個 tool。</strong>
      </p>

      <!-- Section: API vs MCP Tool -->
      <h2 class="detail-heading">先講結論：API 是原料，MCP Tool 是給 AI 用的工具</h2>
      <p class="detail-paragraph">
        API 和 MCP Tool 可以這樣分：
      </p>

      <ul class="role-list">
        <li class="role-item">
          <span class="role-item__label">API</span>
          <span class="role-item__desc">外部服務原本提供的入口</span>
        </li>
        <li class="role-item">
          <span class="role-item__label">MCP Tool</span>
          <span class="role-item__desc">我們幫 AI 包好的使用方式</span>
        </li>
      </ul>

      <p class="detail-paragraph">
        舉例來說，如果外部天氣服務提供一個 API，它可能需要這些東西：
      </p>

      <ul class="check-list">
        <li>緯度</li>
        <li>經度</li>
        <li>要查哪些天氣欄位</li>
        <li>回傳格式</li>
        <li>時區設定</li>
      </ul>

      <p class="detail-paragraph">
        這些對初學者來說看起來會有點亂，對 AI 來說也不一定是最安全、最穩定的使用方式。所以我們可以把它包成一個 MCP Tool：
      </p>

      <div class="tool-box">
        <p class="tool-box__label">MCP Tool</p>
        <p class="tool-box__name"><code>get_current_weather(city)</code></p>
        <p class="tool-box__desc">AI 只要給城市名稱，背後的細節由 MCP Server 處理</p>
      </div>

      <p class="detail-paragraph">AI 只要給城市名稱，MCP Server 就負責：</p>

      <div class="flow-card flow-card--tall">
        <div class="flow-step">檢查城市名稱</div>
        <div class="flow-arrow">↓</div>
        <div class="flow-step">轉成緯度與經度</div>
        <div class="flow-arrow">↓</div>
        <div class="flow-step flow-step--accent">呼叫外部天氣 API</div>
        <div class="flow-arrow">↓</div>
        <div class="flow-step">整理 API 回傳資料</div>
        <div class="flow-arrow">↓</div>
        <div class="flow-step">回傳簡單好讀的結果</div>
      </div>

      <div class="highlight-card">
        <p class="highlight-text">
          這就是「把 API 包成 MCP Tool」的核心：<br/>
          不是把 API 原封不動丟給 AI，<br/>
          而是把它<strong>整理成 AI 容易用、人也容易檢查的工具</strong>。
        </p>
      </div>

      <!-- Section: Use no-key API -->
      <h2 class="detail-heading">這次先用「不用 API Key 的服務」來練習</h2>
      <p class="detail-paragraph">
        這裡先使用 <strong>Open-Meteo</strong> 這類不需要 API Key 的公開天氣 API 來示範。Open-Meteo 的官方說明中提到，它提供非商業用途的免費天氣 API，而且不需要 API Key。
      </p>

      <div class="callout-card">
        <p class="callout-text">
          這樣做的好處是：我們不用一開始就卡在<strong>註冊帳號、申請金鑰、貼錯 API Key</strong> 等問題，可以先把「API 怎麼被包成 tool」這件事學清楚。
        </p>
      </div>

      <p class="detail-paragraph">
        等這個流程懂了，以後要換成需要 API Key 的服務，也只是多一步 ——<strong>從環境變數讀取 API Key</strong>。這個概念已經在前一頁學過，所以這裡先把重點放在 tool 的包裝。
      </p>

      <!-- Section: Design tool first -->
      <h2 class="detail-heading">不要急著寫程式，先設計工具長什麼樣</h2>
      <p class="detail-paragraph">
        很多人第一次寫 MCP Tool，會一開始就打開 <code>server.py</code> 開始寫。但比較好的做法是：
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          <strong>先想清楚這個 tool 要給 AI 怎麼用。</strong>
        </p>
      </div>

      <p class="detail-paragraph">我們這次要設計的工具是：</p>

      <div class="tool-box">
        <p class="tool-box__label">本頁主角</p>
        <p class="tool-box__name"><code>get_current_weather</code></p>
        <p class="tool-box__desc">查詢指定城市目前的天氣資料</p>
      </div>

      <h3 class="sub-heading">輸入</h3>
      <ul class="check-list">
        <li><code>city</code>：城市名稱</li>
      </ul>

      <h3 class="sub-heading">回傳的內容</h3>
      <ul class="check-list">
        <li>城市</li>
        <li>查詢時間</li>
        <li>目前氣溫</li>
        <li>目前風速</li>
        <li>簡短提醒</li>
      </ul>

      <h3 class="sub-heading">它不做的事</h3>
      <ul class="cross-list">
        <li>不查<strong>學生個資</strong></li>
        <li>不<strong>寫入</strong>資料</li>
        <li>不<strong>刪除</strong>資料</li>
        <li>不讓 AI 自己輸入<strong>任意 API URL</strong></li>
        <li>不讓 AI 自己決定要打<strong>哪個外部網站</strong></li>
      </ul>

      <div class="highlight-card">
        <p class="highlight-text">
          我們不是做一個「什麼都能查」的大工具，<br/>
          而是做一個<strong>「查指定城市天氣」的小工具</strong>。
        </p>
      </div>

      <p class="detail-paragraph">
        小工具<strong>比較好教、比較好測，也比較安全</strong>。
      </p>

      <!-- ============ Part 2: 實際測試 ============ -->
      <div class="part-divider">
        <span class="part-label">PART 2</span>
        <span class="part-text">實際測試</span>
      </div>

      <!-- STEP 1 -->
      <div class="case-header">
        <p class="case-label">STEP 1</p>
        <h2 class="case-title">建立專案資料夾</h2>
      </div>

      <p class="detail-paragraph">
        建立一個新的專案資料夾，例如：
      </p>

      <pre class="code-block code-block--tree" is:raw><code>my-weather-mcp-server/
├─ server.py
└─ .gemini/
   └─ settings.json</code></pre>

      <ul class="check-list">
        <li><code>server.py</code> 是我們的 MCP Server 程式</li>
        <li><code>.gemini/settings.json</code> 是讓 Gemini CLI 知道<strong>怎麼啟動這個 MCP Server</strong></li>
      </ul>

      <div class="callout-card">
        <p class="callout-text">
          這裡<strong>先不用建立 .env</strong>，因為這次使用的天氣 API 不需要 API Key。如果之後換成需要金鑰的服務，再加入 .env 和 .gitignore。
        </p>
      </div>

      <!-- STEP 2 -->
      <div class="case-header">
        <p class="case-label">STEP 2</p>
        <h2 class="case-title">安裝需要的套件</h2>
      </div>

      <p class="detail-paragraph">
        如果你前面已經用過 uv，可以在專案資料夾裡執行：
      </p>

      <pre class="code-block" is:raw><code class="lang-shell">uv init
uv add "mcp[cli]" httpx</code></pre>

      <p class="detail-paragraph">
        這裡多安裝一個 <code>httpx</code>。<strong>httpx 是 Python 裡常用來發送網路請求的套件</strong>，簡單說，就是讓我們的 Python 程式可以去呼叫外部 API。這時候，整個流程會變成：
      </p>

      <div class="flow-card flow-card--tall">
        <div class="flow-step">Gemini CLI</div>
        <div class="flow-arrow">↓</div>
        <div class="flow-step flow-step--accent">MCP Tool</div>
        <div class="flow-arrow">↓</div>
        <div class="flow-step">Python 使用 httpx 呼叫天氣 API</div>
        <div class="flow-arrow">↓</div>
        <div class="flow-step">API 回傳資料</div>
        <div class="flow-arrow">↓</div>
        <div class="flow-step flow-step--accent">MCP Tool 整理後交給 Gemini</div>
      </div>

      <!-- STEP 3 -->
      <div class="case-header">
        <p class="case-label">STEP 3</p>
        <h2 class="case-title">先寫一個可以查天氣的 MCP Server</h2>
      </div>

      <p class="detail-paragraph">
        請打開 <code>server.py</code>，放入下面這段程式：
      </p>

      <details class="details-card details-card--code">
        <summary class="details-card__summary">
          <span class="details-card__icon" aria-hidden="true">▸</span>
          <span class="details-card__title">完整版 server.py</span>
          <span class="details-card__hint">點擊展開程式碼</span>
        </summary>
        <div class="details-card__body details-card__body--flush">
          <pre class="code-block code-block--in-details"><code class="lang-python">{fullServerPy}</code></pre>
        </div>
      </details>

      <p class="detail-paragraph">
        這段程式就是一個很基本的 API 型 MCP Server。它有三個 tools：
      </p>

      <ul class="check-list">
        <li><code>hello</code> — 用來<strong>確認 server 有沒有啟動</strong></li>
        <li><code>list_supported_cities</code> — 用來<strong>列出目前支援哪些城市</strong></li>
        <li><code>get_current_weather</code> — 才是<strong>真正呼叫外部 API 的工具</strong></li>
      </ul>

      <div class="callout-card">
        <p class="callout-text">
          MCP 的 Python SDK 可以用 <code>FastMCP</code> 建立 server，也可以用 Python 函式、型別提示和 docstring 產生工具定義；這就是為什麼程式會使用 <code>@mcp.tool()</code> 把一般函式變成 MCP Tool。
        </p>
      </div>

      <p class="detail-paragraph">
        想更深入了解程式細節，可以展開下面三個區塊：
      </p>

      <details class="details-card">
        <summary class="details-card__summary">
          <span class="details-card__icon" aria-hidden="true">▸</span>
          <span class="details-card__title"> 這段程式在做什麼</span>
          <span class="details-card__hint">點擊展開</span>
        </summary>
        <div class="details-card__body">
          <p class="detail-paragraph">先看這一段：</p>
          <pre class="code-block" is:raw><code class="lang-python">mcp = FastMCP("Weather API Assistant")</code></pre>
          <p class="detail-paragraph">
            這是在建立一個 MCP Server，名字叫做 <strong>Weather API Assistant</strong>。這個名字比較像是告訴系統：「這個 server 是一個天氣 API 助理」。
          </p>

          <p class="detail-paragraph">再看這一段：</p>
          <pre class="code-block" is:raw><code class="lang-python">CITY_LOCATIONS = {
    "台北": {
        "latitude": 25.0330,
        "longitude": 121.5654,
    },
    ...
}</code></pre>
          <p class="detail-paragraph">
            這裡我們<strong>先把可以查詢的城市寫死</strong>。為什麼不讓 AI 自己輸入任何城市？<strong>因為我們是初學者</strong>。一開始範圍小一點，會比較好觀察，也比較不容易出錯。
          </p>

          <p class="detail-paragraph">如果學生輸入「台北」，程式就知道台北的緯度和經度。如果學生輸入「火星」，程式就會回覆：</p>
          <blockquote class="prompt-example">
            目前不支援「火星」。
          </blockquote>

          <div class="callout-card">
            <p class="callout-text">
              這就是<strong>輸入限制</strong>。輸入限制不是故意刁難使用者，而是<strong>讓工具更穩</strong>。
            </p>
          </div>
        </div>
      </details>

      <details class="details-card">
        <summary class="details-card__summary">
          <span class="details-card__icon" aria-hidden="true">▸</span>
          <span class="details-card__title"> 真正呼叫 API 的地方</span>
          <span class="details-card__hint">點擊展開</span>
        </summary>
        <div class="details-card__body">
          <p class="detail-paragraph">
            這一段是整個工具最重要的地方：
          </p>
          <pre class="code-block" is:raw><code class="lang-python">with httpx.Client(timeout=10.0) as client:
    response = client.get(OPEN_METEO_API_URL, params=params)
    response.raise_for_status()
    data: dict[str, Any] = response.json()</code></pre>

          <p class="detail-paragraph">
            可以把它想成：<strong>Python 幫我們去敲外部 API 的門</strong>。
          </p>

          <ul class="check-list">
            <li><code>client.get(...)</code> 是<strong>送出請求</strong></li>
            <li><code>params=params</code> 是把<strong>查詢條件帶給 API</strong></li>
            <li><code>response.raise_for_status()</code> 是<strong>檢查對方有沒有回傳錯誤</strong></li>
            <li><code>response.json()</code> 是把 API 回傳的 JSON 資料<strong>轉成 Python 比較好處理的格式</strong></li>
          </ul>

          <div class="highlight-card">
            <p class="highlight-text">
              API 回傳的資料通常不是漂亮文章，<br/>
              它<strong>比較像一包資料</strong>。
            </p>
          </div>

          <p class="detail-paragraph">
            所以 MCP Tool 要做的事情，<strong>就是先把這包資料整理過，再交給 AI</strong>。
          </p>
        </div>
      </details>

      <details class="details-card">
        <summary class="details-card__summary">
          <span class="details-card__icon" aria-hidden="true">▸</span>
          <span class="details-card__title"> 為什麼不要把整包 API 結果都丟給 AI</span>
          <span class="details-card__hint">點擊展開</span>
        </summary>
        <div class="details-card__body">
          <p class="detail-paragraph">
            有些人會想：既然 API 回傳很多資料，那我全部回傳給 AI 不就好了？<strong>不一定</strong>。這樣可能會有幾個問題：
          </p>

          <ul class="cross-list">
            <li>資料<strong>太長</strong></li>
            <li>AI <strong>抓不到重點</strong></li>
            <li><strong>使用者看不懂</strong></li>
            <li>不需要的欄位<strong>也被暴露出來</strong></li>
            <li>除錯時<strong>很難知道問題在哪裡</strong></li>
          </ul>

          <p class="detail-paragraph">
            所以我們在 tool 裡<strong>只挑出這次需要的欄位</strong>：
          </p>

          <pre class="code-block" is:raw><code class="lang-python">temperature = current.get("temperature_2m")
wind_speed = current.get("wind_speed_10m")
update_time = current.get("time")</code></pre>

          <p class="detail-paragraph">然後整理成比較好讀的文字：</p>

          <pre class="code-block" is:raw><code class="lang-python">return (
    f"{city}目前天氣資料：\n"
    f"- 更新時間：{update_time}\n"
    f"- 氣溫：約 {temperature} °C\n"
    f"- 風速：約 {wind_speed} km/h\n\n"
    f"提醒：這是由外部天氣 API 回傳的即時資料，"
    f"實際天氣仍可能因地點和時間有所差異。"
)</code></pre>

          <div class="highlight-card">
            <p class="highlight-text">
              這就是 MCP Tool 很重要的工作：<br/>
              <strong>不是只把 API 資料搬過來，而是先整理成 AI 和人都看得懂的格式。</strong>
            </p>
          </div>
        </div>
      </details>

      <!-- STEP 4 -->
      <div class="case-header">
        <p class="case-label">STEP 4</p>
        <h2 class="case-title">把 MCP Server 接到 Gemini CLI</h2>
      </div>

      <p class="detail-paragraph">
        在專案裡的 <code>.gemini/settings.json</code> 內容可以先寫成這樣：
      </p>

      <pre class="code-block" is:raw><code class="lang-json">{
  "mcpServers": {
    "weather-api": {
      "command": "uv",
      "args": ["run", "python", "server.py"],
      "cwd": "請改成你的專案資料夾完整路徑",
      "timeout": 30000,
      "trust": false,
      "includeTools": [
        "hello",
        "list_supported_cities",
        "get_current_weather"
      ]
    }
  }
}</code></pre>

      <p class="detail-paragraph">
        請記得把 <code>cwd</code> 改成你自己的專案資料夾完整路徑。另外，這裡還是使用 <code>includeTools</code>。原因很簡單：
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          就算這個 MCP Server 是自己寫的，<br/>
          <strong>也不要養成「全部開放」的習慣。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        現在只需要開放三個工具，就只開這三個。
      </p>

      <!-- STEP 5 -->
      <div class="case-header">
        <p class="case-label">STEP 5</p>
        <h2 class="case-title">測試工具有沒有成功</h2>
      </div>

      <p class="detail-paragraph">
        在 VS Code 終端機中啟動 Gemini CLI：
      </p>

      <pre class="code-block" is:raw><code class="lang-shell">gemini</code></pre>

      <p class="detail-paragraph">進入 Gemini CLI 後，先輸入：</p>

      <pre class="code-block" is:raw><code class="lang-shell">/mcp list</code></pre>

      <p class="detail-paragraph">
        如果看到 <code>weather-api</code> 成功連上，代表 MCP Server 已經被 Gemini CLI 找到了。接著可以試試看：
      </p>

      <blockquote class="prompt-example">
        請使用 weather-api 的 hello 工具。
      </blockquote>

      <p class="detail-paragraph">如果回覆「天氣 API MCP Server 已經啟動。」代表第一步成功。再試：</p>

      <blockquote class="prompt-example">
        請列出目前可以查詢的城市。
      </blockquote>

      <p class="detail-paragraph">接著再試：</p>

      <blockquote class="prompt-example">
        請查詢台北目前的天氣，並用適合大學生閱讀的方式整理。
      </blockquote>

      <div class="highlight-card">
        <p class="highlight-text">
          這時候 AI <strong>不是憑空回答</strong>。<br/>
          它是真的<strong>透過 MCP Tool 去查外部 API</strong>。
        </p>
      </div>

      <!-- ============ Part 3: 安全與進階 ============ -->

      <!-- Section: Three layers of protection -->
      <h2 class="detail-heading">這個工具其實做了三層保護</h2>
      <p class="detail-paragraph">
        這個範例看起來只是查天氣，但它其實示範了很重要的<strong>安全觀念</strong>。
      </p>

      <ul class="role-list">
        <li class="role-item">
          <span class="role-item__label">第一層</span>
          <span class="role-item__desc"><strong>只支援指定城市。</strong>AI 不能自己亂輸入任意地點，也不能自己改 API 查詢範圍。</span>
        </li>
        <li class="role-item">
          <span class="role-item__label">第二層</span>
          <span class="role-item__desc"><strong>只查目前天氣。</strong>這個 tool 不是「天氣資料萬能工具」，它只做一件事情。</span>
        </li>
        <li class="role-item">
          <span class="role-item__label">第三層</span>
          <span class="role-item__desc"><strong>只回傳需要的欄位。</strong>API 回來的資料不會整包丟給 AI，而是先整理成簡單文字。</span>
        </li>
      </ul>

      <p class="detail-paragraph">
        這三件事加起來，就是這裡 MCP Tool 想傳達的<strong>重要設計精神</strong>：
      </p>

      <ul class="check-list">
        <li>功能<strong>小一點</strong></li>
        <li>範圍<strong>清楚一點</strong></li>
        <li>輸出<strong>乾淨一點</strong></li>
      </ul>

      <!-- Section: API Key version -->
      <h2 class="detail-heading">如果之後 API 需要 API Key，要怎麼改</h2>
      <p class="detail-paragraph">
        這裡使用的 API 不需要 API Key。但很多外部服務是需要 API Key 的。如果之後換成需要金鑰的 API，<strong>請不要這樣寫</strong>：
      </p>

      <pre class="code-block code-block--bad" is:raw><code class="lang-python">api_key = "你的真正 API Key"</code></pre>

      <p class="detail-paragraph">
        這樣會把鑰匙直接寫進程式裡，<strong>不安全</strong>。比較好的方式是：
      </p>

      <pre class="code-block" is:raw><code class="lang-python">import os

api_key = os.getenv("WEATHER_API_KEY")

if not api_key:
    return "找不到 WEATHER_API_KEY，請先設定環境變數。"</code></pre>

      <p class="detail-paragraph">
        如果 API 需要把金鑰放在 header 裡，可能會像這樣：
      </p>

      <pre class="code-block" is:raw><code class="lang-python">headers = {
    "Authorization": f"Bearer {api_key}"
}</code></pre>

      <p class="detail-paragraph">然後呼叫 API 時帶進去：</p>

      <pre class="code-block" is:raw><code class="lang-python">response = client.get(API_URL, params=params, headers=headers)</code></pre>

      <div class="callout-card">
        <p class="callout-text">
          這裡先不用急著實作。先記住一件事就好：<strong>API Key 是鑰匙，不要寫死在程式裡</strong>。詳細做法請看前一頁「<a href="/zh/learning/ai-agent/api-server/keys">API Key 與環境變數</a>」。
        </p>
      </div>

      <!-- Section: Common errors -->
      <h2 class="detail-heading">常見錯誤</h2>

      <details class="details-card">
        <summary class="details-card__summary">
          <span class="details-card__icon" aria-hidden="true">▸</span>
          <span class="details-card__title">1. Gemini 找不到 MCP Server</span>
          <span class="details-card__hint">點擊查看排查步驟</span>
        </summary>
        <div class="details-card__body">
          <p class="detail-paragraph">
            如果 <code>/mcp list</code> 裡沒有看到 <code>weather-api</code>，可以先檢查：
          </p>
          <ul class="check-list">
            <li><code>settings.json</code> 有沒有放在 <code>.gemini</code> 資料夾裡</li>
            <li><code>cwd</code> 有沒有改成<strong>正確路徑</strong></li>
            <li><code>server.py</code> 檔名有沒有打錯</li>
            <li><code>uv</code> 有沒有<strong>安裝成功</strong></li>
          </ul>

          <p class="detail-paragraph">可以先在終端機中測試：</p>
          <pre class="code-block" is:raw><code class="lang-shell">uv run python server.py</code></pre>

          <div class="callout-card">
            <p class="callout-text">
              如果這一步就失敗，<strong>代表問題還在 Python 或套件安裝，不是 Gemini CLI 的問題</strong>。
            </p>
          </div>
        </div>
      </details>

      <details class="details-card">
        <summary class="details-card__summary">
          <span class="details-card__icon" aria-hidden="true">▸</span>
          <span class="details-card__title">2. 找不到 httpx</span>
          <span class="details-card__hint">點擊查看排查步驟</span>
        </summary>
        <div class="details-card__body">
          <p class="detail-paragraph">如果出現類似：</p>
          <pre class="code-block" is:raw><code class="lang-shell">ModuleNotFoundError: No module named 'httpx'</code></pre>
          <p class="detail-paragraph">
            代表還沒有安裝 <code>httpx</code>。請回到專案資料夾，執行：
          </p>
          <pre class="code-block" is:raw><code class="lang-shell">uv add httpx</code></pre>
          <p class="detail-paragraph">
            然後<strong>重新啟動 Gemini CLI</strong>。
          </p>
        </div>
      </details>

      <details class="details-card">
        <summary class="details-card__summary">
          <span class="details-card__icon" aria-hidden="true">▸</span>
          <span class="details-card__title">3. API 等太久沒有回應</span>
          <span class="details-card__hint">點擊查看排查步驟</span>
        </summary>
        <div class="details-card__body">
          <p class="detail-paragraph">程式裡有這一行：</p>
          <pre class="code-block" is:raw><code class="lang-python">with httpx.Client(timeout=10.0) as client:</code></pre>
          <p class="detail-paragraph">
            意思是<strong>最多等 10 秒</strong>。如果外部 API 或網路太慢，就會回傳：
          </p>
          <blockquote class="prompt-example">
            查詢天氣時等太久沒有回應，請稍後再試。
          </blockquote>
          <p class="detail-paragraph">
            這種錯誤<strong>不一定表示寫錯</strong>。有時候只是網路、API 服務或外部連線暫時不穩。
          </p>

          <div class="callout-card">
            <p class="callout-text">
              所以工具要能<strong>把錯誤講清楚</strong>，不要讓使用者只看到一大串英文錯誤訊息。
            </p>
          </div>
        </div>
      </details>

    </div>
  </section>
</BaseLayout>

<style>
  .ai-agent-wrapping {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  /* Header */
  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  .detail-subtitle strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Section headings */
  .detail-heading {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-heading);
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-paragraph {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    margin-bottom: var(--space-4);
  }

  .detail-paragraph strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .detail-paragraph a {
    color: var(--color-accent);
    text-decoration: underline;
  }

  /* Sub heading */
  .sub-heading {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-heading);
    margin-top: var(--space-8);
    margin-bottom: var(--space-3);
    padding-left: var(--space-3);
    border-left: 3px solid var(--color-accent);
  }

  /* Part divider */
  .part-divider {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    margin: var(--space-16) 0 var(--space-8);
    padding: var(--space-4) 0;
    border-top: 2px solid var(--color-accent);
    border-bottom: 2px solid var(--color-accent);
  }

  .part-label {
    flex-shrink: 0;
    padding: var(--space-1) var(--space-4);
    font-size: var(--text-sm);
    font-weight: 700;
    letter-spacing: 0.1em;
    color: #fff;
    background-color: var(--color-accent);
    border-radius: var(--border-radius);
  }

  .part-text {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-heading);
  }

  /* Case header (STEP) */
  .case-label {
    display: inline-block;
    padding: var(--space-1) var(--space-4);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.08em;
    color: #fff;
    background-color: var(--color-accent);
    border-radius: var(--border-radius);
  }

  .case-title {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-heading);
    margin: 0;
    line-height: 2.4;
  }

  .case-header {
    margin-top: var(--space-12);
  }

  /* Inline code */
  :global(.detail-paragraph) code,
  :global(.detail-subtitle) code,
  :global(.check-list) code,
  :global(.cross-list) code,
  :global(.callout-text) code,
  :global(.highlight-text) code,
  :global(.role-item__desc) code {
    padding: 0.1em 0.4em;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 0.9em;
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
    border-radius: var(--border-radius);
  }

  /* Check list */
  .check-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .check-list li {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .check-list li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  .check-list li strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Cross list */
  .cross-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .cross-list li {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.9;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .cross-list li::before {
    content: '\2717';
    position: absolute;
    left: 0;
    color: var(--color-text-secondary);
    font-weight: 700;
  }

  .cross-list li strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Callout card */
  .callout-card {
    margin: var(--space-6) 0 var(--space-8);
    padding: var(--space-4) var(--space-8);
    border-left: 4px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .callout-text {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.9;
    margin: 0;
  }

  .callout-text strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  .callout-text a {
    color: var(--color-accent);
    text-decoration: underline;
  }

  /* Highlight card */
  .highlight-card {
    margin: var(--space-4) 0 var(--space-6);
    padding: var(--space-3) var(--space-8);
    border: 1px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-tag-bg);
  }

  .highlight-text {
    font-size: var(--text-base);
    color: var(--color-heading);
    line-height: 1.8;
    margin: 0;
    text-align: center;
  }

  .highlight-text strong {
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Flow card */
  .flow-card {
    margin: var(--space-6) 0;
    padding: var(--space-6) var(--space-4);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-2);
  }

  .flow-card--tall {
    padding: var(--space-7) var(--space-4);
  }

  .flow-step {
    display: inline-block;
    padding: var(--space-2);
    background-color: #fff;
    color: var(--color-heading);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    font-size: var(--text-sm);
    font-weight: 600;
    text-align: center;
  }

  .flow-step--accent {
    color: var(--color-accent);
    border-color: var(--color-accent);
  }

  .flow-arrow {
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    letter-spacing: 0.05em;
  }

  /* Role list (used for API/MCP Tool comparison and 3-layer protection) */
  .role-list {
    list-style: none;
    padding: 0;
    margin: var(--space-6) 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .role-item {
    display: flex;
    align-items: baseline;
    gap: var(--space-5);
    padding: var(--space-4) var(--space-6);
    border: 1px solid var(--color-border);
    border-left: 4px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg);
  }

  .role-item__label {
    flex-shrink: 0;
    min-width: 6rem;
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-accent);
  }

  .role-item__desc {
    flex: 1;
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  .role-item__desc strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Tool box */
  .tool-box {
    margin: var(--space-4) 0;
    padding: var(--space-2) var(--space-6);
    border: 1px dashed var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg);
  }

  .tool-box__label {
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.05em;
    color: var(--color-accent);
    margin-bottom: var(--space-2);
    text-align: center;
  }

  .tool-box__name {
    margin-bottom: var(--space-3);
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-heading);
    text-align: center;
  }

  .tool-box__name code {
    padding: 0.15em 0.5em;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 1em;
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
    border-radius: var(--border-radius);
  }

  .tool-box__desc {
    margin: 0;
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
    text-align: center;
  }

  /* Prompt example */
  .prompt-example {
    margin: var(--space-4) 0;
    padding: var(--space-4) var(--space-6);
    border-left: 3px solid var(--color-accent);
    background-color: var(--color-bg-secondary);
    border-radius: 0 var(--border-radius) var(--border-radius) 0;
    font-size: var(--text-sm);
    color: var(--color-heading);
    line-height: 1.9;
    font-style: italic;
  }

  /* Code block */
  .code-block {
    margin: var(--space-4) 0;
    padding: var(--space-4);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: #1e1e2e;
    color: #e4e4e7;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: var(--text-sm);
    line-height: 1.7;
    overflow-x: auto;
    white-space: pre;
    tab-size: 4;
  }

  .code-block code {
    background: transparent;
    color: inherit;
    padding: 0;
    font-family: inherit;
    font-size: inherit;
  }

  .code-block--tree {
    background-color: var(--color-bg-secondary);
    color: var(--color-heading);
    border-left: 3px solid var(--color-accent);
  }

  .code-block--in-details {
    margin: 0;
    border: none;
    border-radius: 0 0 var(--border-radius) var(--border-radius);
  }

  .code-block--bad {
    border: 1px solid #d97706;
    border-left: 4px solid #d97706;
    position: relative;
  }

  .code-block--bad::before {
    content: '✗ 不建議';
    position: absolute;
    top: 0;
    right: 0;
    padding: 1.11rem 0.6rem;
    font-size: var(--text-xs);
    font-weight: 700;
    color: #fff;
    background-color: #d97706;
    border-bottom-left-radius: var(--border-radius);
    border-top-right-radius: calc(var(--border-radius) - 1px);
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    letter-spacing: 0.05em;
  }

  /* Details card (collapsible) */
  .details-card {
    margin: var(--space-4) 0 var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg);
    overflow: hidden;
  }

  .details-card[open] {
    border-color: var(--color-accent);
  }

  .details-card__summary {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-4) var(--space-5);
    list-style: none;
    cursor: pointer;
    background-color: var(--color-bg-secondary);
    transition: background-color var(--transition);
    user-select: none;
  }

  .details-card__summary::-webkit-details-marker {
    display: none;
  }

  .details-card__summary:hover {
    background-color: var(--color-tag-bg);
  }

  .details-card[open] .details-card__summary {
    border-bottom: 1px solid var(--color-border);
  }

  .details-card__icon {
    flex-shrink: 0;
    display: inline-block;
    color: var(--color-accent);
    font-size: var(--text-base);
    transition: transform 0.2s ease;
    transform-origin: center;
  }

  .details-card[open] .details-card__icon {
    transform: rotate(90deg);
  }

  .details-card__title {
    flex: 1;
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-heading);
  }

  .details-card__hint {
    flex-shrink: 0;
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    font-weight: 500;
  }

  .details-card[open] .details-card__hint {
    display: none;
  }

  .details-card__body {
    padding: var(--space-3);
  }

  .details-card__body--flush {
    padding: 0;
  }

  /* Mobile */
  @media (max-width: 720px) {
    .case-title {
      font-size: var(--text-lg);
    }

    .part-divider {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-2);
    }

    .role-item {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-2);
    }

    .role-item__label {
      min-width: 0;
    }

    .callout-card,
    .highlight-card {
      padding: var(--space-5) var(--space-6);
    }

    .flow-card {
      padding: var(--space-5) var(--space-4);
    }

    .code-block {
      font-size: 0.78rem;
      padding: var(--space-4);
    }

    .code-block--bad::before {
      font-size: 0.7rem;
      padding: 0.1rem 0.4rem;
    }

    .details-card__summary {
      padding: var(--space-4);
      flex-wrap: wrap;
    }

    .details-card__hint {
      flex-basis: 100%;
      padding-left: calc(var(--space-3) + 1em);
    }

    .details-card__body {
      padding: var(--space-4);
    }
  }
</style>
