---
import BaseLayout from '../../../../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="自製 MCP Server：從課堂筆記助理開始 — AI Agent 拼圖"
  description="第一次自己做 MCP Server，不是為了做更大的系統，而是為了做一個更清楚、更安全、更符合自己情境的小工具。從一個只處理課堂筆記的小 server 開始：讀取、列出、產生反思草稿，邊界畫得很清楚。"
  lang="zh"
>
  <section class="section">
    <div class="container ai-agent-custom-server">
      <a class="back-link" href="/zh/">&larr; 返回首頁</a>

      <header class="detail-header">
        <h1 class="detail-title">自製 MCP Server：從課堂筆記助理開始</h1>
        <p class="detail-subtitle">
          前面我們已經用過現成 MCP，也看過 Notion 可以幫我們搜尋、讀取、建立頁面。但現成 MCP 通常能力很多，適合一般情境，不一定剛好適合初學者。很多時候，我們只是想讓它<strong>完成一小段很清楚的任務</strong>。
        </p>
      </header>

      <p class="detail-paragraph">
        所以接下來我們要做的，不是一個很大、很完整、什麼都會的 AI 系統。我們只做一個很小的 MCP Server：它可以讀取指定資料夾裡的課堂筆記，依照固定格式產生反思草稿，<strong>而且不會亂改原始資料</strong>。
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          自製 MCP Server，不是為了做更大的系統，<br/>
          而是為了做<strong>更清楚、更安全、更符合自己情境的工具面</strong>。
        </p>
      </div>

      <!-- Section: MCP Server as tool provider -->
      <h2 class="detail-heading">先把 MCP Server 想成「工具提供者」</h2>
      <p class="detail-paragraph">
        很多人第一次聽到「自己做 MCP Server」，會以為自己要做一個新的 AI。其實不是。AI 還是 Gemini，我們要做的 MCP Server，<strong>比較像是站在旁邊的「工具提供者」</strong>。
      </p>

      <p class="detail-paragraph">它會告訴 Gemini CLI：</p>
      <ul class="check-list">
        <li>我這裡有<strong>哪些工具</strong>可以用</li>
        <li>這些工具<strong>可以做什麼</strong></li>
        <li>每個工具<strong>需要哪些輸入</strong></li>
        <li>做完之後<strong>會回傳什麼結果</strong></li>
      </ul>

      <p class="detail-paragraph">
        MCP 的 Python SDK 可以用來建立會提供 <code>tools</code>、<code>resources</code>、<code>prompts</code> 的 server；而 Gemini CLI 可以透過 <code>settings.json</code> 連接這些 MCP server，並讓模型呼叫裡面的工具。
      </p>

      <div class="callout-card">
        <p class="callout-text">
          所以你可以把整件事想成：<strong>Gemini CLI 是你和 AI 工作的地方，MCP Server 是你自己做的一組小工具。</strong>
        </p>
      </div>

      <!-- Section: stdio vs HTTP -->
      <h2 class="detail-heading">這次用的是 stdio，不是 HTTP</h2>
      <p class="detail-paragraph">
        前面我們接 Notion MCP 時用的是 HTTP，也就是 Gemini CLI 去連一個已經存在的網址。但這次不一樣，我們要做的是<strong>本機 MCP Server</strong>，使用的是 stdio。
      </p>

      <div class="highlight-card">
        <p class="highlight-text">
          stdio 可以先簡單想成：<br/>
          <strong>Gemini CLI 會在你的電腦裡啟動一個 Python 程式，然後透過標準輸入與標準輸出和它溝通。</strong>
        </p>
      </div>

      <p class="detail-paragraph">
        所以這次不會有像 <code>https://mcp.notion.com/mcp</code> 這樣的網址，也不會用 <code>--transport http</code> 去連遠端服務。這次的邏輯比較像這樣：
      </p>

      <div class="flow-card">
        <div class="flow-step">Gemini CLI</div>
        <div class="flow-arrow">↓ 啟動</div>
        <div class="flow-step flow-step--accent">python server.py</div>
        <div class="flow-arrow">↓ 提供</div>
        <div class="flow-step">tools / resources / prompts</div>
      </div>

      <p class="detail-paragraph">
        Gemini CLI 支援不同的 MCP transport，其中 stdio 是「啟動一個子程序，透過 stdin/stdout 溝通」；HTTP 類型則是連到 HTTP 或 SSE 端點。
      </p>

      <!-- Section: What we are building -->
      <h2 class="detail-heading">這次我們要做什麼</h2>
      <p class="detail-paragraph">
        這裡先不要接 Notion API。因為第一次自己做 MCP Server，如果同時遇到 Python、MCP、API、金鑰、權限、網路請求，<strong>很容易分不清楚到底是哪裡出錯</strong>。
      </p>

      <p class="detail-paragraph">
        所以第一個版本，我們先做一個很小的：
      </p>

      <div class="project-card">
        <p class="project-label">第一個自製 MCP Server</p>
        <p class="project-name">課堂筆記助理</p>
        <p class="project-desc">
          它只處理電腦裡指定資料夾中的 Markdown 筆記。
        </p>
      </div>

      <div class="boundary-grid">
        <div class="boundary-col boundary-col--can">
          <p class="boundary-label">它可以做</p>
          <ul class="check-list boundary-list">
            <li>讀取有<strong>哪些</strong>課堂筆記</li>
            <li>讀取<strong>某一份</strong>課堂筆記的內容</li>
            <li>根據內容<strong>建立一份新的反思草稿</strong></li>
          </ul>
        </div>
        <div class="boundary-col boundary-col--cant">
          <p class="boundary-label">它不能做</p>
          <ul class="cross-list boundary-list">
            <li>不能<strong>刪除</strong>原始筆記</li>
            <li>不能<strong>修改</strong>原始筆記</li>
            <li>不能讀取<strong>指定資料夾以外</strong>的檔案</li>
            <li>不能自動<strong>交作業</strong>或發布內容</li>
            <li>不能替學生<strong>完成最後定稿</strong></li>
          </ul>
        </div>
      </div>

      <div class="callout-card">
        <p class="callout-text">
          這樣的設計看起來很小，但很重要。因為好的 AI 工作流，不是看它一開始能不能做很多事，而是看它能不能<strong>在清楚的邊界裡，把一件小事穩定做好</strong>。
        </p>
      </div>

      <!-- Section: Tool surface design first -->
      <h2 class="detail-heading">先設計工具面，不要急著寫程式</h2>
      <p class="detail-paragraph">
        在寫程式前，我們先把工具面想清楚。這個「課堂筆記助理」可以分成<strong>三種東西</strong>：
      </p>

      <!-- Tools -->
      <div class="case-header">
        <p class="case-label">TOOLS</p>
        <h2 class="case-title">真的會動手做事的工具</h2>
      </div>
      <p class="detail-paragraph">
        這次我們會做四個 tools。
      </p>

      <div class="tool-box">
        <p class="tool-box__label">Tool 1</p>
        <p class="tool-box__name"><code>hello</code></p>
        <p class="tool-box__desc">
          用來確認 MCP Server 有沒有成功啟動。
        </p>
      </div>

      <div class="tool-box">
        <p class="tool-box__label">Tool 2</p>
        <p class="tool-box__name"><code>list_notes</code></p>
        <p class="tool-box__desc">
          列出 <code>class_notes</code> 資料夾裡有哪些課堂筆記。
        </p>
      </div>

      <div class="tool-box">
        <p class="tool-box__label">Tool 3</p>
        <p class="tool-box__name"><code>read_note</code></p>
        <p class="tool-box__desc">
          讀取某一份指定的課堂筆記。
        </p>
      </div>

      <div class="tool-box">
        <p class="tool-box__label">Tool 4</p>
        <p class="tool-box__name"><code>create_reflection_draft</code></p>
        <p class="tool-box__desc">
          在 <code>drafts</code> 資料夾裡建立一份新的反思草稿。
        </p>
      </div>

      <!-- Resource -->
      <div class="case-header">
        <p class="case-label">RESOURCE</p>
        <h2 class="case-title">給 AI 參考的固定資料</h2>
      </div>
      <p class="detail-paragraph">
        這次我們會做一個 resource：
      </p>

      <div class="tool-box">
        <p class="tool-box__label">Resource</p>
        <p class="tool-box__name"><code>course://reflection-rules</code></p>
        <p class="tool-box__desc">
          它裡面放的是課堂反思草稿的固定規則。
        </p>
      </div>

      <p class="detail-paragraph">例如：</p>
      <ul class="check-list">
        <li>反思要包含<strong>「我理解了什麼」、「我還卡在哪裡」、「我可以怎麼應用」</strong></li>
        <li>不要直接替學生完成最後作業</li>
        <li>如果資料不足，要先<strong>提醒使用者</strong>，不要自己亂補</li>
      </ul>

      <!-- Prompt -->
      <div class="case-header">
        <p class="case-label">PROMPT</p>
        <h2 class="case-title">可以重複使用的工作流程</h2>
      </div>
      <p class="detail-paragraph">
        這次我們也會做一個 prompt：
      </p>

      <div class="tool-box">
        <p class="tool-box__label">Prompt</p>
        <p class="tool-box__name"><code>draft_reflection</code></p>
        <p class="tool-box__desc">
          把「產生課堂反思草稿」這件事整理成固定流程。
        </p>
      </div>

      <p class="detail-paragraph">這個流程大概像這樣：</p>

      <ol class="reflect-list reflect-list--single">
        <li class="reflect-item">
          <span class="reflect-num">1</span>
          <span class="reflect-text">先<strong>讀取</strong>指定筆記</span>
        </li>
        <li class="reflect-item">
          <span class="reflect-num">2</span>
          <span class="reflect-text">再找出和主題<strong>有關的重點</strong></span>
        </li>
        <li class="reflect-item">
          <span class="reflect-num">3</span>
          <span class="reflect-text">再參考<strong>反思規則</strong></span>
        </li>
        <li class="reflect-item">
          <span class="reflect-num">4</span>
          <span class="reflect-text">先產生草稿給<strong>使用者確認</strong></span>
        </li>
        <li class="reflect-item">
          <span class="reflect-num">5</span>
          <span class="reflect-text">使用者同意後，<strong>才建立草稿檔案</strong></span>
        </li>
      </ol>

      <div class="highlight-card">
        <p class="highlight-text">
          先把 tools、resource、prompt 各自的用途想清楚，<br/>
          <strong>實作的時候會輕鬆很多。</strong>
        </p>
      </div>

    </div>
  </section>
</BaseLayout>

<style>
  .ai-agent-custom-server {
    max-width: 52rem;
  }

  .back-link {
    display: inline-block;
    margin-bottom: var(--space-8);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  /* Header */
  .detail-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    line-height: 1.3;
  }

  .detail-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  .detail-subtitle strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Section headings */
  .detail-heading {
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--color-heading);
    margin-top: var(--space-12);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }

  .detail-paragraph {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    margin-bottom: var(--space-4);
  }

  .detail-paragraph strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Case header (TOOLS / RESOURCE / PROMPT) */
  .case-label {
    display: inline-block;
    margin-bottom: var(--space-0);
    padding: var(--space-1) var(--space-4);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.08em;
    color: #fff;
    background-color: var(--color-accent);
    border-radius: var(--border-radius);
  }

  .case-title {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-heading);
    margin: 0;
    line-height: 2.4;
  }

  .case-header {
    margin-top: var(--space-12);
  }

  /* Inline code */
  :global(.detail-paragraph) code,
  :global(.detail-subtitle) code,
  :global(.check-list) code,
  :global(.cross-list) code,
  :global(.callout-text) code,
  :global(.highlight-text) code,
  :global(.tool-box__desc) code,
  :global(.boundary-list) code,
  :global(.reflect-text) code {
    padding: 0.1em 0.4em;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 0.9em;
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
    border-radius: var(--border-radius);
  }

  /* Check list */
  .check-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .check-list li {
    font-size: var(--text-base);
    color: var(--color-text);
    line-height: 1.9;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .check-list li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 700;
  }

  .check-list li strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Cross list (✗ items) */
  .cross-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .cross-list li {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.9;
    padding-left: var(--space-6);
    position: relative;
    margin-bottom: var(--space-2);
  }

  .cross-list li::before {
    content: '\2717';
    position: absolute;
    left: 0;
    color: var(--color-text-secondary);
    font-weight: 700;
  }

  .cross-list li strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Callout card */
  .callout-card {
    margin: var(--space-6) 0 var(--space-8);
    padding: var(--space-4) var(--space-8);
    border-left: 4px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
  }

  .callout-text {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.9;
    margin: 0;
  }

  .callout-text strong {
    color: var(--color-heading);
    font-weight: 600;
  }

  /* Highlight card (key takeaway) */
  .highlight-card {
    margin: var(--space-4) 0 var(--space-6);
    padding: var(--space-3) var(--space-8);
    border: 1px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-tag-bg);
  }

  .highlight-text {
    font-size: var(--text-base);
    color: var(--color-heading);
    line-height: 1.8;
    margin: 0;
    text-align: center;
  }

  .highlight-text strong {
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Flow card (Gemini CLI ↓ python server.py ↓ tools) */
  .flow-card {
    margin: var(--space-6) 0;
    padding: var(--space-3) var(--space-4);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg-secondary);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-2);
  }

  .flow-step {
    display: inline-block;
    padding: var(--space-2);
    background-color: #fff;
    color: var(--color-heading);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    font-size: var(--text-sm);
    font-weight: 600;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  }

  .flow-step--accent {
    color: var(--color-accent);
    border-color: var(--color-accent);
  }

  .flow-arrow {
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    letter-spacing: 0.05em;
  }

  /* Project card (課堂筆記助理) */
  .project-card {
    margin: var(--space-4) 0 var(--space-6);
    padding: var(--space-3) var(--space-8);
    border: 2px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-tag-bg);
    text-align: center;
  }

  .project-label {
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.08em;
    color: var(--color-accent);
    margin-bottom: var(--space-2);
  }

  .project-name {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--color-heading);
    margin-bottom: var(--space-2);
  }

  .project-desc {
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.7;
    margin: 0;
  }

  /* Boundary grid (能做 / 不能做) */
  .boundary-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
    margin: var(--space-6) 0;
  }

  .boundary-col {
    padding: var(--space-3) var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background-color: var(--color-bg);
  }

  .boundary-col--can {
    border-top: 3px solid var(--color-accent);
  }

  .boundary-col--cant {
    border-top: 3px solid var(--color-text-secondary);
    background-color: var(--color-bg-secondary);
  }

  .boundary-label {
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.08em;
    color: var(--color-heading);
    margin-bottom: var(--space-3);
    text-transform: uppercase;
  }

  .boundary-list {
    margin-bottom: 0;
  }

  .boundary-list li {
    font-size: var(--text-sm);
    line-height: 1.8;
  }

  /* Tool box */
  .tool-box {
    margin: var(--space-4) 0 var(--space-4);
    padding: var(--space-2) var(--space-6);
    border: 1px dashed var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg);
    text-align: center;
  }

  .tool-box__label {
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.05em;
    color: var(--color-accent);
    margin-bottom: var(--space-2);
    text-align: center;
  }

  .tool-box__name {
    margin-bottom: var(--space-3);
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-heading);
    text-align: center;
  }

  .tool-box__name code {
    padding: 0.15em 0.5em;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 1em;
    background-color: var(--color-tag-bg);
    color: var(--color-tag-text);
    border-radius: var(--border-radius);
  }

  .tool-box__desc {
    margin: 0;
    font-size: var(--text-sm);
    color: var(--color-text);
    line-height: 1.8;
  }

  /* Reflect list (steps in the prompt flow) */
  .reflect-list {
    list-style: none;
    padding: 0;
    margin: var(--space-6) 0 var(--space-6);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3);
  }

  .reflect-list--single {
    grid-template-columns: 1fr;
  }

  .reflect-item {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    padding: var(--space-4) var(--space-5);
    border: 1px solid var(--color-border);
    border-left: 3px solid var(--color-accent);
    border-radius: var(--border-radius);
    background-color: var(--color-bg);
  }

  .reflect-num {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 50%;
    background-color: var(--color-tag-bg);
    color: var(--color-accent);
    font-size: var(--text-xs);
    font-weight: 700;
  }

  .reflect-text {
    font-size: var(--text-sm);
    color: var(--color-heading);
    line-height: 1.6;
    font-weight: 500;
  }

  .reflect-text strong {
    color: var(--color-accent);
    font-weight: 700;
  }

  /* Mobile */
  @media (max-width: 720px) {
    .boundary-grid {
      grid-template-columns: 1fr;
    }

    .reflect-list {
      grid-template-columns: 1fr;
    }

    .case-title {
      font-size: var(--text-lg);
    }

    .callout-card,
    .highlight-card {
      padding: var(--space-5) var(--space-6);
    }

    .project-card {
      padding: var(--space-5) var(--space-6);
    }

    .tool-box {
      padding: var(--space-4) var(--space-5);
    }

    .flow-card {
      padding: var(--space-5) var(--space-4);
    }

    .reflect-item {
      gap: var(--space-3);
      padding: var(--space-4) var(--space-5);
    }
  }
</style>
